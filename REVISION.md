# 修订与校验说明

正文的定稿版本是 **v1.5**；**v1.5.2** 在此之上做了四处作者批准的正文改动（删整节摘要、解空间那一节前移、两处措辞换字），**v1.5.3** 只调了图 3 内一行注记的基线，**v1.5.4** 修掉我在 v1.5.2 引入的三处问题、按作者的两处纠正改齐附录口径，并新增 `UNDERSTANDING.md` 把理解落盘。逐条记在下面。这份文件记录四件事：正文改过什么、成本账的数字从哪来、交付是怎么做出来的、以及怎么重建。

## 一、正文改过什么

**正文文字自定稿提交 `1771d49`（"正文补三处附录所需的架构锚点"）起只有下面这张表里的改动**——其余所有提交只插入了引用角标、重编号，或改了附录；每一轮都用同一套指标做过"零差异"比对。

| 位置 | 原文 | 改为 |
| --- | --- | --- |
| 序言 L29 | `进入到”任务判别器“的上下文，再被组装上”项目整体目标”` | `进入到“任务判别器”的上下文，再被组装上“项目整体目标”` |
| 序言 L45 | `然后再次读取...` | `然后再次读取……` |
| 序言 L49 | `如何做减法...那有没有可能` | `如何做减法……那有没有可能` |
| 全书 108 行 | 372 个 ASCII 直引号 `"…"` | 中文引号 `“…”`（正文 10 个、附录2 256 个、附录3 106 个） |
| 序言 L65 | `一颗纯净的，被构建的前缀“种子”` | `一颗纯净、被构建的前缀“种子”` |
| 正文 L89 | `压缩与重建的全部差别` | `压缩/剪枝与重建的全部差别` |
| 正文 L93 | **作者在公开留档后自己插入的一整段** | 保留其结构、语气与观点，只把措辞与概念拧准（逐条见下） |

规范化由 `normalize-punctuation.mjs` 完成，规则写在脚本里：**每行内按出现次序配对替换，出现奇数个引号的行一律跳过并报出**（不做猜测，避免把开引号写成闭引号）。干跑结果 108 行 / 372 个，跳过 0 行；没有引号落在行内代码或公式里。替换后全篇 ASCII 直引号 0 个、中文引号左 205 / 右 205（平衡）。

**作者插入的那一段，改了这几处**（结构与观点一字未动）：

- **「对错相互抵消」→「错的代价不是损失，是少省了一部分」。** 对与错不会相互抵消，是相减；而“错的代价不是损失”既准确又更强——猜错不产生损失，只是少省了一部分。
- **「缓存尾」→「多出来的那部分落在最便宜的一档」。** 该词全文无定义；而且正文到这一行还没引入价格表（价格表在附录2），所以这里只说“哪一档”，不写单价。
- **给预取类比加了一句限定**：预取是硬件顺手完成的，这里的“预取”要模型自己判断、自己写下来。不加这句，类比会被读成“两者机制相同”。
- **建筑比喻换成与全文同源的表述**：`高楼大厦已经垒起，抽掉其支撑结构` → `内容已经垒起来、后面的东西又压在它上面`。全文的隐喻系统是“缓冲 → 投影”“删除 → 不构建”，抽掉支撑结构是另一套。
- **补了一个指向**：“思考与工具调用的轮次会跟着收敛，输出与缓存也随之少下去”属于**没有计进账里**的收益（见 A2.6）。不加指向，读者翻到 A2.4 会以为它与账里的单一 $T$ 冲突。
- **加粗用作者级粗体承担（两处），没有加进渲染器的 `EM` 列表。** `EM` 是字面串匹配——作者改一次序言那句问句，对应的强调就静默失效（本轮已修）；往 `EM` 里加条目只会让这个脆弱面变大。

**附录2 A2.6 本轮新增两条，并给第 3 条配了价签：**

| 位置 | 加了什么 |
| --- | --- |
| 第 3 条 | 基线的核验费：九次压缩共丢 194.8 万 token，把丢掉的重读一遍来核对摘要，按未命中价是 **¥1.95**，等于它整张账单的 **28%**；本架构的“校验”是把该装的内容装出来，顺带完成 |
| 第 7 条（新） | 延迟与吞吐，带三条甲档证据（[16][17][37]），并注明按 A2.4 的取法不计入结论 |
| 第 8 条（新） | 装配容错余量：¥1.32 的余量允许每轮多装约 3.2 万 token 废料（正常装配 60k 的一半以上），斜率是命中价；真正的上限是 63k 那个注意力预算，不是这 3.2 万 |

**v1.3.1 只重做了强调装置，正文一个字没动。** 这句话有机械证据：把 v1.3 与 v1.3.1 两版的 Markdown 都去掉 `*` 之后逐行比对，839 行全部相同、两边都是 57,662 字符；`git diff` 的改动落在 10 个 hunk、20 行，每一行只差 `*` 字符。正文的加粗标记从 23 处增到 33 处。成品层面也一样：两版 PDF 用 `pdftotext` 取文字、去掉空白后比对，**前 34,006 个字符逐字相同**，差异只在末页「版式说明」那一句改写与页脚版本戳。

| 位置 | 改了什么 |
| --- | --- |
| 正文全书 | 强调的来源从"渲染层里的一份字面量表"改成"作者写在 Markdown 里的 `**`"：原先的 `EM`（12 条字面串）与 `EQ`（2 条等式）整份删除。理由不是洁癖——`EM` 已经因为作者改过一次问句而静默失效 |
| 正文 L61 / L69 | 旧公式「Agent = Harness + Model」原与本文主张共用同一层等式底色，观感上两者等重；现将旧公式退回普通引文，只留本文「Agent = Environment + Context + Model」有强调 |
| 正文 L69 | 补一处原先漏掉的重点：决定模型输出状态的只有参数与上下文 |
| 正文 L143 | 强调从对冲词「有待实验验证」移到本段的真结论上（传统累计思路会受对齐需求与探索复利的反噬） |
| 正文 L151 | 「build上下文」（6 字）扩成整句主张（30 字） |
| 正文 L155 | 补一处：正文收尾那句追问（Harness 的革新成本与风险要小得多）原先没有任何重点，而它前面两个问句都有 |
| 正文 L73 | 撤掉整段的强调：这一段（"这句话反映的是两个层级…"）与上一段的等式胶囊说的是同一件事，而它本身只有一行半，涂满底色等于给整段加底 |
| 正文 L89 | 撤掉打在「内容离开上下文，不再等于失去内容」上的强调——它与作者自己加粗的「取消了一个约束」只隔一个冒号，两块底色并排会读成渲染故障；作者原有的加粗一处未动 |
| 正文 L147 | 撤掉「并行写者N²的冲突」——那段本来就挂着一条边注（→A3.4 同类踩坑：N² 写冲突），行内再涂一块是重复；而且这一块正好跨行，行尾只剩两个字，是视觉评审挑出来的最刺眼一处 |

装完之后做了两轮**独立的视觉评审**（把成品 PDF 的第 4–8 页栅格化，再逐块量底色的像素范围与文字盒），改掉三处只有看图才会暴露的东西：**两种宽度的底色差得不够**（每通道 8/255，隔开就分不出）、**整句胶囊的竖直内边距太大**（相邻两行只隔 3px，连读像给整段加了底）、**角标紧贴胶囊右缘**（看起来像连角标一起涂了底）。第二轮复核确认前两处已解决（深浅差从 2.6 拉到 4.9 个 L\*；相邻行白缝 0.6mm→1.06–1.27mm）并指出还剩一处行尾两字残块，就是上表 L147 那一行的由来，同时把竖直内边距压到 0（白缝到头约 2mm，是行高 6.6mm 减字身框的极限）。第五页原先的密度被指出"强调的选择性基本消失"，这也是撤掉 L73 那一段的理由之一。

**v1.4 只动引用：附录3 从 40 条扩到 52 条，正文只挂了一处新引用。**

起因是一个合理的质疑——"附录读起来像整个领域都在反对本文"。查下来的结论是：**支持并不少，是位置吃亏**。A3.3 原本 14 行、行行写着"支撑"，可 A3.2（分界）、A3.4（坑）、A3.5（没人做过）连着占了附录3 的后半；同时本文确实漏引了一批**支持自己**的甲、乙档文献。这一版把两头都补上。

| 位置 | 改了什么 |
| --- | --- |
| A3.1 | 新增 12 条：[41] Agentless（FSE 2025，甲）、[42] HippoRAG（NeurIPS 2024，甲）、[43] ACON（ICML 2026，甲）、[51] Li et al.（EMNLP 2024，甲）、[46] Building effective agents、[47] Context editing、[48] Memory tool（乙）、[44] Context Rot in Coding Agents、[45] Context Engineering 综述（丙）、[49][50] LangChain 两篇、[52] permafrost 实测记录（丁）。**全部以【原文】核验**——逐条打开原始页面读过，不是转述 |
| A3.3 | 补 7 行支撑：固定阶段胜过自由探索[41]、单步取回胜过反复迭代[42]、外部清单胜过模型自检[44]、"简单可组合"优于复杂框架[46]、工业界官方口径已转向让工具结果离开上下文[47][48][49]、上下文工程已是独立学科[45]；另加一行**双向**证据——干净上下文只在开头挡得住继承来的杂乱[44]，它同时给"种子"划了时效边界 |
| A3.2 | 补 2 行分界：ACON[43]（本文的对照面里最强的一篇，原先是漏的）、LangChain Deep Agents[49]（同样是"卸载 + 按需取回"，但仍是累积历史） |
| A3.4 | 把两处**没有编号的来源**补成正式条目：MCP 工具顺序致命中率 71%→33% 的那条"社区实测"实为 permafrost 的公开实测记录[52]；Li et al. 是 EMNLP 2024 的甲档论文[51]。另加一行"fork 继承父会话 vs 子代理另起一份"的两难[50] |
| A3.6 | 补一行口径：**前缀字节相同是必要不充分**——缓存身份还包含客户端 HTTP 头指纹与请求参数，字节不变而换一套请求头会掉到 0%；且跨会话预热不可能[52] |
| A3.5 / A3.7 / A3.8 | A3.5 第二条补一句"ACON 报的也是总账、没有单列压缩开销"；A3.7 的甲档条数 10 → 14，并注明本轮 12 条全为【原文】；A3.8 结尾补一段：**缺的不是支持，是把这些支持拼成"每轮重建"的那一次对照** |
| 正文 L57 | 这一版唯一一处正文改动：那句"20–30% 甜点、不严谨"后面挂上 [44]（编码代理白盒实测：onset 不是稳定阈值）。原先的自谦因此变成有出处 |

扩写时踩过一次表格陷阱，记在这里备查：新行一开始插到了 A3.2 表格的**收尾段落之后**，于是它与相邻那行被当成一张只有表头的新表，其中一行被渲染器静默吞掉（`rows.slice(2)` 的既有行为）。`verify.mjs` 的逐行比对把它抓了出来（1 行未命中），插回表内后为 0。

**v1.4.1 是排版修补：把“整表不跨页”这条规则拿掉。**

正文与附录文字一字未动（除参考表里 16 个日期的连字符）。起因是视觉评审发现三处跨页断口：A3.2、A3.4、A3.6 三张表因为打印样式里的 `table{break-inside:avoid}` 被整张推到下一页，分别留出 67 / 95 / 154 mm 的空白。

| 位置 | 改了什么 |
| --- | --- |
| `build-html.mjs` 打印样式 | 从 `tr,.callout,.fig,table{break-inside:avoid}` 里去掉 `table`，改成 `table{break-inside:auto}` ＋ `thead{display:table-header-group}`：表格像 A3.1 那张长表一样带表头跨页，行仍然不拆 |
| 附录3 A3.1 | 16 个 ISO 日期改用不间断连字符（U+2011），日期不再被折成 `2026-` / `02-05` 两行 |
| 附录3 A3.8 | 新增的那一段从引语框里移出来——原先它与上面那条加粗引语共用一个满宽底色，7 行全粗，是全书最重的一块 |

效果是量出来的，不是看出来的：**底部留白合计从 960mm 降到 618mm**（均 25 → 17mm/页），留白超过 30mm 的页从 9 页降到 4 页（其中第 2 页是目录页、末页是收尾，本来就短），**成品从 38 页收到 37 页**。

**另有两类纯格式改动，对成品零影响：** 编辑器把 26 张表的**分隔行与单元格**重排成对齐宽度（渲染器 `rows.slice(2)` 直接丢弃分隔行、单元格一律 `trim()`，列宽不受影响）；序言等处的 `>` 后面补了空格。

另有一个决定：**正文不加编号体系**。正文里没有按编号的交叉引用（引用全部指向附录 A2.x / A3.x）。原先那 6 个「导航组」是挂在段落上的不可见锚点，v1.5 已把它们落成可见的三级标题（见下）。

**v1.5 是评审驱动的核验修正 + 可读性交付。**

起因是三份独立评审（一份对抗式学术评审、一份阅读体验评审，加我自己按九个角度的评分）在**同一处**撞车，另外两份各自找出若干可核验的缺陷。成立的全部修掉。

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 附录2 敏感性表 | 「不并行（$N=1$），其余同基准」那一行原先写 ¥6.25 / 省 11%，是**一次换了两件事**（既改 $N$ 又改了前缀计价）；按"其余同基准"应为 **¥6.67 / 省 5%**。现补成两行，两档都留着 | 三份评审独立发现：同一变更在同一节里是两个数，而 A2.7 的"省 5%"才是对的 |
| A2.7 | 标题与全文口径从「三条例外」改成**四条例外**，新增第四例：压缩间隔 $k_{b}$ 取了公式值 202 步而不是实测的 230 步 | 这个取法偏在有利的一侧（基线重置 ¥0.30→¥0.26，整张账单 ¥7.02→¥6.99，省 19%→18.4%，**0.6 个百分点**）。A2.0 第三条规矩问的正是"这些保守取法是不是真的都在对我们不利"——它就是那次复查漏掉的 |
| A2.0 | 删掉「隐含兑换率约 200 : 1，与价格表直接算出的 200 完全吻合」 | 那张表的金额列本来就是用价格表乘出来的，"吻合"是恒等式、不是经验佐证。真正的实测事实是写量只有背量的 **0.51%（193 : 1）**，这句留着，结论一分不少 |
| A2.4 | 删掉与 A2.0 / A2.4 逐行重复的"参数取值"表（它额外只给了 $P$、$s$ 两个分量，已并入 $P+s$ 那一行） | 同一批数字在同一节讲两遍 |
| A2.3 | 给"压缩税"补一句定义（写摘要 + 缓存作废后的重读 + 丢掉的细节日后被重新推导） | 它原先只被抛出，没有被定义 |
| A3.6 | 新增一行口径：**坐标是版本相关的**——本文没有指定坐标的绑定方式（绑提交哈希，或照 [30] 的 SHA-256 地址库存取，都可行），也没有给出"坐标写全了没有"的校验（**v1.5.2 已按新口径改写成"映射是版本相关的"**） | 能校验的是"装回来的字节与原文一致"，不能校验的是"该写进去的有没有漏"。两份独立评审都指向这里，是本版最重要的一处诚实补记 |
| A3.1 | [12] 补上可核位置（github.com/redhat-ai-americas/memory-hub）、[37] 补上 NeurIPS 2025 poster 链接 | 支撑"命中率是常态"的条目原先没有可核标识 |
| A3.7 | 新增两条：**[10][11] 缺可核标识、只作方向性参考**（它们独立支撑的结论另有 [7][12] 两条可核来源包住）；**A2.0 第三条规矩漏了一处的补记** | 同上 |
| 序言 | 两对引号方向写反（`：”要不…简历“。`、`怎么样的”零花钱地狱“。`）改正；`神鬼二相性` → `神鬼二象性`；`限时很骨感` → `现实很骨感` | 都是打错字。**`梁文峰` 保留原样**——按作者要求不动这一处 |
| 新增「摘要」（**v1.5.2 已整节移除**，此条只作历史保留） | 一页以内，全部由正文已有句子重组：一句主张、四个台阶与"台阶四＝0"、19% / 66% / 贵 12% 三个数，以及那三个可以否掉的问题 | 三份评审都把"全文无摘要、第一屏是日记"列为最主要的进入成本 |
| 新增图 3 | **手写 SVG 架构拓扑图**（不是 mermaid 自动布局），接在正文"具体实现"一节之后 | 前两版只有两张成本图；全文的核心贡献是一个结构，一张结构图都没有 |
| 图 3 的复核修补 | 视觉评审量到三处：主 Agent 的箭头尖、虚线组框左缘、分支 1 左缘挤在 1.3mm 内；箭头头只有 1.3mm 宽；图内那行"共享的是环境…"与图注重复。**分支行整体右移、箭头画到紧贴分支框、加了一个"委派"小标签、箭头头加宽到约 1.8mm、删掉图内那行**。终审又指出该标签只有约 6.2pt、是全图最小字号，于是分支行再右移 8 个单位、标签提到 **8pt**（左右余量各约 1.7mm），并把图内其余注释从 12px 提到 **13.5px**——与另外两张图的最小字号（`.cax`）拉平，全文图内不再有低于 7.6pt 的文字 | 这几处都是只有把成品栅格化量像素才会暴露的；语义上"前缀派生分支"的误读也一并挡掉 |
| 标题与摘要的分级 | 打印样式里 h3（正文 6 个小标题与附录 A2.x / A3.x）从 10.5pt 提到 **11.5pt**、颜色加深、上间距 7mm；h2 提到 13.5pt。摘要正文降为 **9.75pt** 并左右缩进 7mm，末尾补一行**关键词** | 原先小标题与正文同为 10.5pt，只靠加粗区分，等于没有分级；摘要原先与正文完全同款 |
| 一处样式覆盖 bug | 打印块里的 `.abs` 被文件后面同优先级的屏幕规则盖掉，**摘要实际渲染成 11.25pt、比正文还大**（墨高比 1.073＝11.25/10.5）。改选择器为 `main .abs` 后墨高比 0.929＝9.75/10.5 | 这类"后面的规则赢"只有把成品量一遍才会暴露；记在这里备查 |
| 正文 6 个可见小标题 | 把原先"挂在段落上的不可见锚点"落成**可见的三级标题**（问题的由来 / 换一个主体 / 四个推论 / 具体实现 / 相对主流的优势 / 边界与未来），同时删掉渲染层里的 `GROUPS` 机制 | 目录原先承诺这 6 个标题、纸面一个都没有，PDF 读者按目录翻页找不到对应标题 |
| README / CITATION.cff | 口径与数字对齐：版本 v1.5、字数 2.3 万→2.4 万、日期统一，删掉"凡是有利的量一律按 0 计"的绝对句 | A2.7 已明说四处例外，而 README 比正文更绝对，方向反了 |

**这一版正文文字仍是零改动**——摘要、图 3 与 6 个标题都是新增的独立块，正文原有句子一字未动。机械证据：把正文区（`### 正文` 到 `### 附录1` 之间）与上一版逐行比对，差异**只有 6 行 `####` 标题加 18 行空行**，没有一句正文被改；序言区差异只有 2 行、都是打错字。`verify.mjs` 的逐行比对从 490 行升到 494 行、未命中仍为 0；引文角标 115 → 124（新增的口径与清单条目里挂了引用），52 条双向可达 52/52。

**v1.5.2 把纲目摆正，并修掉一处说反了的机制。** 正文只动四处：删掉整节摘要、把解空间那一节从正文最尾前移、换掉两处与机制冲突的措辞。**除删除与移动之外，正文没有新增一句我的解释**——解空间那一段是作者原话，前移之后正文的顺序自然变成：命题 → 解空间 → 具体实现 → 优势 → 边界。

作者对本版定的口径：**文章的核心是 Context 解耦换掉了解空间，Git 树只是那个解空间里的一个点。** 原先正好相反——解空间那一节压在正文最尾、连标题都没有，而实现的账（摘要里的省 19%）占着结论位。

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 摘要（整节，含关键词行） | **删除** | 作者判断它没有价值、也不是论文体例。它承载的三个判据在 A2.5 原文里都有，四个台阶在 A2.2 |
| 正文 | 「Context成为一等公民……」＋解空间那两条，从正文最尾**整体前移到「具体实现」之前**，标题「被换掉的是解空间」取自该段原话 | 层次靠位置摆正，不靠加解释 |
| 正文（前缀定义） | `必要的代码与意图映射汇总` → `必要的意图映射汇总` | 原文不属于前缀：主 Agent 按需存取，子 Agent 的原文是委派切片；前缀只装跨轮稳定的那一段 |
| 正文（合并那一步） | `进行**坐标申报**——写下“东西在哪里”，由 Harness 按坐标把原文装配成新的前缀。（这一步是写地址，不是重写正文……）` → `进行**更新**——由主 Agent 重写需要模型更新的部分，其余部分由 Harness 从环境分别装配成新的前缀。（没变的部分逐字节不动，附录2 会算这笔账。）` | 原句把机制说反了：合并后更新的是**需要模型更新的那一段**（意图映射），其余各段由 Harness 从环境**分段装配**；前缀里并不装一批靠坐标取回的原文 |
| 附录2 | 「申报量 $M$」→「更新量 $M$」；「写的是地址、不是内容」→「写的是变动的那一段」；「随时可照坐标取回」→「要用的时候读一次」；A2.1 第 4 行、A2.3 的 $E_d$ 说明与 A2.4 的拆项改成**分段装配**口径 | 同一处机制贯穿改正。**数字与公式一个没动**：$M$、$\Delta P$、$E_d$、$R_d$ 与 13 个金额全部原样 |
| 附录2 开头 | 新增一段：这一节量的是**文中那处具体实现（Git 树）**的账；换一处实现、换一组参数，数字会变，而四个台阶、三个判据、有效残留的形状不变 | 原先读者无法判断这些数字属于范式还是属于这处实现 |
| A3.0 | 新增三行：分清「上下文原生」（那一步解耦）、「Git 树」（本文给的一处实现）、附录2 的数字（这处实现的账） | 全文「本架构」原先同时指三层，是读者对不上的根源 |
| A3.2 / A3.4 / A3.5 | 各加一句：这张表是**分区图**不是优劣表；这些坑连起来是这条路能用与不能用的**边界**；这一栏测的是**解耦本身** | 三处原先读起来像本实现的防守与优劣 |
| A3.6 | 「按坐标把原文装配成新前缀／坐标是版本相关的」改为「模型每轮重写需要模型更新的那一段／**映射是版本相关的**」，缺口照旧 | 结论（这是设计缺口）保留，理由换成新口径：能校验的是装回来的字节与原文一致，不能校验的是该写进映射的有没有漏 |
| A3.4 表内 | 「本文不重写正文：Harness 按坐标重装原文」→「本文不让模型平铺重写正文：模型只重写该更新的那一段，原文留在环境里、要用的时候读一次」 | 后者正落在 GRACE「结构化更新 0.673 对平铺重写 0.191」的**正结果**一侧，比原说法更贴证据 |
| 图 3 | 重画：前缀条内补一行成员（人设与工具 · 意图映射 · 系统状态）；底部条改为「单写者合并 · 重写意图映射 M ／ Harness 分段装配」；回路箭头**止于前缀条右边界**；删掉左侧「下一轮」虚线；图注撤掉原先那句「上下文原生」的定义 | 视觉复核发现箭头终点在 `XR-8`，也就是进入前缀条内约 1.6mm；这处越界量的是标记、不是文字，历次审计都没有报出来 |
| `build-pdf.mjs` | 目录页识别阈值从「一页出现 ≥4 个标题」改成「不少于目标数的四成、且不少于 8」 | 附录3 那一页同时出现 4 个小标题，被误判成目录页，那三条标题因此查不到页码、目录里出现空页码；改后 30/30 |

**v1.5.2 作废 v1.5 记录里的三条**（都因摘要整节移除）：上表里「新增摘要」那一条、标题分级里的摘要部分、「一处样式覆盖 bug」。三条只作历史保留，`.abs` 相关样式已从渲染器里删掉。

机械核对：`verify.mjs` 逐行比对 **493 行、未命中 0**；胶囊 29 → **27**（整句 17 / 短语 10，摘要那节移除后少了 2 个短语胶囊）；成品仍 **38 页、209 个链接**；图内文字越界 0、表格横向溢出 0。

**一处如实记录的回退：底部留白从 421mm 升到 471mm**（均 11.1 → 12.4mm/页）。原因是删掉摘要后整体前移约半页，三张图（在第 7、15、24 页）恰好都落在「本页放不下、只能推到下一页」的位置，第 6、14、23 页因此各留 53 / 44 / 70mm。图不能拆开，这是分页本身的代价，不是溢出——溢出正文框 0、图内文字越界 0。若要把这三处补回来，得动图的锚点或图内装的内容，属于另一轮。

**v1.5.3 只调了图 3 内一行注记的基线**，正文、附录与其余两张图一字未动。成品栅格化复核量到：前缀条右侧那行「逐字节不变 → 缓存命中」原先停在条内两行文字之间的空档里（下缘离成员行只剩 1px），既不与标题对齐、也不与成员行对齐；把它提到与标题同一基线后复核确认——两端基线差 1px（不可辨），与标题横向留 395px、与成员行留 10px 白，回路箭头尖仍落在前缀条右边界上、正对条身的竖向中心。

**README 在 v1.5.3 之后改成面向读者**：摘要从正文里整节移除之后，由 README 顶上它的位置——先用外行能懂的说法讲清这篇在说什么（上下文是什么、压缩为什么贵、解耦换掉的是什么），再给三个数字和四条阅读入口。**README 不再讲构建**：正文本身就随仓库提供，读者不需要知道它是怎么生成的；原来那个折叠块里的构建链、脚本表与环境变量，整体搬到本文件第六节。

**v1.5.4 是口径收口：正文三处精准修复、附录两处纠正、把理解落盘。**

正文只动三处，**全部是 v1.5.2 我引入的问题**，作者指出后按"不改文风结构"修复：

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 正文（合并那一步的括注） | 恢复成「（这一步重写的是**变动的那一段**，不是**整篇前缀**：一段短，整篇长，两者的价钱差一个量级，附录2 会算这笔账。）」 | 我在 v1.5.2 把原来的括注换成「没变的部分逐字节不动」时，**删掉了一个正文里别处没有的点**（写的量小一个量级，$M$ 对 $P$），**又插进了一句十六行后 L169 已经说过的话**——同一节里说两遍。现在按原句的节奏把那一点写回来 |
| 正文（同一句的主体） | 「由主 **Agent** 重写」→「由主 Agent／**委派 Agent** 重写」；加粗从通用动词「更新」移到机制术语「**分别装配**」 | 上半句的主语本来就允许委派，我上一版把它缩成了主 Agent；「更新」是句中最泛的动词，撑不起一个术语胶囊，而"分别装配"与正文别处的「最大前缀组装」「装配时段序稳定」同族 |
| 正文 L169（作者原话，一处换词） | 「前缀为了信息密度**每轮整体重写**」→「每轮**都要更新一版**」 | 修完上面那条之后，它成了正文里唯一读起来像"模型把前缀整篇重写一遍"的表述，与 L153 的新口径挨着看会打架；只换一个短语，不动句子结构 |

附录按作者两处纠正改口径，**数字与公式一个没动**：

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| A2.6 第 1 条 | 撤掉「本架构不是让模型更强，而是让同一个模型少受长上下文拖累」 | 这句话本身是退让。**更短、更干净、更聚焦的上下文就是能力上的更强**；[1] 的 focused/full 与"人停手的位置中位 30.9%"两条证据本来就在这条里 |
| A2.7 第二条 | 重写：$\theta_d=20\%$ 是我们的能力假设；**而基线那一侧的 8% 连假设都不是**——它只有一层上下文，理解与干活挤在同一段内容里，串行又不拆分，就不可能不带着足量的信息工作；压到 8% 省下的正是它干活所必需的那部分理解。另加一段正面说明：**低水位不是设计出来的，是拆分出来的；而拆分的前提是先有一次全量理解**。"两边都按在 31% 时贵 12%"降为**算法核对** | A2.3 原来只说到"那个最优点把 ρ 与'丢掉的东西一文不值'推到极致"，缺的是**结构上的理由**：为什么基线到不了低水位。这一条由作者补上 |
| A2.4 / A2.5 / A2.8 | θ_d 说明与敏感性表那一行改标"算法核对，不是我们会选的位置"；推力一表里 8% 那档由"不可达"改成"纸面点：串行不拆分，它站不住"；A2.8 的"看数"段与结尾引语按新口径重写 | 同一处口径在四处出现，必须一起改，否则读者会在 A2.4 读到旧的让步说法 |
| A3.4 表内 | 「本文的主张不是"小上下文更强"」→「不是"任何条件下小上下文都更强"，而是"把理解与干活分成两层之后，更短、更聚焦的上下文更好，而且不必因此丢内容"」 | 同上一条：撤掉对能力主张的自我限缩，同时保留对 Li et al.[51] 与 Chroma[1] 条件冲突的诚实说明 |
| 图 2 图注 | @8% 那一档补上"为什么" | 原先只说"工程上不可达"，没有给理由 |

**新增 `UNDERSTANDING.md`：把理解落盘，供下一个读者或下一个 agent 复用。** 内容分八节：一句话、三层称呼、命题、机制（前缀是什么／谁产出谁装配／更新时机／"回读"不是禁忌／主 Agent 与子 Agent 的差别／低水位从哪来）、成本口径、为什么比现状优的九条、边界与缺口、**已经读错过并被作者纠正的八条**、文件地图。它明写"不替代正文，口径冲突以正文与附录为准"。

**排版这一轮也动了一处渲染层**：窄胶囊（`.em`）加 `white-space:nowrap`。起因是我在合并那一步新加的两个短语胶囊里，有一个正好折在行尾、只剩一个字，那个字的底色带加右侧内边距就顶出版心 2px——这是"溢出正文框 0"这条线在本轮第一次报警，也顺势给自检补了一条诊断：**报出问题元素的文字与行内片段数**（原先只说 `span.em 右溢 2px`，看不出是谁）。加 nowrap 后归 0；整句胶囊不加这条，因为 20–30 字的主张必须允许折行。**代价是成品从 38 页变成 39 页、底部留白 471→624mm**，主体来自附录2 那段口径纠正净增约半页——这一笔单独记在第七节。

**v1.5.5 是作者与我的第二轮对齐：正文六处定性收口，理解文档三处实质改正。**

起因是作者要求「根据理解文档继续对齐，还有什么内容是你质疑的，聚焦正文，关注定性」。我提出十二条质疑，作者逐条回答后，**六条落成正文改动**（全部为半句到一句，不改文风、不动结构）：

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 正文 L95（推论3 普适） | 「上下文照样要重建、切分、合并、重载，只是没有需要维护的环境实体」→「**上下文那时就是全部环境，状态同样要**以一个个可指认的单元保存下来、像文件一样被管理**（写下来、取出来、换版、弃置），只是没有需要维护的环境实体」 | 我把普适版读成"抽掉环境后四步没有依据、退化成改名"，原句字面正好支持这种读法——它把"没有环境实体"写成了唯一差别。作者的原意是**四步的形态**：纯 chat 里状态仍要以单元形式、像文件一样被管理 |
| 正文 L101（投影） | 在"新框架里上下文是投影"后补一句：新上下文里住着**两种东西**——环境里已有的（代码、状态、依赖）离开随时能重新取出；环境里没有的（"这个功能落在哪些结构上"这份理解）离开就得重新读懂一遍，所以它不离开，读懂之后写下来、作为前缀被每步复用、只在合并时更新一次 | "投影"只覆盖环境里有的东西。**前缀里最贵、最必须每步被读的那一段（意图映射）没有原像**——它不是投影回来的，是写下来的。这一条作者认同 |
| 正文 L105（构建可被规划） | 加限定：「**重启点由任务边界给定，到边界重建一次，边界内按需存取**」 | 主 Agent 的循环重启**本身就是重建**，只是以任务边界为粒度；我上一版在理解文档里写成"只适用于子 Agent 那一侧"，是把"粒度不同"错写成"只有一侧成立" |
| 正文 L105（"错"的定义域） | 「猜错了」→「猜偏了，也只是多背进来一点**为真却用不上**的背景信息」；结尾括注：这里的"错"指的是装了为真却用不上的内容，装进来与事实不符的东西，代价仍是做错决策，不落在这一档 | 原文的保证（错的代价不是损失）只在**多带了为真但无用的内容**时成立；意图映射写错是**冲着错的地图干活**，那不是少省一部分。作者认同 |
| 正文 L125（"协议"） | 「环境 + 协议」后补括注：**协议即每个模型自己的上下文与工具拟合形状**，见附录1 | "协议"全篇只出现这一次，且与别处的"契约"不是一个东西。作者明确：协议＝每个模型特定的上下文与工具拟合形状，即解耦 Context 之后得到的**模型上下文耦合兼容层**，落点就是附录1 的"在上下文组装环境为每个模型进行分野" |
| 正文 L157 / L175（能力口径） | 「确保了模型能力发挥稳定」→「**让同一个模型发挥得更充分**」；「中段迷失和能力下降被有效控制」→「中段迷失与能力衰减被压住，而**更短、更聚焦的上下文本身就让同一个模型发挥得更好**」 | 这两句还是"防止下降"的框架，而作者两轮纠正过的口径是"就是更强"。**注意 L105 本来就写了"干净上下文本身的能力增益"**，所以正文并非整体保守，不一致只在这两处用词 |

顺手修掉附录2 里两处遗留的 ASCII 直引号（L572「能在低水位上干活」、L641「少受拖累」）——0 直引号是硬不变量，这两处是我在 v1.5.2 / v1.5.4 改附录时留下的。

**还有一处是排版复核逼出来的**：我插在 L105 段首的那句限定（27 字）把整段往后推，同一段里那个 33 字的整句胶囊「没有对与错，只有对多少与错多少；而错的代价不是损失，是少省了一部分」折行后，第二行行首只剩一个「分」字带深色底块——视觉复核报出来的唯一瑕疵（v1.5.4 里同一句折在「失」之后，完好）。整句胶囊必须允许折行，所以不能用 v1.5.4 那种 `nowrap` 的解法；把插入句的「边界之内」紧成「边界内」一个字，折行点退回一格，该胶囊现在整句落在同一行、残片归零，复核确认瑕疵消失。**这是我自己的插入句在适配版面，没有动作者的原句。**

理解文档（`UNDERSTANDING.md`）这一轮有三处**实质改正**，不只是补充：

| 位置 | 原来（错） | 改成 |
| --- | --- | --- |
| §3 机制 | "构建可被规划"的适用范围是**子 Agent 那一侧**，不是无条件的 | **两侧都成立，差别只在粒度与触发点**：主 Agent 以任务边界为粒度重建自身上下文，子 Agent 以"分派时的那份确定任务"为粒度 |
| §2 命题 | 上下文是投影 | 补上"**投影只覆盖一半**"：环境里有的才是投影，环境里没有的（理解）是写下来一次的，靠复用摊薄 |
| §3 / §6 / §7 | 无 | 新增"协议"的定义；新增"错的代价不是损失"的定义域；读错清单从八条增到十二条（推论3 的读法、序言"申报坐标"不是架构机制、"不反对耦合"与"拒绝认知错位"分属训练层与认知/架构层、"构建可被规划"的粒度） |

**这一轮不改的**：序言（那里的"申报坐标"是 dsh-price-less 插件的现行设计，与架构无关，只在理解文档里点明）；推论1／推论2 的性质（记账边界移动、定义的回声）；"换一个主体"实质是边界移动；"必然反噬"缺一环——后三条补论证会把正文从"提出解空间"变成"自我辩护"。这些留在理解文档的边界里。

**v1.5.6 是第二轮术语对齐：正文四处措辞收窄。**

作者就我提的十个"不明白"逐条作答（答案全部落进 `UNDERSTANDING.md`，见下），其中四处需要动正文，作者逐条批准：

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 正文 L105（我上一轮插进去的那半句） | 「重启点由任务边界给定，**到边界重建一次**」→「重启点由任务边界给定**（一个目标之内，每完成一轮归并又是一次）**，到点重建一次」 | 我上一轮把"任务边界"与"合并"读成了两个互斥的触发。作者的口径是**两个尺度上的同一个东西**：任务边界＝用户目标（目标级重启）；目标的实现途径就是归并，一轮末归并完成就是一次大更新（轮级重启）。正文那半句原来只写到目标级 |
| 正文 L157 | 「其具有**理解/执行模型解耦**」→「其具有**理解与执行解耦**」 | 删掉"模型"二字。作者的口径：**解耦的是职责，不是模型实例**——模型一致性的目的是同一主子模型下把缓存对齐最大化；"理解/执行模型解耦"会被读成"理解模型 + 执行模型"两个模型 |
| 正文 L153 | 「并同时会对系统状态、**代码映射**等进行更新」→「**意图映射**」 | 作者确认二者同指；"代码映射"全文只此一处，其余（L137、L187 与附录 282/296/338/374/649）都是"意图映射"，统一掉 |
| 正文 L187 | 「即可轻松回退到**主Agent**/少分支结构上」→「即可轻松回退到**少分支、乃至单分支**的结构上，**落地仍然是委派**」 | 作者的不变量：**对齐理解必须在主 Agent，具体落地必须委派——哪怕不分支也要委派**。原文"回退到主Agent"会被读成主 Agent 自己动手；不易解耦的真实成本是分支少→缓存共享的摊薄变差 |

同一轮把口径整体落进 `UNDERSTANDING.md`（单独提交 `7e91bcc`）：两个尺度的重启、响应式并发的声明/信号模型（用户与子 Agent 都是输入信号，像响应式＋声明式 UI）、"最大前缀组装"的两个"最大"（共享字节最大 ＋ 保住文件本身的结构性以对齐 tools 格式）、段序（易变段固定在前缀尾部）、环境（底层一份＋每分支一份虚拟工作区、写时才占空间，难点是虚拟文件的编译与测试）、底层压缩仍在且只不触发、极端兜底＝收敛为线性、模型一致性与"理解／执行解耦的是职责"、线性是分支循环重启结构的**特例**、冲突由主分支依契约上下文审查择优。读错清单 **12 → 17 条**。

**视觉复核（p6 / p8）与一处如实记录**：p6 那个 33 字的整句胶囊这次折出 17 字续行，没有孤字底块；p8 末行只有「样。」两个字，复核标注为需要修——查过之后**它不是本轮改出来的**：v1.5.5 里同一段的同一处折行本来就是这个两字末行，只是当时它落在页面中部（后面还接着下一段）。本轮内容把后面各推下两三行，它才落到页面底部；而 `p{orphans:2;widows:2}`（v1.3 起就在打印样式里）要求下一段若要在 p8 开头就得留下两行，p8 只剩 **12.7mm**、不足两行（24.5px ≈ 6.5mm ×2），于是下一段整体移到 p9。**这是控制规则在正常工作，不是排版错误**；能解它的是 `text-wrap:pretty`（Chrome 会主动避免过短的末行），但那会重排全部 39 页、可能改页数与目录，属于版式约定，留给作者定。

**v1.6 是整套图重做：正文三张、附录2 三张，编号按阅读顺序重排。**

起因是作者问「图有没有过时或者需要更新？我觉得目前的图没有完整体现我们架构真正好的设计点」。我先做了一轮图审计，结论是：

- **图 1（四个台阶）不过时**——单价与步数逐项校对过；
- **图 2（三种活法）图内标签过时**：@8% 那一档的副标注还写着「不可达」，而 A2.5 的表格与图注在 v1.5.4 已改成「纸面点：串行不拆分，它站不住」——**只有图内那个字没跟着改**；
- **图 3（一轮的动作）有四处与现行口径不符**：环境框写「环境共享」（而 L97 与作者口径是"底层内容一份 ＋ 每分支一份虚拟工作区、写时才占空间"）、前缀只画三段漏了「项目结构说明」（A2.6 是四段）、合并栏只写"重写意图映射 M"（口径是"重写需要模型更新的那一段"，含系统状态）、**完全没有段序**（易变段在尾部才是"逐字节相同"成立的前提）；
- 更要紧的是**它画的是一张流程图**：回答"一轮里发生什么"，不回答"为什么这样就好"。我请一个只拿图不看文章的读者描述它，他记住的是"一份共享前缀＋四个并行分支＋一次合并"这个**形状**，而答不出「P 是什么」「意图映射对应哪一块」「回环线送回什么」。而全篇最硬的三个机制点——**低水位是拆分出来的**、**共享的摊薄形状**、**两个尺度的重启**——一张图都没有。

所以这一版新增三张、重画一张、沿用两张，共六张，编号按阅读顺序（正文 1–3，附录2 4–6）：

| # | 位置 | 论点（图顶那行字） | 状态 |
| --- | --- | --- | --- |
| 图 1 | 正文·「被换掉的是解空间」末 | 理解与干活分成两层，上下文才可能一直短 | 新增 |
| 图 2 | 正文·「具体实现」首 | 一轮里变的是尾部那一段，不变的是整片前缀 | 重画 |
| 图 3 | 正文·「具体实现」尾（合并那段之后） | 重启有两个尺度：目标与轮；一轮之内不换前缀版本 | 新增 |
| 图 4 | 附录2·A2.1「本架构的成本来源」表后 | 未命中按前缀版本数计，不按分支数计 | 新增 |
| 图 5 | 附录2·A2.2 台阶表后 | （账图，原图 1） | 编号顺延 |
| 图 6 | 附录2·A2.5 活法表后 | （账图，原图 2，图内「不可达」→「纸面点」） | 改一个字 |

**这套图的规矩**（写在这里，以后新增图按它来）：每张图顶上一行论点行；一图不超过六个主体块、不画两条时间轴；**图注只写图上没有的信息**（时序、谁执行、口径来源、边界），不重复图内标签；图内自足（正文一处"见图 x"都不加，所以图必须自带面板标题与口径来源）。

**排版是四轮视觉复核逼出来的**，每轮都改后重跑构建再复核（我读不了图，只能靠"请一个只拿图、不看文章的读者描述它"来验，标准是三件事：只看图能说出论点、他记住的那件事＝论点、他答不出的问题里没有"这块对应哪块"这类机制性缺口）：

| 图 | 复核抓出的缺陷 | 修法 |
| --- | --- | --- |
| 图 1 | 论点行用了「低水位」，而**这个词在正文里从未出现**（首次出现在附录2 L484） | 论点行改成白话「理解与干活分成两层，上下文才可能一直短」，图注里给"水位"下定义并说明附录2 会用这个说法 |
| 图 1 | 我拿 `.cempty`（空心方块）当"纸面点"的标记，而这个标记在图 5 里表示"0（不付）"——**同一套图里一个符号两个意思** | 去掉方块，改成两行文字 |
| 图 1 | 右面板是 ①②③④ 的平级列表，**"两层"没画出来**；左面板灰框没有名字 | 加"理解层／干活层"两个标签与一条分隔线；左面板标题写成"基线：上下文只有一层" |
| 图 1 | 那条分隔线贴死下一行（下间距 2px） | 上移到两行之间（11px / 12px） |
| 图 2 | 环境那行说明**骑在虚线容器边框上、左端露在框外** | 容器上移、标签下移，四边全在框内 |
| 图 2 | 合并条里粗体行与灰字行只隔 4px | 条体加高、两行拉开（净空 11px） |
| 图 2 | 回环箭头指在**最后一格**（系统状态）上，而标签说的是"公共前缀" | 在整片前缀右侧加一条带上下短钩的括线，箭头指它 |
| 图 2 | 从横条向下的四支箭头**扎进虚线容器** 6px | 缩短到容器上方 |
| 图 2 | 稳定段／易变段只靠底色深浅区分（差 12 级），灰度打印会消失 | 两段各加一条带钩的括线，钩落在格线上 |
| 图 3 | 面板中央那支竖直箭头**正压在"分支 3"的左边框上**——而"哪一支在写回"全靠它表达 | 改成四支箭头，分别从"一版前缀"底部落到每个分支框中心；分支行→归并框同样四支 |
| 图 3 | 分支行右端比上下两条通栏框短 11px | 四格重排，右端对齐 |
| 图 3 | 「目标达成」「下一轮直接用新版前缀」两句**没有引线，不知挂在哪** | 前者加一条短引线接到上面横条；后者移进面板内、紧贴归并框下方 |
| 图 4 | 起点处横轴、基线、本架构三条线挤在一起（5–6px），看不出谁是谁 | 拉开层级，并把第三条线（若按分支数计）**改成文字对照**，不再塞进同一个小坐标区 |
| 图 4 | 基线折线右端以**悬空竖线断头**；虚线也中途断头 | 阶梯改成末级平收，两条线都画到坐标区右端 |
| 图 4 | 起点三个小圆点**相切粘成一串**，且让人误读成"黑线从灰点起笔" | 去掉圆点（身份由图例承担） |
| 图 4 | 图注写"计费次数"，图上写"累计重读次数"；图注里还混了一个直引号 | 图注统一成"重读次数"，引号统一为弯引号 |

**正文一字未动**：图与图注都长在渲染层（`build-html.mjs`），所以逐行核对仍是 **495 行未命中 0**。**代价是 40 页（+1 页）**，但底部留白反而从 618mm 降到 **607mm**——新图把原先空着的地方吃掉了。

**v1.6.1 是作者的一处口径改动：模型一致性从“要求”降为“更好”。**

作者原话：「具有模型一致性改为，对模型一致的情况下更优，模型不一致也没问题。」正文只动这一句：

| 位置 | 原文 | 改为 |
| --- | --- | --- |
| 正文 L133 | `其分为主Agent与子Agent，具有模型一致性。` | `其分为主Agent与子Agent，两者模型一致时更优，模型不一致也没问题。` |

原句把这套结构写成了**要求**主与子同模型；作者的口径是它只是一个更优的情形——v1.5.6 已经定过“解耦的是职责，不是模型实例”，所以分叉、契约、按需读回、合并都不依赖模型是否相同。

配套两处都在正文之外（附录与图注）：

| 位置 | 加了什么 | 为什么 |
| --- | --- | --- |
| A3.6 口径表 | 新增一行：**前缀单元按模型分家**——同一段字节换一个模型就不构成命中，所以“未命中按前缀版本数计、不按分支数计”与 $p_{h}$ 那一档只在**同模型**下成立；跨模型时各分支按各自模型的价目重算 | 账里的摊薄（图 4）建立在“一版前缀被所有分支吃到缓存”上。正文既然允许模型不一致，这处边界就必须写出来，否则读者会把同模型那一档的价钱当成无条件结论 |
| 图 4 论点行 | 在图顶那行“未命中按前缀版本数计，不按分支数计”后面补“（同模型；见 A3.6）” | 图 2、图 4 的**图注**各加一句边界的写法试过：图 4 图注多一行就把整块图推到下一页、在第 14 页留出 77mm 空白（页数不变但多一个洞，留白合计 535 → 517mm）。改放在图 4 自己的论点行末尾——它正是画着“命中 ×4”的那张图；图内第一行右侧空着约 350 单位，不触发行数变化，留白合计反而降到 **487.4mm**（同一把尺：每页 275mm 减最低正文字符下缘；v1.6 用这把尺实测是 535.1mm，而第七节里 v1.5.4–v1.6 那串数字是更早的算法，两者不可直接比） |

数字与公式一个没动；$p_{h}$ 的全篇用法与四条例外都不受影响。

**视觉复核（第 14 页，图 4）**：把成品栅格化，请一位只拿图、不看文章的评审读那一页。三件事被确认：新增的论点行是**单行**、右括号完整、上下 50px 内没有别的墨迹；左侧那块的文字最右到 x767、右侧坐标块最左在 x834，**没有一行越过两块之间的分界**；图注三行完整落在页内。同时量到两处 v1.6 那四轮复核没抓到的瑕疵：图内两支箭头的**箭尖各差 7–8px（≈1.2mm）没落到目标框上**（横箭头的尾端贴着源框、头端却悬空，首尾不对称），已把横箭头终点 `164 → 169`、竖箭头终点 `136 → 141`，两支都贴到框上。评审还提到右图两条阶梯交叉处会夹出一个无图例的小方块、“本架构”的竖直段不在基线两级台阶的中点上——这两处是画法本身的产物（错开层级是为了不让横段互相压住，方块是交叉的副产物），未改。

**机械核对**：`verify.mjs` 逐行比对 **496 行、未命中 0**（比 v1.6 多 1 行，就是 A3.6 新加的那一行）；角标 125 → **126**（那一行挂的 [52]）、条目 52、双向 52/52；成品 **40 页、211 链接**（+1）、表 25、图 6；图内文字越界 0、表格横向溢出 0/25、内部链接悬空 0、目录 30/30 与 `_tocpages.json` 一字未变（页码表与 v1.6 完全相同）。底部留白 **487.4mm**（同一把尺下 v1.6 是 535.1mm），超 30mm 的页从 4 页（p2 / p24 / p39 / p40）降到 **3 页**（p2 / p24 / p40）——A3.6 新增那一行正好补上了第 39 页原先的 44.5mm 空白。

**v1.7 是可读性收口：正文九处最小改动、附录二十处口径与定义，账里的数字一个没动。**

起因是作者问「还有什么可以增强可读性、理解困难的点」。我把正文、附录2 的账、附录3 分别交给三位**只读一遍、不看别处**的生读者，再逐条自己复核（数字类全部重算过）。结论：**主线是通的**——三个人写下的「记住的一件事」都落在本文主命题上（上下文可以是每轮重照环境重建的投影；不构建≠删除；省的钱大头是根本没构建）；卡点集中在两类：**正文的自造专名先出现、后定义**，和**附录2 的数字口径不严**。

正文这一版只动九处，全部是夹注、指针、改名或一个多余的「的」，**没有增删观点，也没有动语感**：

| 位置 | 改了什么 | 为什么 |
| --- | --- | --- |
| 正文 L133 | 「…其分为主Agent与子Agent，两者模型一致时更优，模型不一致也没问题。」→「…其分为主Agent与子Agent。**对单模型串行、对“贵模型规划、便宜模型执行”，这套结构都更优**；而主与子用同一个模型时更优——同一份缓存前缀在两边命中，成本结构比分模型更好，模型不一致也不影响结构成立。」 | 作者要把这句的意思说全：两种对比（单模型串行、贵规划＋便宜执行）都更优；结构内部同模型更优、分模型不影响成立 |
| 正文 L243 | 「四个台阶（A2.2）」→ 加六字「（A2.2：同一份内容在账上的四种计费状态）」 | 有读者把「台阶」读成「四个阶段（时间先后）」，带着这个误读读了一百行 |
| 正文 L245 | 「三个判据」「有效残留」各加六到十五字当场定义 | 同上：两个词先出现，定义在 55–355 行之后 |
| 正文 L105 | 「见 A2.6」→「见附录2 A2.6」 | 正文从不用 A2.x 这种编号，读者此刻不知道「附录2」存在 |
| 正文 L109 / L117 | 「Agent 切片」→「Agent 运行单元」（两处） | 与附录符号 $s$ 的「切片」撞名，有读者明确问过这是不是一个东西 |
| 正文 L137 | 「**任务的拆分的**具体原则」→「**任务拆分的**具体原则」 | 一个多余的「的」。这处是视觉复核读那一页时顺带报出来的，属打错字，不是口径 |

「最大前缀组装」的「最大」没有写进正文——那句是作者原话，定义补在 A3.0 更省。

附录改二十处，**全部是口径与定义；数字、公式与四条例外一个没动**。其中六处是读者算出或读出不一致的地方：

| 位置 | 原来 | 改成 |
| --- | --- | --- |
| A2.2 $E_{b}$ 公式 | $E_{b}=\sigma p_{o}+C_{c}p_{m}$ | $E_{b}=\sigma p_{o}+C_{r}p_{m}$：第二项按**重读段** $C_{r}$（实测 9.2k）计，不是残留 $C_{c}$（15.2k）。**照原式算是 ¥0.0352，而表里是 ¥0.029——公式与代入值原来对不上**；符号表补 $C_{c}$、$C_{r}$ 两行 |
| A2.2 实测校验 | 「低估约 28%」 | 「低估约 22%（实测比式算高约 28%）」：162.6k 对 208,853 是 77.9% |
| A2.4 一、 | 「我们的重置频率约为基线的 $1/N$」 | 「重置间隔是一轮的 $N\cdot L$ 次调用（384 步，基线 202 步），**约一半**；这一次重置的钱由 $N$ 个分支分摊」。$1/N$ 要成立，得轮长等于基线的压缩间隔，而实际轮长 96 步是它的一半 |
| A2.4 一、读法 | 「只要“前缀 + 切片”不超过 63k 我们就赢；本节保守取的 60k 刚好卡在线内」 | 补前提：63k 那一行是「更新 2k ＋ 前缀按命中」；本节保守档（10k ＋ 整段未命中）只有 **34k**——**按最保守的取法，我们赢不下基线的纸面最优点**（A2.7 第三条那张 106.8k / 98.4k / 92.0k / 77.5k 对 80.5k 的表本来就写着输赢） |
| A2.5 | 「管与不管的每步成本差 **1.33 倍**」 | 换成可复算的口径：「不管控」与「有人管」每步成本差 **2.4 倍**（¥16.97 对 ¥7.02）。1.33 无法从本节任何数字复现 |
| A2.0 | 「背着 38.5% + 重置 2%」 | 「背着 + 重置」——那张表上只有 38.5 / 21.7 / 39.8 三行，没有 2% 这个数 |

另有四处是「读者无法核对、只能问作者」的，按其性质补了口径：

| 位置 | 补了什么 |
| --- | --- |
| A2.3 表后 | 表内「触发占用」与「丢弃＋保留」横向对不上（九行差 16.5k–76k）：前者是那一刻**整个请求**的占用，还含人设与工具清单、环境视图这些**不进消息历史**的部分，后者是 Harness 对**消息历史**报出的数。**这一句的成分判断是我按两份数的性质推断的，作者可核** |
| A2.4 二、 | 「全部预算 ¥0.0115」补出处：这是 $P+s$ 取 60k 时**每一版前缀能负担的重置上限**，由 $R_{d}<R_{b}$ 解出（$(80.5\mathrm{k}-60\mathrm{k})^{2}\cdot Np_{h}/(2g)$），与「整段未命中时光读前缀就 ¥0.02、越线」自洽 |
| A2.0 | 三条会话与锚定会话的关系：构成占比用三条合计（9,023 步、54 次压缩）；**A2.3 起的账锚在 2,068 步、9 次人工压缩那条**（`session-b5f92412`），另有 455 步那条当「不管控」的例子；**两组数不要互相折算** |
| A2.7 第二条 | 「这一条要靠 A2.6 证明」→「这一条不在算术里：**A2.6 给的是方向性证据，完整的证明还没有做**（见 A3.5 第三条）」。原句是全书唯一一处「按指引进去找不到答案」，会让读者以为 19% 的能力前提已经被证明 |

其余是定义与指针：A2.6 开头明写这一节是「未计账的收益」清单、不是能力证明；符号表补 $\overline{L}$ 与 $C_{r}$；$\theta_{b}^{*}=8\%$ 那里的「每 45 步」注明「31% 时是 202 步」（同一个 $k_{b}$ 在两个触发点上是两个值）；A3.0 补「核验＝原文/转述」的释义与「最大前缀组装」的两个「最大」；附录1 开头点明「这一节说的就是正文里的『协议』」（「协议」全篇只出现一次，而被指向的附录1 原先通篇不再出现这个词）；三处表头补主语或单位（成本表首列「情形」、敏感性表「（省幅）」、活法表「一次 2,068 步的账单」）；A2.6 第（1）条补一句「这条主张有边界、也有反向证据，见 A3.3」。

**新增 A2.6 第（9）条：跨模型的那条分工，不靠「把计划再发一遍」。** 常见的做法是贵模型规划、便宜模型执行，计划每轮重新生成或重新递一遍，执行侧读到的是一份跨模型拼起来的上下文，没有一个能一直命中的缓存单元；本结构里贵模型写下的是一份短、稳的前缀，此后每轮只重写变动的那一段，执行侧读的是逐字节不变的同一份字节，**各自模型内部都能一直命中**；主与子同模型时这份前缀还能在两边共用，不同模型时少的只是这一层。这条是定性比较，没有进账——本文的账算的是同模型那一档。A3.6 那一行同步补上「跨模型丢掉的只是这一层折扣」。

**机械核对**：`verify.mjs` 逐行核对 **504 行、未命中 0**（v1.6.1 是 496 行）；角标 126、条目 52、双向 52/52；表 25 块列数一致、图 6 张、胶囊 35 处（整句 19 / 短语 16）、专名 2 处；行内 KaTeX 207 → **217 处**（符号表与公式里新增的 $C_{c}$、$C_{r}$、$\overline{L}$）；内部链接 134 个目标、悬空 0；图内文字越界 0、表格横向溢出 0/25、残留占位符 0、正文可见星号 0。

**版面代价如实记录：成品 40 → 41 页**，底部留白 487.4 → **511.3mm**（同一把尺），超 30mm 的页 3 → **5 页**（p2 目录页、p19、p24、p25、p30）。增的是内容——正文与附录净增 251mm，约一页。五处空白全部发生在**不能拆开的块**（引语框、图、表）被整体推到下一页的位置，不是溢出（溢出正文框 0、图内文字越界 0、表格横向溢出 0）；要收掉它们只能砍掉这一轮加进去的口径，或者让引语框允许跨页拆分——后者动的是既有的版式约定，留给作者定。

**另有一处视觉复核顺带报出的打错字**（正文 L137「任务的拆分的」→「任务拆分的」），已计入上面的九处。

### v1.7 增补：正文新增一条「在需求表达层面」，五条优势变六条

作者提出「这个架构还有一个好处：提示词工程的重要性下降了，可以更随意更轻松地对齐需求，而不是把人往 AI 的方式上逼」，并指定落在正文。这一条是主命题的**推论**而不是新论点——单上下文的活法里，人的意图只能靠一段话进入系统，于是「把需求写对」成了一门要人迁就模型的手艺；解耦之后这件事由环境、契约、前缀与按需读回接走——因此并入「相对主流的优势」那张清单，**插在「在透明度层面」与「在模型适配性层面」之间**：L171 刚说完「需求对齐与实际落地解耦」，新条紧接着说这个解耦对人是单向减负，是递进不是并列；而末尾两条仍落在模型那一侧（适配性、能力），该节的收尾力度不变。

衔接靠三处，都不动既有文字：开口用「这种解耦」回指 L171；段内的四个构件（环境、契约、前缀、按需读回）全部是上文已有的术语，不引入新词；收口回指本文自己的账（装配段序稳定、最大前缀组装），把这条定性好处挂在已有论证上，免得它变成一句感想。全文唯一一处跨节回指是「序言里那个『提示词优化器』想做的事，到这里换了一种更省的做法」——序言 L21 把提示词优化器列为四个重大构想之一、L29 说它「将用户的提示词扩写锚定」，这句让那条研发线与正文接上；作者认可保留。

**这条自带边界**，照 L167 的体例（优势后紧跟一句但书）收尾：降下来的只是**修辞那一层**（人设、格式模板、咒语），升上去的是**结构那一层**（什么进前缀、段序、给哪些工具）——本文的账正是靠后一层赢的；而省下的是表达不是判断，目标仍要人来定，改目标也仍要付一次重启的钱。

**格式：** 初稿把末两句整段加粗，`build-html.mjs` 的阅读增强把它识别成 **101 字**的「主张·宽胶囊」（全篇其余胶囊 6–39 字），会渲染成跨两行的色块。改为只加粗「降下来的是修辞那一层，升上去的是结构那一层」**21 字**，括号内的例示与后半句回到常规字重，与全篇胶囊尺度一致。

**机械核对**：`verify.mjs` 逐行核对 **505 行、未命中 0**（v1.7 是 504 行，多的就是新增那一行）；角标 126、条目 52、双向 52/52；表格 25 块列数一致、图 6 张、胶囊 36 处（整句 20 / 短语 16）、专名 2 处；内部链接 134 个目标、悬空 0；溢出正文框 0、图内文字越界 0、表格横向溢出 0/25、残留占位符 0、正文可见星号 0。正文引号 **262/262** 平衡、ASCII 直引号 0。

**版面：成品仍 41 页、211 链接**（净增四行没有跨页）；目录 30/30 重解析，只有两个三级标题后移一位——**A2.0 计费模型与符号 11 → 12、A2.2 两种钱、四个台阶 15 → 16**。六张图与 `build-html.mjs` 一字未动，按作者意见不做图体复查。

### v1.7 增补二：交付核验（链接、页码、指针），查出并修掉一处页码错

作者要求核对「超链接与页码是否正常、指针引用是否准确」。这需要一层比 `verify.mjs` 更深的核对——`verify.mjs` 查得到「链接没断」，查不到「指到哪一页」和「指对了没有」。为此在成品仓外写了 `_cn_audit/linkcheck.mjs`：把成品 PDF 解开到对象层，页树展开成页码，命名目标字典（`/sec-7 [61 0 R /XYZ …]`）解成目标页，再与 `_tocpages.json`、目录印刷页、PDF 每页实际文字**三方对照**。

**链接机制先说清**：成品 211 个链接全是内部命名目标（`/Dest /sec-N`），**没有 `/URI` 外链**——引用条目本身不带 URL，与源文件一致。134 个不同目标（目录 30 ＋ 角标 52 ＋ 回链 52），**悬空 0、解析不到页码 0**；211 个实例 ＝ 角标 126 ＋ 回链 52 ＋ 目录 30 ＋ 页边注 3。

**查出并修掉两处：**

| 位置 | 问题 | 处置 |
| --- | --- | --- |
| 目录「具体实现」 | 印 **6**，标题实际在 **7**（链接也跳到 7）。根因在 `build-pdf.mjs` 的 tocpages：它按「标题文字在正文里**第一次出现**的页」定页码，而「具体实现」这个词在第 6 页的行文里先出现一次（“Git 只是一个具体实现”），标题本身在第 7 页。长标题踩不到这个坑，短而通用的标题会 | `build-pdf.mjs` 改为**以锚点自己的链接目标页为准**——那既是读者点下去会到的一页，也正是目录该印的页码；文本反查降为兜底。重建后 30 条全部由锚点给出（锚点目标 30 / 文本兜底 0），`sec-7` 归位为 7 |
| A2.8（附录2） | 「在能力上表现为 A2.6 那**八条**」——v1.7 刚给 A2.6 加了第（9）条，计数过期 | 改为「那**九条**」 |

**另修一处口径自相矛盾（附录2）**：A2.7 收尾那句原本写「四条例外里，三条……**它们的证明不在账上，在 A2.6**」，而 A2.6 自己的开头（v1.7 加的）明写「它是未计账的收益清单，其中多数是方向性证据，它不构成『我们能在 20% 的水位上正常工作』的证明」。同一本书里一句话指着另一节说“证明在那里”、而那一节说自己不构成证明。改为「它们成不成立不在账上，要看 A2.6 那批方向性证据（完整证明还没有做）」，与 A2.7 第二条在 v1.7 的改法同向。

**指针引用逐条核对**：正文与附录里 40 余处 `见 A2.x／A3.x／附录N`、3 条页边注、图注里的「数据源」，逐条打开目标节核对所指之物是否在其中，**除上表两处外全部准确**。抽查到的几处：L105「见附录2 A2.6」所指的两笔未计账收益确在 A2.6；L593「见 A2.7 第三条」确指那张 106.8k／98.4k／92.0k／77.5k 对 80.5k 的输赢表；L394「实测中位 31%（见 A2.3）」在 A2.3 是 30.9%，取整一致；L563 与 L672 所指的「砸地板进篮框」「$\rho=0$」两句确在 A2.3。

**顺带把核对固化成常驻检查**：`verify.mjs` 末尾新增「目录页码 vs 链接目标」一节，用同一口径（锚点目标页）比对 `_tocpages.json`，不一致就报出来。这一类错误因此从「靠人偶然发现」变成「每次构建自动报」。

**成品**：仍 **41 页、211 链接**；`verify.mjs` 逐行核对 505 行、未命中 0；角标 126、条目 52、双向 52/52；表格 25 块列数一致；内部链接 134 个目标、悬空 0；**目录页码 30/30 三方一致**。

### v1.8：对话原文在架构里的位置（三处附录新增）

作者提出一个观察：**在需求对齐与验收都做得好的前提下，用户需求本身不太需要存储（它已由代码库反映），但模型对用户说过的文本需要留档一定量**——因为用户日后提出疑问时，最可能引用的正是这些文本。

讨论之后把它定成一条**主命题的推论**而不是新增好处，形态是：**原文零有损冷存在环境侧 ＋ 按需回查 ＋ 不建索引**。理由有三条：一是文章说"上下文随时能从环境 + 协议重建"，而"用户说过什么、我说过什么"重装不出来，所以对话原文**必然**落在环境那一侧——这是既有主张成立的前提，文章原来没写出来（是一处真空）。二是既然触发者是用户，**检索的键由用户免费提供**，因此不需要 embedding、不需要语义索引，也就不必预先判断"什么值得记"——这同时绕开了序言里记录过的那个坑（embedding 区分度极低，RAG 因此搁置）。三是不建索引还避免引入第二个不可靠环节：让模型申报"我刚才断言了什么"必然漏、必然错，会与 A3.6 已承认的"意图映射无机械校验"相乘。

作者明确"绝对不索引"，只保留"用户突然提到早期概念时，模型能去搜索这个空间"，并把定位收在**用户体验**上——"用户突然提出早期信息"不是高频场景，不值得在上下文里占位。因此本文不主张断言账、不做语义索引，只承认词法回查，并明确它占的上下文只有环境视图里一行恒定声明（恒定＝逐轮逐字节相同、永远命中缓存），**不随对话增长**。

三处新增，**正文一字未动**——按文章已有的分工放置，而不是并列成因：

| 位置 | 加了什么 | 为什么在这里 |
| --- | --- | --- |
| A2.6 第（10）条 | 对话原文不必再当上下文，因此可以无损留着：必要性（重建不出来）＋收益（摘要掉"为什么否掉另一个方案"这类没被计划过的信息，而用户日后最可能引用的正是它）＋机制（按需回查，命中的只是那几段）＋防误读（**反对的是"把日志当上下文"，不是"有日志"**）＋兜底（正文那条"写对提示词的重要性下降"成立的前提是"说漏了还能回头查"） | A2.6 就是"未计账的收益"清单，与第（9）条同性质（定性、不进账） |
| A3.6 新增一行 | 适用范围：这是**体验层的保证，不是正确性保证**；词法检索只在用户喊出当时那个词时有效；查不到时不编；不进 A2.4 的账；留存策略与访问控制属工程细节，本文不规定 | A3.6 就是"口径与适用范围"，最后一句替作者把"隐私那些是工程细节"在文章里明说掉 |
| A3.5 新增第五条 | **体验这一侧，本文没有给出判据，也没有任何对照**——成本侧压成三个可复算的数、不确定的那部分列了四条例外与待测项，体验侧至今只有零散的好处条目；列出三个只能靠用户实验回答、本文一个都没测的问题（说漏了能不能低成本收回／用户能不能预期"过了这一步它还记不记得"／能不能不描述、直接指着说"照那个来"） | A3.5 的题就是"至今没有人做过的对照"，体验侧无判据、无对照正是同一类东西 |

**体验侧那三个问题故意只写问句、不造术语。** 若给它们起名（可回收／可预期／可指认），就是给一篇已经自造了意图映射、契约、段序、有效前缀组装、有效残留、四个台阶的长文再加三个名词，直接扣可读性；问句拿走了同样的信息，读者不必先学词。

**机械核对**：`verify.mjs` 逐行核对 **508 行、未命中 0**（v1.7.1 是 505 行）；角标 **126 未变**（新增三处不含任何引用）、条目 52、双向 52/52；表格 25 块列数一致（A3.6 增行后仍一致）；内部链接 134 个目标、悬空 0；图 6 张、页边注 3 条、专名 2 处；溢出正文框 0、残留占位符 0、正文可见星号 0；引号 **272/272** 平衡、ASCII 直引号 0。**目录页码 vs 链接目标：30 条，不一致 0 条**（v1.7.1 新增的常驻检查）。

**阅读增强的一个意外好处**：新增的三处加粗**没有变成胶囊**——阅读增强只作用于序言与正文，附录里的加粗按普通加粗渲染。所以这次不必像 v1.7 增补那样迁就胶囊长度（那次 101 字的加粗被识别成"主张·宽胶囊"，比全篇上限 39 字长出一倍多）。胶囊仍是 36 处（整句 20 / 短语 16），与 v1.7.1 完全相同。

**版面代价**：**41 → 42 页**，PDF 211 个链接、1468 KB。目录 30/30 重解析，两个标题后移一位——**A3.1 条目 31 → 32、A3.8 一句话 41 → 42**。六张图与 `build-html.mjs` 一字未动。

**底部留白合计 524.9mm**（v1.7 是 511.3mm，同一把尺），但**超 30mm 的页从 5 页降到 3 页**——p2 目录页 81.8mm、p30 33.7mm、p42 末页 67.1mm。多出来的那一页正好是末页：它把原先散在 p19／p24／p25／p30 的空白收了进去，所以留白总量略增、而"大洞"的页数反而变少。末页与目录页的留白属结构固有，不是排版缺陷。

### v1.9：Harness 耦合的思考写进附录1；全篇中英空格统一

作者把一路想下来的东西自己写成了附录1 的正文——耦合现象（同一个模型、只换一层交互面、成绩就换了一次）、模型对这套接口的"不自知"（问它读取工具返回什么形状，它绕出去把工具调了一遍、再从代码里推断出一个形状）、权重是状态却在部署那一刻冻成一个静态切面（缺的是**状态的动态**）、外赋的状态只扩展广度而没有对自己的指涉、判断只属于人，以及"四件代价里没有一件是判断，它们全是表达"。落点句是：**我们把模型为了框架做的妥协，通过框架所能管理的状态外包了出去。** 附录1 因此从一页扩到约 80 行，成为全文最长的一节。这一节由作者手写定稿，我只在逻辑接续处落位，不代笔。

**正文只有两处改动，都是格式，一个字的表述都没动。** 一是「相对主流的优势」最后一条末尾加 `（见附录1）` 四个字：正文在那里写了结论（四件事分别由环境、契约、前缀与按需读回接走；省下的是表达，不是判断），而理由是附录1 给的——这是全文唯一一处"正文断言、附录论证"的倒挂，加个指针把它接上（占位最小，且正文里已有先例：解空间那一节的"见附录1"）。二是「推论 3（普适）」与「推论 3′（Agent 特化）」的冒号移入加粗，与「推论 1／2」的写法归一。

**附录1 里替作者接住的四处**：把「对各自复杂度harness环境和自制工具增强泛化能力」这句补通顺（缺"的"、缺"都"、"是必然大于"去掉"是"）；把「"提示词工程"本就不该存在」后面那句主语缠在一起的长句拆顺；把 L209 那句夹在事实与回溯之间的结论句挪到段末；给 L275 的"定性成立"补上它成立的理由——**即便某次返回值不够用，被丢掉的那段原文仍在环境里，回取一次就补上；它不是靠模型自足，是靠环境兜底**。另外把序言里对具名模型"被质疑为刷分"的断言删掉，改用序言已有的"神鬼二象性"接回，落点写成"同一个模型，只换一层交互面，成绩就换了一次"——后者是本文自己的观察，前者需要出处。这一处**没有记进 A3.7**：A3.7 记的是"未采信的外部说法"，而它从未在任何版本里发布过，没有条目可记。

**A3.6 新增一行"省约 19% / 66%"的适用范围。** 这是本轮唯一一处实质性的口径新增：这两个差是在**基线仍在累积、仍在压缩**的前提下做出来的，而附录1 主张的单工具／代码化调用会改这个前提——中间结果前置地不进上下文，基线的堆积变慢、压缩变稀，差要对着一个更小的基准重估。**本文没有测过这一档，所以不给幅度**；方向是确定的（本文也吃到往返数下降的收益，落在少写、少背；基线吃到的是少压缩——两边同时变化，差只会变小），而代码化压的是**取数往返**、压不掉**判断往返**。写这一行不只是尽诚实义务：文章一边主张单工具方向、一边不提它对 A2.4 那个差的影响，本身不自洽。

**A3.5 新增第六条**：附录1 的两条推断还没有**量级**对照——返回值作为"结论"是否足以支撑下一步（定性成立，但省在哪一档、省多少没量过），以及为工具形状做拟合训练所需的数据量是否真的大于只训练通用代码能力。第五节的"（见本节第三条）"同步改成"（见本节第三、六条）"，否则新条目挂空。

**全篇中英之间的空格统一为"恰好一个"**，这是本轮最大的一处机械改动：65 行、净增 271 个空格。动手之前先量了渲染器——`build-html.mjs` 里那句 `text-spacing:normal` 在 Edge 153 上**不被支持**（`CSS.supports('text-spacing','normal')` 为 false），所以成品 PDF 里的中西文间距完全来自源文件里的字面空格；源文件里混着"有空格"与"没空格"两种写法，PDF 就跟着一起不一致。方向定为"一律加"而不是"一律不加"：`.md` 本身也是发布物，而 GitHub 不认 `text-autospace`，不加空格在那边会挤在一起。规则只在中文字与拉丁字母／数字（含 `%` 与上标 `²³`）相邻处插一个空格，**不碰全角标点与拉丁的邻接、不碰 `汉[12]` 角标、不碰行内代码与数学**；改完逐行复核过 65 行 diff，全部只增删了空格、没有一个字变化。

**这一改连带暴露并修掉了构建脚本的一处静默失效。** `build-html.mjs` 里的页边注与两张架构图是按**文字前缀**挂在 Markdown 上的；空格一加，「全部子Agent回归后」「而对于我们这套Git架构」就对不上了——**图 3 与两条边注会静默消失，页面照样生成**。修法两件：把锚点比对改成**先去掉全部空白再比**（改字或改空格从此不会再打断挂载），并在构建末尾加一条**硬自检**——图必须 6 张且编号 1–6 齐全、边注 3 条、专名 2 处，不符即以非零码退出。这条自检是专门为"静默失效"写的：少一张图、少一条注，肉眼很难发现。

**顺手修掉的几处**：一处多余的行内空格（`**"Agent = Environment + Context + Model"** 。`）、8 行只含空白的行（`build-html.mjs` 里 `if (!l.trim())` 视同空行，删掉不改渲染）、一个大小写不一致的术语（`主 agent` → `主 Agent`）、两处漏掉的空格（`30%上下`、`N²的`）。

**机械核对**：`verify.mjs` 逐行核对 **525 行、未命中 0**（v1.8 是 508 行）；角标 **126 未变**、条目 52、双向 52/52；表格 25 块列数一致；内部链接 134 个目标、悬空 0；图 6 张、页边注 3 条、专名 2 处；溢出正文框 0、残留占位符 0、正文可见星号 0；引号 **288/288** 平衡、ASCII 直引号 0、直角引号 0；`**` 共 1040 处、无一行是奇数。**目录页码 vs 链接目标：30 条，不一致 0 条**。胶囊 36 处（整句 20 / 短语 16），与 v1.8 **完全相同**——加粗长度跨过 12 字就会改变胶囊宽度，所以逐条查过所有长度 12 的加粗，确认没有一条含中英边界，没有一处因加空格而改档。

**版面代价**：**42 → 45 页**，PDF 211 个链接，MediaBox 与 v1.8 逐字相同（594.96 × 841.92），内嵌字体集合与 v1.8 **完全相同**（NotoSansSC-Regular / -SemiBold / -Bold、NotoSerifSC-Bold、SegoeUI、LucidaSansUnicode、KaTeX ×2、NSimSun）——**NSimSun 在 v1.8 里就有，不是本版引入的回退**。目录 30/30 按锚点目标重解析，两轮收敛。

**底部留白合计 739.8mm**（v1.8 是 524.9mm，同一把尺），超 30mm 的页 **3 → 5 页**——p2 目录页 81.8mm、p26 35.8mm、p27 69.9mm、p32 33.7mm、p45 末页 210.6mm。多出来的 214.9mm 里 210.6mm 是末页：正文长了三页，最后一页只剩二十来个词；目录页与末页的留白属结构固有。p27 那 69.9mm 已逐页核过，是**图 6 放不下、被推到 p28**（`.fig{break-inside:avoid}` 的既定行为，v1.7 记录过同一现象），不是渲染缺陷——p28 页首正是图 6。正文文字横向 22–**173.8**mm，超出的 1.8mm 是**行末标点悬挂**（第五节已记，中文排版的正常行为，不是溢出）。

**这一版的数字口径**：字数由"约 2.4 万"改为"**约 2.9 万**"（汉字计：29,211 个汉字；非空白字符 52,778）。原来那个 2.4 万是 v1.5 时代写下的，此后一直没再算过，误差约两成；README 与 `CITATION.cff` 同步更正。第五节的胶囊计数由"19 个整句 + 16 个短语"更正为"**20 个整句 + 16 个短语**"（v1.8 记录的就是 36 处，是那一行没跟着改）。

**一处工具改善**：`measure-pdf.mjs` 原来只打每页几何，"底部留白合计多少、超 30mm 有几页"是手工从表里加出来的；这一版把这两个数移进脚本一起算，记录从此可复算。

## 二、账的数字从哪来

- 三条**从未装过任何上下文处理插件**的干净会话：合计 **9,023 步 / 363 小时 / 54 次原生压缩**，逐次调用的计费数据完整；
- 锚定会话 `session-b5f92412`：**2,068 步**、**9 次人工压缩**（触发点中位 **30.9%**）；
- 全部金额按 **DeepSeek V4.1 Flash 空闲时段**官方价：缓存命中 ¥0.02 / 未命中 ¥1 / 输出 ¥4（每百万 token，高峰为 2 倍）。

取值分三类：

| 类别 | 包含 | 说明 |
| --- | --- | --- |
| **实测** | $W$、$T$、$g$、$\theta_b$、$C_c$、$\sigma$、$E_b$、缓存命中率 | 见附录2 A2.4 |
| **保守取法** | 压缩后重读段 9.2k（九次压缩后首次调用的未命中均值）、价格取低谷价、$P+s=60\mathrm{k}$、$M=10\mathrm{k}$、$\rho=0$、$\varepsilon=0$、合并冲突不入账、质量与能力收益一律为 0 | 全部取在对本文**不利**的一侧 |
| **例外（四条）** | $N=4$、$\theta_d=20\%$、$\Delta P=P$（前缀整段按未命中）、$k_b$ 取公式值 202 步而非实测的 230 步 | 前两个是"设计被正确使用"与能力水位的假设，偏有利；第三个不偏向哪一边，而是越过了真实机制；第四个偏有利 0.6 个百分点，是 A2.0 第三条规矩复查时漏掉的那一处 |

## 三、成本结论速查（一次 2,068 步任务）

| 情形 | 账单 |
| --- | --- |
| 基线，人停手在实测的 31% | **¥7.02** |
| 基线，完全不管控（撞默认阈值 80%） | ¥16.97 |
| 基线，按钱调到自己的最优点 8%（工程上不可达） | ¥3.33 |
| 本架构，20% 水位、4 分支、更新量按保守的 10k | **¥5.70** |

对实测的真实基线**省约 19%**；对完全不管控**省约 66%**；对基线自己的钱最优**贵 71%**；与基线同水位（31%）**贵 12%**。

三条设计条件：**更新量 ≤ 2.8k**、**前缀 + 切片 < 63k**、**重发现盈亏平衡 $\rho^* = 160\text{–}194$ token/步**。
四个台阶：写一次 ¥4/M（＝背 200 步）、第一次构建 ¥1/M（＝背 50 步）、此后每步被读 ¥0.02/M（＝背 1 步）、不构建 **0**。

## 四、引用体系

共 **52 条**，按来源分四档：**甲 14 条**（同行评审）、**乙 19 条**（厂商一手文档/官方仓库）、**丙 13 条**（预印本）、**丁 6 条**（工程博客）。**只有甲、乙两档被用来支撑结论**；丙、丁只作方向性参考。

条目表的「核验」列：**【原文】**＝直接访问了原始页面或论文正文；**【转述】**＝经镜像、索引或第三方转述核实（方向可信、精度待复核）。未采信与不可用清单见附录3 A3.7。

## 五、版式约定

- **字体**：正文与标题用思源黑体（Noto Sans SC），封面大标题用思源宋体（Noto Serif SC），代码、图表刻度、编号用 Cascadia Mono。PDF 内嵌 `NotoSansSC-Regular / -SemiBold / -Bold` 与 `NotoSerifSC-Bold`；系统里的思源是变量字体，导出前必须先实例化成静态字重（见第六节）。
- **版心**：A4，页边距 上 22 / 右 38 / 下 20 / 左 22 mm，正文宽 150mm ≈ **41 字/行**；正文 10.5pt（五号）、行距 1.75、段间 0.72em，不首行缩进。右侧 38mm 留白给页边注。
- **强调（胶囊）**：一种装置、两种宽度，底色一律中性暖灰，全篇不用彩色高亮。**选哪句由作者在 Markdown 里加粗决定**：渲染层只按长度决定宽度——≥12 字渲染成**整句主张**（`#E6E1D5`），<12 字渲染成**短语/术语**（`#F2EFE9`，与行内代码、术语胶囊同一档），列表标号（推论1：…）不加底色、保持纯加粗。跨行时用 `box-decoration-break:clone`，每行片段各带一块底色。正文现有 **20 个整句胶囊 + 16 个短语胶囊**（v1.9 复核；v1.5.5 的六处收口新增两颗整句、四颗短语：`以一个个可指认的单元保存下来、像文件一样被管理`、`协议即每个模型自己的上下文与工具拟合形状`，以及三颗 6 字与一颗 11 字的短语）；序言与附录的加粗保持纯加粗，不套胶囊——否则序言那一行并列的四个"构想"会变成两块宽胶囊夹两块窄胶囊并排。
  - 两个内边距都是量出来的，不是审美选择：宽胶囊常整句落在行尾，**左右**内边距稍大就顶出版心（0.34em 时右溢 3px），现取 0.09em；**竖直**内边距每多一分，同一段里相邻两行的胶囊就靠近一分（0.1em 时两块之间只隔 3px、连读像给整段加了底），现取 0——底色带正好贴住字身框，相邻行之间留出约 2mm 白缝，这是行高减去字身框的极限。页面上"溢出正文框 0 个"这条自检就是为前者服务的。
  - **窄胶囊（`.em`）另加 `white-space:nowrap`**（v1.5.4）：它只有 2–11 字，本来不该被折行；而一旦折在行尾、只剩一个字，那个字的底色带加右侧内边距就会顶出版心 2px（"溢出正文框"报出 `span.em` 右溢）。整句胶囊（`.claim`）不加这条——20–30 字的主张必须允许折行。
  - 两种宽度的底**必须拉开深浅**才分得出来：初版取 `#EAE7DF` 与 `#F2EFE9`，每通道只差 8/255，隔开看就分不出——现取 `#E6E1D5` 与 `#F2EFE9`（差 12/255）。
  - 紧跟在胶囊后的脚注角标加 0.16em 左边距（`.claim + a.cite`）：上标的方括号会贴到胶囊右缘，看起来像被一起涂了底。
- **表格**：25 张全部为三线表（顶/底线 1.5pt `#1a1a1a`、表头下 0.6pt `#8a8a8a`，无竖线、无斑马、无圆角），表头不折行、数值列右对齐并用等宽数字。**不写"表 x"编号题注**——这些表是夹在论述链条里的论证表，编号会制造虚假的"表格清单"预期。**表格允许跨页**：表头用 `table-header-group` 每页重印，行不跨页；整表不拆的代价实测很大（三张表各留 67 / 95 / 154 mm 空白）。参考表里的日期用不间断连字符（U+2011），免得被折成两行。
- **图**：六张 SVG 为单色系（暖灰 + 墨蓝），柱体直角、网格极淡；每张图顶上一行**论点行**，图内不重复图注标题；图注带「图 1 – 图 6」编号，按阅读顺序（正文三张、附录2 三张）。图注只写图上没有的信息（时序、谁执行、口径来源、边界）。
- **封面与目录**：封面页（标题 / 主题式 / 作者 / 日期）与目录页（30 行，含真实页码与点线引导，可点击）由渲染层生成。
- **页边注**：屏幕上是正文右缘留白里的真页边注，与所注段落首行齐平；打印时改为插在**区块末尾**的一条右对齐灰色小注——Chrome 导出 PDF 会丢弃落在版心外的绝对定位元素，而把注插在开头会把列表项拆成"孤立项目符号 + 换行正文"。
- **页码**：目录页码用 `pdftotext` 反查成品 PDF 得到，迭代到两轮一致；页脚由导出脚本注入「当前页 / 总页数」。
- **末页**：colophon 前加「版式说明」小标、字号降到 11px 灰色，避免被当成正文的一部分。

## 六、重建与字体

**这一节原来挂在 README 末尾，现已整体搬到这里**——README 面向读者（承担原先摘要的位置，讲清这篇在说什么、外行怎么读），不再讲构建：正文本身就随仓库提供，读者不需要知道它是怎么生成的。

需要 Node 18+、本机装好的 Chrome 或 Edge，以及 KaTeX 0.16.47。

```bash
npm i                        # 装 katex
node build-html.mjs          # Markdown → 单文件 HTML
node build-pdf.mjs tocpages  # 反复跑到 _tocpages.json 稳定（目录页码两轮收敛）
node build-html.mjs
node build-pdf.mjs final     # 先导出 PDF，再做分页截图与版式审计
node verify.mjs              # 逐行文字比对、引用双向可达、内部链接无悬空
```

| 脚本 | 干什么 |
| --- | --- |
| `build-html.mjs` | Markdown → HTML（分块、表格、目录、图、页边注、胶囊） |
| `build-pdf.mjs` | HTML → PDF，另有 `shots` / `audit` / `tocpages` / `final` 模式 |
| `verify.mjs` | 交付校验 |
| `measure-pdf.mjs` | 量成品 PDF 里文字的真实位置 |
| `normalize-punctuation.mjs` | 中文标点规范化，默认只干跑 |
| `fix-font-names.py` | 修正字重实例的名称表（见下） |

顺序上有一条硬规则：**先导出 PDF，再截图**——截图用的模拟层会污染导出结果。环境变量都有默认值，见各脚本头部：`DOC_DIR`（文档目录）、`KATEX_DIR`（KaTeX 位置）、`POPPLER_DIR` / `PDFTOTEXT` / `PDFINFO`（只有 `measure-pdf.mjs` 需要）。

`_fonts/`（四个静态字重，约 45 MB，已 gitignore）不在仓库里，只在重建 PDF 时用得上，由系统变量字体实例化而来，需要 Python + fontTools：

```bash
py -m fontTools.varLib.instancer "$WINDIR/Fonts/NotoSansSC-VF.ttf"  wght=400 --update-name-table -o _fonts/NotoSansSC-Regular.ttf
py -m fontTools.varLib.instancer "$WINDIR/Fonts/NotoSansSC-VF.ttf"  wght=700 --update-name-table -o _fonts/NotoSansSC-Bold.ttf
py -m fontTools.varLib.instancer "$WINDIR/Fonts/NotoSerifSC-VF.ttf" wght=700 --update-name-table -o _fonts/NotoSerifSC-Bold.ttf
py -m fontTools.varLib.instancer "$WINDIR/Fonts/NotoSansSC-VF.ttf"  wght=600 -o _fonts/NotoSansSC-SemiBold.ttf
py fix-font-names.py
```

- **为什么要静态字重**：Chrome 导出 PDF 时无法嵌入变量字体，会静默回退成新宋体（新宋体里的中文看起来"对"，所以这个问题不会自己暴露）。实例化后经 `@font-face` 接入导出管线，PDF 里才是真思源。
- **`wght=600` 要单独处理**：SemiBold（小节标题用）不在思源变量字体的命名实例里，`--update-name-table` 会报 `Cannot find Axis Values`；改不带该参数生成，再用 `fix-font-names.py` 把名称表改成 `Noto Sans SC SemiBold`。

## 七、校验结论

- **本版（v1.9）实测**：Markdown 与 HTML 逐行比对 **525 行、未命中 0**；角标 126、条目 52、双向 52/52；内部链接 134 个目标、悬空 0；表格 25 块列数一致；胶囊 **36 处（整句 20 / 短语 16）**；成品 **A4 45 页、211 个可点击链接、图 6 张**，MediaBox 594.96×841.92 与 v1.8 逐字相同、内嵌字体集合也相同；底部留白全 45 页合计 **739.8mm**，超 30mm 的 5 页列在第一节 v1.9 那一条里。
  **下面这几条是 v1.6 时点写下的，属于当时的历史记录**——其中的"495 行""125 处角标""35 处胶囊（整句 19 / 短语 16）""40 页""210 个链接""607mm"都已经过期，以本节第一条为准。
- **正文文字**：Markdown 与 HTML 逐行比对 **495 行**，**未命中 0 行**；v1.5 的正文区差异只有 6 行 `####` 标题与 18 行空行，v1.5.2 的正文差异只有"删摘要、移一段、换两处词"，v1.5.4 的正文差异只有三处（合并那一步的括注与主体、L169 一处换词），v1.5.5 的正文差异只有六处定性收口（推论3 普适、"投影"分两半、构建的粒度、"错"的定义域、"协议"的定义、能力口径两处用词），v1.5.6 的正文差异只有四处措辞收窄（L105 补轮级重启、L157 去"模型"二字、L153 统一"意图映射"、L187 回退用词）；**v1.6 只重做图（正文与附录文字一字未动）**，所以这条仍是 495 行未命中 0；
- **引用**：125 处角标、52 条条目、**双向可达 52/52**，编号连续 1–52，无未定义引用、无孤儿条目；
- **链接与表格**：134 个内部链接目标**悬空 0 个**；25 张表**列数全部一致**；
- **版式几何**：溢出正文框 0、图内文字越界 0（含新增的手绘架构图）、表格横向溢出 0；
- **文字保真（v1.3.1 建的口径）**：那一版的正文与 v1.3 逐字节相同，两版 PDF 的可见文字前 34,006 字符一致；v1.4 起有实际改动（新增引用与一处正文引用），v1.5 的新增内容全部是独立块（摘要、图 3、6 个标题），正文原有句子未动；**v1.5.4 动了正文三处，其中一处是作者原话的单个短语**，逐条见第一节；
- **强调装置**：正文 **35 处胶囊（整句 19 / 短语 16）**全部落在版心内；最紧的一对相邻胶囊间隔 8 字（`删除` 与 `不构建` 的对照，随解空间那一节一起前移），是有意为之；
- **成品**：A4 **40 页**、210 个可点击链接、**图 6 张**。唯一的"越界"是两处行末标点的**悬挂**（半个全角 = 1.8mm，第 21、26 页各一处），是中文排版的正常行为而不是溢出；
- **底部留白**：全部 40 页合计 **607mm**（均 **15.6mm**/页），超过 30mm 的有 4 页：目录页（84mm）、第 24 页（39mm，图被推到下一页）、以及末两页（47mm 与 145mm）。**这是 v1.5.4 到 v1.6 四轮累积的账**：附录2 那段口径纠正净增约半页（471 → 624mm），v1.5.5 六处定性收口再加约 400 字（624 → 645mm），v1.5.6 四处措辞收窄又把第 14 页原先的余量填掉（645 → 618mm）；**v1.6 新增的四张图把页数从 39 推到 40，留白反而降到 608mm**——新图吃掉的正是原先空着的地方。**内容优先于页数**，这一笔继续留着；若要把末两页收回去，得动附录3 尾部或把图挪位，属于另一轮。
  同一轮复核记下一处孤行：第 8 页末行只有「样。」两字。查明**不是本轮改出来的**——v1.5.5 里同一段的同一处折行就是这个两字末行，当时落在页面中部；本轮内容把后面各推下两三行，它才落到页底，而 `p{orphans:2;widows:2}` 让下一段整体移到第 9 页（p8 只剩 12.7mm，不足两行）。控制规则在正常工作，不是排版错误；能解它的是 `text-wrap:pretty`，但那会重排全部 39 页，属于版式约定，待作者定。
- **账目重算**：13 个金额与 4 组派生量（$R_b$、$\theta_b^*$、$k_b$、$R_d$）用附录2 的参数独立重算，全部命中；v1.5 又用同一套式子把敏感性表逐行重算了一遍，据此改掉一处标错的档位。

## 八、公开留档

仓库在 <https://github.com/54shitaimzf/context-native-agent>。许可见 `LICENSE`（正文与附录 CC BY-NC 4.0）、`LICENSE-CODE`（脚本 MIT）、`THIRD-PARTY.md`（内嵌字体与 KaTeX）。附录3 里的【原文】引用，文字权利仍属原作者。

为公开分发做过三处改动，都不涉及正文文字：脚本里写死的本机绝对路径改为从脚本自身位置推导（可用 `DOC_DIR` 覆盖）；26 个提交的作者邮箱由 `yujin@localhost` 改写为 GitHub 的 noreply 地址（所有哈希因此改变，成品页脚戳重建一次即更新）；补齐 `README.md`、`LICENSE`、`LICENSE-CODE`、`THIRD-PARTY.md`、`.gitattributes`、`package.json`。

两条维护须知：

- **页脚版本戳**是构建时 `HEAD` 的短哈希（`git rev-parse --short=7 HEAD`）。成品作为一次提交落在那个提交之上，所以戳总比成品自身的提交早一个——它标记的是构建时所处的源文件状态。
- **GitHub 不识别 CC BY-NC 4.0**（CC 系列里只认 CC0 / CC-BY / CC-BY-SA），所以侧栏不显示许可标签；`LICENSE` 本身完整有效，以 README 的许可表为准。仓库的语言统计由 `.gitattributes` 维持为 Markdown 为主。
