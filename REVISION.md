# 修订与校验说明（v1.0）

本文的定稿与交付说明。**正文文字自 `8229d01` 起未改动**——其后所有提交只插入了引用角标、重编号，或改了附录；每一轮改动都用同一套指标做过"零差异"比对。

## 一、交付物

| 文件 | 说明 |
| --- | --- |
| `Agent架构革新：迈向上下文原生智能-Context-Native Agent.md` | 定稿源文件（序言 / 正文 / 附录1 Harness 耦合思考 / 附录2 半定量成本分析 / 附录3 引用与对照） |
| `Agent架构革新：迈向上下文原生智能-Context-Native Agent.html` | 自包含单文件阅读版：KaTeX 公式与 20 个字体全部内联，离线可用。顶部两个开关——「强调·专名·边注」与「白底（PDF 预览）」 |
| `Agent架构革新：迈向上下文原生智能-Context-Native Agent.pdf` | 最终格式：A4、41 页、125 个可点击链接，白底黑字、中文字体为 Microsoft YaHei（粗体为真实 Bold 字面） |
| `build-html.mjs` | 渲染脚本：`node build-html.mjs` 从 Markdown 重建 HTML（只读 Markdown，不改它） |

## 二、数据来源

- 三条**从未装过任何上下文处理插件**的干净会话：合计 **9,023 步 / 363 小时 / 54 次原生压缩**，逐次调用的计费数据完整；
- 锚定会话 `session-b5f92412`：**2,068 步**、**9 次人工压缩**（触发点中位 **30.9%**）；
- 全部金额按 **DeepSeek V4.1 Flash 空闲时段**官方价：缓存命中 ¥0.02 / 未命中 ¥1 / 输出 ¥4（每百万 token，高峰为 2 倍）。

## 三、哪些是实测、哪些是保守取法

| 量 | 取值 | 性质 |
| --- | --- | --- |
| $W$、$T$、$g$、$\theta_b$、$C_c$、$\sigma$、$E_b$、$k_b$、缓存命中率 | 见附录2 A2.4 | **实测** |
| 压缩后重读段 | 9.2k | **实测**：九次压缩后首次调用的未命中均值（残留中位 15.3k，其余仍在缓存中） |
| 价格取低谷价、$P+s=60\mathrm{k}$、$M=10\mathrm{k}$、$\rho=0$、$\varepsilon=0$、$N^2c$ 不入账、质量与能力收益一律为 0 | 见附录2 A2.4 | **保守**（全部取在对本文不利的一侧） |
| $N=4$、$\theta_d=20\%$ | — | **三条例外**之一二：不是保守取法，是"设计被正确使用"与"能力水位"的假设（偏有利） |
| $\Delta P=P$（前缀整段按未命中） | — | **三条例外**之三：不是偏向哪一边，而是越过了真实机制 |

## 四、成本结论速查（一次 2,068 步任务）

| 情形 | 账单 |
| --- | --- |
| 基线，人停手在实测的 31% | **¥7.02** |
| 基线，完全不管控（撞默认阈值 80%） | ¥16.97 |
| 基线，按钱调到自己的最优点 8%（工程上不可达） | ¥3.33 |
| 本架构，20% 水位、4 分支、申报按保守的 10k | **¥5.70** |

对实测的真实基线**省约 19%**；对完全不管控**省约 66%**；对基线自己的钱最优**贵 71%**；与基线同水位（31%）**贵 12%**。

三条判据：**申报量 ≤ 2.8k**；**前缀 + 切片 < 63k**；**重发现盈亏平衡 $\rho^* = 160\text{–}194$ token/步**。
四个台阶：写一次 ¥4/M（＝背 200 步）、第一次构建 ¥1/M（＝背 50 步）、此后每步被读 ¥0.02/M（＝背 1 步）、不构建 **0**。

## 五、引用体系

共 **40 条**，按来源分四档：**甲 10 条**（同行评审）、**乙 16 条**（厂商一手文档/官方仓库）、**丙 11 条**（预印本）、**丁 3 条**（工程博客）。**只有甲、乙两档被用来支撑结论**；丙、丁只作方向性参考。
条目表的「核验」列：**【原文】**＝直接访问了原始页面或论文正文；**【转述】**＝经镜像、索引或第三方转述核实（方向可信、精度待复核）。未采信与不可用清单见附录3 A3.7。

## 六、定稿校验记录

| 项 | 结果 |
| --- | --- |
| 引用 | 40 条，编号连续 1–40，**无未定义引用、无孤儿条目** |
| 表格 | 26 块，**0 处列数不一致** |
| 交叉引用 | 附录1/2/3 与 A2.0–A2.8、A3.0–A3.8 共 21 种，**全部可解析** |
| 账目重算 | 13 个金额 + $R_b$、$\theta_b^*$、$k_b$、$R_d$ 四组派生量，用附录2 参数**独立重算全部命中** |
| 正文文字 | 自 `8229d01` 起未改动；其后仅有角标插入、角标重编号与两处附录编号引用 |
| 渲染一致性 | HTML 与 Markdown **逐行核对 446 行，零缺失**（渲染层新增文字仅：两张图注、页脚、工具条、3 条页边注） |
| 图 | 两张 SVG（四个台阶的价钱换算、三种活法与本架构的对比），数据源均标注为附录2 A2.2 / A2.4–A2.5 |

## 七、本轮修订做了哪些事（最近的提交）

| 提交 | 做了什么 |
| --- | --- |
| `fad568b` | 全文统一表述：用"构建"替换"换出"一系说法，等量替换、不动结构 |
| `769e00b` | 附录1 改写为正面完整表述，去掉"上一版/这一版/自查修正"的修补痕迹 |
| `8229d01` | 正文补三处附录所需的架构锚点（每轮固定开销、段序稳定、分支上下文随分支消失） |
| `e2cf4f5` | 新增附录3：引用条目、主张×外部证据、坑×设计区分点、尚无人做过的对照、口径、未采信清单 |
| `aa2c38d` | 附录3 重做：新增"相似架构对比与区分"；引用分四档重筛（44→39 条），剔除媒体转述与重复来源 |
| `d6fe6d2` | 附录顺序调整：Harness 耦合思考前移为附录1，成本分析顺延为附录2，A1.x 全部改号 A2.x |
| `173a0fb` | 附录2/3 去痕与精简：删 39 行重复与过程叙述；A2.6 由 8 条并为 6 条；补丁式语气改为正面陈述 |
| `c0da313` | 甲类硬伤修复：dsh 正名、草稿行号改正文引句、RootUp 换成中等规模开源仓库、补记 9.2k 的实测来历、四臂表 C 行补齐 |
| `5158847` | A3.3 两处元论证改正面叙述 |
| `3ae4a34` | A3.1 补第 40 条：DeepSeek Harness 官方开源仓库（乙档，原文） |
| `b70e7ea` | 交付自包含 HTML；修正 A3.7 甲档条数（11→10）、A3.6 标题中性化 |
| `3e8d2ec` | HTML 阅读增强（渲染层）：正文 6 组段落导航、13 处强调、2 处等式、2 处专名、3 处页边注 |

## 八、重建与导出

```bash
node build-html.mjs        # Markdown → 自包含 HTML
```

导出 PDF：用浏览器打开 HTML → 打印 → 目标选"另存为 PDF"、勾选"背景图形"、边距默认。
打印样式已强制白底黑字（不受系统深色模式影响），并保留黄色强调底纹、表格底色与两张矢量图。
