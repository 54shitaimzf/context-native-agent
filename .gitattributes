# 换行：仓库内一律 LF（现有源文件本来就是 LF，这里把它显式固定下来）
* text=auto eol=lf

# 二进制：不做行尾转换，也不逐行 diff
*.pdf   binary
*.ttf   binary
*.woff2 binary
*.png   binary

# 语言统计：Markdown 是这份仓库的正体，要计入；渲染产物不计入，
# 否则 770 KB 的成品 HTML 会把仓库标成「HTML 项目」。
*.md                linguist-documentation=false linguist-detectable=true
Agent重架构*.html linguist-documentation linguist-generated
Agent重架构*.pdf  linguist-documentation linguist-generated
