# Agent重架构：迈向上下文原生智能（Context-Native Agent）



*作者：余烬*



### 序言：余烬在剪枝



上个月，Deepseek核弹连发，不管是掀起flash革命的Deepseek V4 Flash-0731，还是神鬼二相性的Deepseek V4 Pro-0813，至少都在大家伙心里留下了深刻的印象。更不要说面向AGI设计，万物皆插件的野心勃勃的Deepseek Harness了。对我这个🐋小子来说，惊喜来的实在是太突然。我立刻动手体验了这个有趣的Harness框架，并测试了它的创造模式（虽然不出所料地把自己搞崩溃了）。然而噩耗来的总是那般突然，就在我兴奋地为我的个人小文件整理开源项目添砖加瓦的时候，梁文峰带着他的涨价来了。眼见着自己的费用蹭蹭上涨，上下文缓存那些钱也不由得让我心疼起来。可怜的余烬那时候还完全没有意识到，其实雷霆大思考才是上下文那么长的主因。于是我萌生了这样的想法：”要不我自己动手做个插件，把或成熟，或创新的上下文压缩机制缝进去，顺便美美收Github星星，投递一点大模型公司简历“。然而直到动手之前，我都还没意识到自己即将面临怎么样的”零花钱地狱“。



上下文管理是个被研究的十分透彻的领域，在通过免费快速的Deepseek网页快速模式Deeeepseeeek了一段时间后，我就笃定了自己已经对于Harness的上下文管理机制有了充分的了解。好！动手！结果自然是大败而归。



在dsh-price-less插件设计的初版，我进行了几个重大构想：**RAG/代码树为底层的语义化文件读取**，**基于意图的任务边界识别**，**任务边界与压力阈值自动压缩**，**提示词优化器**。



具体来说，任务，也就是Task，并非特指某种类型的任务，而是针对代码的某个结构/功能，具有文件读取特征的修改。根据这一定义，我们在边界区间压缩上下文能够最大程度避免上下文失效，降低重读率，同时拒绝为原本用不到那么多次，但是一直存在于缓存中的内容，大量重复计费哪怕很廉价的缓存计价。



而任务边界唯一的判定标准，就是用户输入。每轮任务内，全量的用户输入都会进入到”任务判别器“的上下文，再被组装上”项目整体目标”作为稳定前缀，确保任务边界判别的成本极低，压缩自动化且准确。而提示词优化器则更是巧妙复用，通过task前上下文，当前用户输入，与安装在Harness中的Skills列表按需取用，做到将用户的提示词扩写锚定，并且对于一些Skill能覆盖的操作给出最优解决方案，避免试错。RAG/代码树则是语义化，细粒度地读取文件，减少盲目全读和反复重读。



设想很美好，但是限时很骨感。因为实习要求在即，我不得不把RAG这种复杂内容搁置，移动到路线图中，全力开发边界识别。在这期间我经历了多次失败。包括机械判定完全不准确，embedding模型区分度极低，参数调优倾向于刀刀切，作弊获取奖励，评价标准难以综合多个指标。更不要说我作为夜猫子多次忘记梁文峰，足足烧掉了几十块的零花钱，最终才形成了上面那套能和提示词优化器复用缓存的成熟方案。



过程中还诞生了很多新的想法，包括工具剪切中间层等。通过对工具结果的保守通配，只保留有用信息，如编译器报错。但是在实测之后收益极低，最终被搁置。还有结论压缩，基于和工具剪切的类似理念，将中间结论直接代替“思考-工具调用-再思考-结论”，嵌入上下文。但是实际上，提示词注入后，大模型很难按照要求给出剪切信号，机械剪切风险和收益不成正比，也被弃用了。还有压缩产物的结构，也让我殚精竭虑。通过任务和类型化子任务高度浓缩之前任务信息固然不错，但是模型申报原文作为热尾天天坐标不准组装出错也非常令我恼火。事实上，经过一些实际使用，有限的原文长度对于模型的复读阻止作用十分有限，现在已经在本地改成申报坐标了，原文什么的，自己多读几轮，至少还更精确吧。



庸庸碌碌忙活这么久，最大的成功还是边界压缩，给我气笑了。索性还是有点安慰，至少Kimi可能为真的面试题，竟然句句踩在我失败的embedding和机械路径上，总算是长舒了一口气。不对啊？人家零花钱比我多多了，要是真试出来更好的，那还是我亏麻了，气气气！



但是这样磕磕绊绊的探索，也让我产生了一个足够疯狂的想法。已知上下文剪枝非常困难，已知上下文积累速度极高，主要来自于模型调用读取的文件慢慢积累，慢慢因为长度和中段被遗忘，然后再次读取...



我们处在一个上下文不够用的环境下太久了，以至于我们一直在思考如何做减法...那有没有可能，上下文是应该自己长出来的呢？



### 正文：欢迎来到上下文原生智能（Context-Native Agent）的世界



所以，当我想到这一点的时候，一切就豁然开朗了。在过去，上下文极度紧缩，完全不够用的时代，我们花费了不知多少功夫，试图将海量内容塞在上下文里，通过各种索引RAG的方式确保模型能够瞻前顾后，将一切纳入考量，但是随着Deepseek家模型拉开百万上下文的普惠年代，30%上下就开始手动压缩成为了常态（目前平衡信息量与模型能力衰减的甜点级上下文长度在20-30%，不严谨，但是大概为社区主流观点[1][2][3]），那么我们是否也该反思自己：“上下文压缩与剪切，是否还是正确的上下文处理方式呢？”



究其本源，我想我们要回归一个最基本的问题上：什么是Agent？在目前的社区主流与共识的说法中：“Agent = Harness + Model”。实际上Deepseek甚至将这个理念写入到了他们的招聘要求中[4]。在他们的实践中，甚至也出现了模型与Harness框架耦合才能解锁最高能力的现象。我对这一点其实并不持反对态度。虽然说在客观上这限制了用户API调用的选择范围，但是事实上这就好比人使用自己的手达成通用能力一样，这样的耦合也许有助于达成更高的能力上限。不过，当然更大的原因是Deepseek Harness是一款自由，开源的框架，并非是为了商业目的的强迫。



回到问题本身，Harness本身并不是什么简单的东西，尤其是学习Claude成熟经验后诞生的各种千奇百怪Harness框架。有的内置各种RAG，Skills，MCP，Tools，以Agent Loop为核心。将Harness视为Agent的组成部分诚然合理，但是这个框架太大，太重，让我们不知道如何去管理。当我们预设了每一分上下文信息的必要性，那必然就要面临顺序，取舍与重读，还有困难的剪枝。历史和实践证明，我们总是习惯性地通过补丁来解决，引入更多的机制，更多的压缩，更多的坐标。可是我在这里想要问下一句，有没有可能，上下文是从一颗纯净的前缀“种子”中生长出来的？



当我们去审视大语言模型的本质，在它的视角里，并不存在什么所谓“Harness”，甚至就连思考过程都是依靠训练对齐的，它只是一遍遍地处理着语言输入的黑盒函数。决定它输出状态的只有上下文与参数，所有的工具调用，思考过程都是训练对齐的结果。那我想，我们应该拒绝以用户交互的对象为Agent主体的认知错位，事实上：**“Agent = Environment + Context + Model”** 。



这句话反映的是两个层级：模型的状态只取决于参数与上下文；Agent的状态取决于环境 + 上下文 + 模型。



那么这种理解方式能给我们带来什么启示？

- **推论1：** 在一个环境受控的Harness框架下，保存一个Agent所需的运行状态无需任何与当前Context无关内容，成本远低于“Agent = Harness + Model”的假设。

- **推论2：** 在同一个 Harness 框架下、且环境一致时，给定上下文就能立刻重建一个 Agent 状态。

- **推论3（普适）**：上下文不该是 Loop Then Compress，而是按轮 **再生成 → 切片 → 合并 → 重载**（Regenerated, Sliced, Combined, Reloaded）。 这一条不依赖环境：对纯对话系统（无文件系统、无进程、无副作用），它同样成立——上下文照样要重建、切分、合并、重载，只是没有需要维护的环境实体。

- **推论3′（Agent 特化）**：当系统存在环境实体（代码、依赖、进程、端口）时，上述四步落到具体对象上即为——**环境维持共享、代码 fork、上下文 build**。
  
  

这三个推论共同指向的并非既有框架的补丁，而是**取消了一个约束**：内容离开上下文，不再等于失去内容。在旧框架里上下文是缓冲区，离开意味着内容丢失；而在新框架里上下文是投影，离开只是此刻无需[5][6]。**压缩与重建的全部差别，就藏在这个等号里**。



最终，以上述的理念为核心，我们可以推导出下一代Agent范式，与其较为具体的工程实现。我称其为上下文原生智能（Context-Native Agent）。在这一架构下Harness的职责从管理Model转向管理Context与管理一个Agent切片运行所需的环境。Context与Harness复杂框架解耦，作为与模型唯一的交互通道被解耦出来，成为Harness的一等公民。



以这一理念出发，一种上下文原生智能的具体实现可以以一种AI native “Git tree” 为核心，其分为主Agent与子Agent，具有模型一致性。



对于任意一个项目，主Agent与子Agent拥有一个共享缓存前缀[7][8]，其为当前系统版本下，必要的代码与意图映射汇总，与必要的系统状态。当一个任务被委派时，主Agent负责与用户对齐需求，并对任务进行拆分，任务的拆分的具体原则以降低耦合度与提高子任务目标的内聚性为主，且对任务的规模进行控制，确保其上下文占用对齐模型能力甜点区间。



然后，其将为每个子Agent创建一个分支，具体而言：maintain environment, fork code, then build context as a seed。**每轮都要走一遍分叉、定契约与准备合并，这笔开销与任务多长无关**[9]。每个子Agent的工作会作用于分支的虚拟文件上，以提交记录的形式落盘。



任务解耦完毕后，主agent将会运用其对于代码整体的理解，为每个子Agent委派上下文中预置的文件内容，然后由Harness进行最大前缀组装[42]。且主Agent会制定断言与测试作为合并前必要验收标准。



任务过程中，子Agent的动作可能或主动或被动提交Signal，供主Agent进行环境观察与调度。也就是说，这一具体设计类似于一种响应式Agent调度框架。



全部子Agent回归后，由主Agent/委派Agent进行代码合并，取长补短，并进行最终测试与汇报，并同时会对系统状态、代码映射等进行**坐标申报**——写下"东西在哪里"，由 Harness 按坐标把原文装配成新的前缀[10]。（这一步是**写地址**，不是**重写正文**：写地址短，重写正文长，两者的价钱差一个量级，附录1 会算这笔账。）这个新的前缀将会成为下一次执行时候的公共前缀，其作为全局理解对于之后的主Agent任务委派与子Agent的全局观起到指导作用，是衔尾蛇循环重启自己生命历程的步骤。



综上为我为上下文原生智能（Context-Native Agent）设计的一种Git Base的基础架构。其具有理解/执行模型解耦，过程透明可理解，并行效率高，各个Agent可复用现有单流程Agent上下文管理成熟经验，前缀缓存利用率高[11][12]，且规避了上下文长度瓶颈，更是从信息熵/上下文长度两方面确保了模型能力发挥稳定。



具体来说其相对主流，定性可能具有的优势：

- 在上下文层面：每个子Agent只会被给定极其干净的 [ Environmental View ] + [ Project Structure and Intention Mapping ] + [ Raw Files Assigned ]，作为其生长出果实的“种子”。这样干净的上下文有助于模型立刻进行工作，减少读取的工具调用次数和探索信息的不可控，但因为只是种子，也保留了发挥空间与一定的不可控风险。在具体的工程实现上，如果Raw Files Assigned段太长，则考虑仅先放入公共前缀部分，后续内容通过一个特定工具按需取用[10]。

- 在缓存利用层面：每个子Agent共享大量共同头，甚至Raw Files之前的内容可以与主Agent共享缓存前缀，等价于将所有只需要探索一次的内容的复利控制在了每个子Agent分区中，上下文累计只以子Agent结出的果实的形式回归主干，被吸收利用。这种共享有个前提：**装配时段序稳定**[13]——前缀为了信息密度每轮整体重写，但段序一动不动，变动的只落在尾部，没变的部分逐字节相同。而分支那份上下文本身并不回归，它随分支一起消失，**消失的只是"曾经构建在上下文里"，不是内容**。而根据Git与软件工程的成熟经验，一次合并就意味着项目意图/功能结构的变更，此时主Agent上下文也是时候进行更新与重启了，最终做到每个目标实现时，上下文长度几乎不触及压缩阈值，每次变更只进行一次干净彻底的探索，不带着压缩后信息密度高却丢失细节的信息包袱。

- 在透明度层面：主次分明，探索思考，需求对齐与实际落地解耦，交互均有signal可查，代码，架构与功能变更清晰可确认。

- 在模型适配性层面：可对齐每个模型所需求的最优上下文形状。

- 在模型能力层面：主要收益来自于信息熵的降低与上下文长度可控，中段迷失和能力下降被有效控制[14][38]。
  
  

其有待实验验证的点在于任务规模与解耦设计对于成本与能力的具体影响。但是，随着Goal模式等交互模式的兴起，任务的复杂度在上升，且逐渐变得更加高层，与实现无关。因此，传统的上下文累计思路必然会受到对齐需求与探索时期复利的反噬，哪怕是压缩之后也一样。



而对于我们这套Git架构，越是随着任务的规模增大与目的的实现无关，就越可以被细化为边界清晰、数量可控、架构解耦的落地分支，并由主Agent一次生成一份稳定的、共享的、能回答"这个功能会落在哪些结构上"的项目意图映射与清晰边界的架构契约。最终规避并行写者N²的冲突[15][16]，因为那是在任务高度耦合的具体实现目标下的不理想情况。当然，具体方面的优化打磨，不易解耦的任务，我们的架构能以付出轻微额外成本为代价，即可轻松回退到主Agent/少分支结构上。



我相信build上下文会是上限更高，更符合未来需求与趋势的更优选择。Git管理只是我结合软件工程经验给定的一个具体实现，只要思想存在，就一定会涌现出更多优秀设计。而当前Git架构下去，困难点在于虚拟文件的编译与测试、环境稳定性维护。



而在更加遥远的未来，当大模型的输出速度成为瓶颈的时候，缓存架构的优秀是否也意味着，首Token和平均Token都能大大提高[17][18]？具有实际生产价值的模型规模上限得到扩充？不过这些都是当前架构成为主流顺带来的好处罢了，总之，它意在扭转还没有回头的迟滞当下，是面向未来以工程复杂度换取落地价值的优秀架构。现在的大模型架构革新，以收敛难度与炸炉风险换取能力上限，而Harness架构的革新成本与风险，可是要小得多，难道不值得探索吗？



Context成为一等公民，意味着我们在文件治理，项目管理，操作系统与软件工程的成熟经验，均可以套用在管理Agent状态的切片上，实现高度透明，机械化，有保障的Agent协作，助力大模型在前AGI时代发挥能力上限。



然而，本文真正的价值不只在 Git 树与围绕它打造的这个系统上。Git 只是一个具体实现，粗糙、可替换，甚至可能是错的。真正被换掉的是**问题的解空间**：

- 在 `Agent = Harness + Model` 下，上下文是一段必须被管理的缓冲区。所有解法因此被逼向同一个方向——压缩、裁剪、摘要、检索——它们的共同点是**都要在信息量与长度之间做取舍**；而只要做取舍，就必然丢东西，就必然要赌。补丁套补丁、坐标套坐标，都是在这个解空间里打转。

- 在 `Agent = Environment + Context + Model` 下，上下文是一份**可以从状态重建的投影**。这一步换掉的正是那个取舍：压缩是**删除**，不可逆；分区是**不构建**，可回取。既然上下文随时能从"环境 + 协议"重建，不构建它就**不再等于失去内容**。这个等号一解开，压缩就不再是唯一出路——而分区、委派、构建之所以在旧框架下显得奢侈，恰恰因为旧框架里没有"可重建"这个前提。



### 附录1：半定量成本分析（已用真实账单锚定）

本节不做实验，只做账。

所谓"半定量"，是只做到**量级正确、方向可靠**，不去追一个精确到小数点的数字。

账本上守三条规矩：

1. **每个参数都取对结论不利的那一侧。**
2. **能实测的用实测值；不能实测的才取保守值，并且标明它是保守值。**
3. **算完回头查一遍：这些"保守"取法，是不是真的都在对我们不利？**

第三条不是客套。查完之后，绝大多数取法确实偏在不利的一侧，但**有三条例外**——两条是假设，一条越过了真实机制。它们单独列在 A1.7，因为它们决定了结论能走多远。

参数全部来自本地归档里三条**从未装过任何上下文处理插件**的干净原始记录：合计 9,023 步、363 小时、54 次原生压缩，逐次调用的计费数据完整保留。本节凡是写"实测"的地方，都指向它们。

还有一件事要在开头说清，因为后面每个数字的读法都由它决定：**上下文是每一轮从 0 构建出来的**，"一份内容不在上下文里"是默认状态，要它出现才需要有人决定、才需要付钱。四个台阶（A1.2）正是按这个前提排的。

#### A1.0 计费模型与符号

**三档价格。** 全文按 **DeepSeek V4.1 Flash 低谷时段**计价[19][20]：

| 计费项 | 单价 | 什么时候按这一档计 |
| --- | --- | --- |
| 缓存命中 | ¥0.02 / 百万 token | 这段内容没变、被缓存复用 |
| 缓存未命中 | ¥1 / 百万 token | 这段内容是新的，或者缓存已经失效 |
| 输出 | ¥4 / 百万 token | 模型**写**出来的每一个 token |

两个比值决定了整笔账的形状：**未命中是命中的 50 倍，输出是命中的 200 倍。** 换成一句人话：

> **模型每写 1 个 token 的钱，等于把 200 个 token 背着走一步。**

**这句话不是修辞，它被实测对上了。** 把三条实测会话合计 9,023 步按上面这张价格表重算[21]：

| 钱花在哪 | token 量 | 花费 | 占比 |
| --- | --- | --- | --- |
| 背着（内容没变，命中复用） | 1,817.1 M | ¥36.34 | 38.5% |
| 第一次读新内容（未命中） | 20.5 M | ¥20.50 | 21.7% |
| 写（模型输出） | 9.41 M | ¥37.64 | **39.8%** |

平均每步：背 203,660 个、写 1,043 个。**写量只有背量的 0.51%，花的钱却是背的 1.04 倍**——隐含兑换率约 200 : 1，与价格表直接算出的 200 完全吻合。

这张表还澄清了一个容易滑过去的说法。**按单价，背着确实极便宜；但真实任务里背的量是写量的 200 倍，于是两者在账单上各占约四成。** 所以准确的说法不是"背着不要钱"，而是：

> **背着的便宜，正好被它庞大的数量抵消掉了；而写只用了 0.5% 的数量，就换来了同等的账单。**
>
> 所以这两笔钱哪一笔更要紧，不能先验地断定——这也正是下一节必须一条一条算的原因。

**而这个差别，本质上是有损与无损的差别：**

| | 重置时要**写下来**什么 | 结果 |
| --- | --- | --- |
| 基线（有损压缩） | **内容**：把这个区间里"以后可能还要用"的东西概括成摘要 | 摘要必须承载语义，所以写得长 |
| 本架构（无损卸载） | **地址**：指出东西在哪（文件、结构、坐标），原文仍然在 git 里 | 地址只是索引，所以写得短；而且原文**本来就不必待在上下文里**，要用的时候照着地址构建一次就行 |

注意"写下来多少"和"上下文里占多少"是**两件事**：摘要和地址都要占据上下文（这部分只是被**读**，便宜），但只有"写出来"的那一步按输出价计费。

**符号。** 后面会用到的每个符号，这里先用白话说清楚。全文只按 **token** 计数；token 是模型读写和计费的最小单位，大约相当于半个到三分之二个汉字。

| 符号 | 白话含义 |
| --- | --- |
| 窗口 $W$ | 一个模型一次请求最多能装多少 token。本文一律按 100 万 token（$W=1\mathrm{M}$）举例。 |
| 上下文长度 | 此刻这一次请求里，一共装了多少 token。 |
| 压缩 | 装不下了，就让模型把已有内容读一遍、写一份更短的摘要，再用这份摘要替换掉原来那一段。 |
| 残留 $L_{0}$ | 一轮刚开始、上下文刚构建完的那一刻，里面装了多少 token。基线的残留是 $C_{c}$（摘要 + 保留的尾部），本架构的残留是 $P+s$（前缀 + 切片）。 |
| 触发点 $\theta$ | 上下文涨到"窗口的百分之几"的时候触发一次压缩。$\theta=50\%$ 就是"装到一半就压"。 |
| 步数 $T$ | 模型的一次"思考 + 动作"算一步，整个任务一共 $T$ 步。 |
| 每步新增 $g$ | 每一步平均往上下文里新增多少 token。 |
| 前缀 $P$ | 所有分支、所有轮次都用、而且内容稳定的那一段：项目结构说明、意图映射（哪个功能大致落在哪些结构上）、必要的系统状态。注意 $P$ 是它**在上下文里占多少 token**，是一个"读"的量。 |
| 申报量 $M$ | 主 Agent 为了更新前缀，**真正写出来**多少 token。因为写的是坐标而不是正文，$M$ 通常远小于 $P$。 |
| 构建 | 上下文不是攒出来的：每一轮开始时，它是照着当时的环境**重新构建一份**递过去，而不是从前一轮接着往下续——**上一轮构建过什么，默认都不在**。构建的时候，由 Harness 按申报的坐标把原文取出来当作前缀（这一步也叫"装配前缀"）；它不花模型的输出钱，只让模型在读的时候多读 $P$ 个 token。 |
| 切片 $s$ | 只发给某一个分支的那部分文件内容。 |
| 不在上下文里（默认） | 一份内容处在"没被构建进这一轮上下文"的状态。**这是默认状态**：不需要谁做决定、不需要动作、不花钱。四个台阶的最后一级说的就是它。 |
| 边界 | 一轮结束、一个分支退出、整个任务收尾时，那一轮的上下文整体消失。因为原文都在 git 里，**消失的不是内容，只是"它曾经被构建在上下文里"这件事**——所以它不需要判断、不花钱。 |
| 摘要长度 $\sigma$ | 基线每压缩一次，模型要**写**出来多少 token（摘要 + 思考）。 |
| 命中价 $p_{h}$ | 内容没变、被缓存复用时，读一个 token 的单价。本文取 ¥0.02/M。 |
| 未命中价 $p_{m}$ | 内容变了、缓存作废、必须重新读一遍时的单价。本文取 ¥1/M。 |
| 输出价 $p_{o}$ | 模型**写**出来的 token 的单价。本文取 ¥4/M。 |
| 重发现 $\rho$ | 被摘要丢掉的信息，后来又被用到，只能重新推导一遍。每一步平均要重新推导的量记作 $\rho$。 |
| 压缩间隔 $k_{b}$ | 基线的两次压缩之间隔多少步。 |
| 轮长 $L$ | 本架构里，**一个分支**走满一轮需要多少步。 |
| 轮数 $R$ | 本架构一共分多少轮。每合并一次、并重建一次前缀，算一轮。注意一轮里有 $N$ 个分支并行，所以按总调用数摊算是 $R=T/(N\cdot L)$，**不是** $T/L$。 |
| 分支数 $N$ | 一轮里同时开多少个分支。 |
| 固定成本 $F$ | 每一轮都要交、而且与任务多长无关的成本：分叉、定契约、准备合并。 |
| 协调开销 $\varepsilon$ | 本架构比基线多出来的步数比例（定契约、委派、写断言这些步）。 |
| 每次重置成本 $E$ | 一次"重置"要花的钱。基线是 $E_{b}$，本架构是 $E_{d}$。 |
| 有效残留 $R_{b},R_{d}$ | 把"每次重置要花的钱"折算成"相当于常态多背多少 token"，再加到残留上，得到一个两边可比的量。**有效残留小的那一方更省。** |
| 差额 $\Delta$ | 两套架构**每一步**平均占用的 token 数之差。 |
| 前缀变动量 $\Delta P$ | 新一版前缀里**真正变了**的那一段有多少 token。段序稳定时它落在前缀尾部，前半段照旧命中缓存。 |
| 重发现盈亏平衡点 $\rho^{*}$ | 基线的重发现量 $\rho$ 涨到多少，才会把本架构的成本优势吃光。 |

#### A1.1 两套架构的成本来源构成

**基线（传统累积 + 边界压缩）的成本来源**

| # | 类别 | 来源 | 具体是什么 | 随什么增长 |
| --- | --- | --- | --- | --- |
| 1 | 背着 | 过时内容被反复重读 | 早就没用了的中间结果还留在上下文里，后面每一步都要重新读它一遍 | $T^{2}$（完全不压缩时）／$T\cdot k_{b}$（压缩后） |
| 2 | 重置 | 压缩把缓存打掉 | 压缩会把替换点之后的上下文全部重写，缓存作废，之后只能按**未命中价**重新读 | $(T/k_{b})\times C_{c}$（未命中价） |
| 3 | 重置 | 写摘要 | 每压缩一次，模型就要写一份摘要，还要先想一遍，**这是输出价** | $(T/k_{b})\times\sigma$ |
| 4 | 重置 | 重发现 | 摘要丢掉的细节之后又被用到，只能重新推导一遍，**同样是输出价，而且不封顶** | $\rho\cdot T$ |
| 5 | 背着 | 压缩这个动作要读输入 | 压缩时必须把被替换掉的那一段重新读一遍（多数是命中价） | $(T/k_{b})\times$ 区间长度 |
| 6 | 背着 | 探索不受控 | 上下文越长，越容易在中段丢信息，模型乱找路的概率越高 | 表现为 $T$ 和 $\rho$ 都变大 |

**本架构的成本来源**

| # | 类别 | 来源 | 具体是什么 | 随什么增长 |
| --- | --- | --- | --- | --- |
| 1 | 固定 | 分叉 | 每一轮做一次 git 操作，给分支切一份代码 | $R$，**固定、极小** |
| 2 | 重置 | 定契约 | 事先说好每个分支要产出什么、边界在哪里。它在上下文状态上与合并只差一条提交记录，因此和合并算同一件事 | 与合并同项 |
| 3 | 重置 | 合并与解冲突 | 两个分支改到同一批文件时要解开冲突，成本由这种重叠的程度决定 | $N^{2}c$ |
| 4 | 重置 | 更新前缀 | 主 Agent **申报坐标**（写下"东西在哪"），Harness 按坐标把原文装配成新前缀。写入量是申报量 $M$，按输出价；装配出来的 $P$ 是被"读"的，走命中价，**只有每版变动的那一段 $\Delta P$ 才付一次未命中** | $R\times M$（输出价）＋ $R\times\Delta P$（未命中价，每版一次） |
| 5 | 背着 | 回归测试 | 改完跑一遍测试，确认没有弄坏原有功能 | 与基线相同，两边抵消 |

**关键观察：这套账里只有两种钱——"背着"和"重置"。** "背着"单价极便宜（¥0.02/M），"重置"极贵（¥4/M，是前者的 200 倍）。基线的重置有三项（2、3、4），其中重发现（第 4 项）不封顶；本架构的重置只有一项，而且**写的是地址、不是内容**，所以它的量级是 $R\times M$ 加上每版一次的读，而不是 $R\times P$。**下一节会把这两侧算清楚——结论是：重置一侧两边基本打平，胜负其实落在"背着"那一侧，落在能站在多低的水位上。**

**还有第三笔钱，实测里它占 21.7%，但两张表里都没有列：第一次读新内容。** 每一步产生的工具结果、用户消息，都是全新内容，第一次读必然未命中，之后才转为命中。它在两套架构里都存在，因此**做差时会抵消掉**，不进结论；但读者应该知道它的存在，否则会以为"背着 38.5% + 重置 2%"就是全部账单。

**在这两张表之外，还有一件更根本的事，它们都没能表达出来：本架构省下的钱，大头不是"少写"，而是"根本没构建"。** 基线把一份内容构建进上下文之后，想让它出来，只有压缩一条路。本架构里，内容默认就不在上下文里——要它出现，得先有人决定构建它。**需要判断的问题于是从"该丢什么"变成了"该构建什么"**，而后者是照着坐标就能核对的事。下一节把这条差别折成四个价码来看。

#### A1.2 两种钱、四个台阶

这套账里只有两种钱：**背着**（内容装在上下文里，每一步都要被读一次）与**重置**（上下文换一版时，我们必须写下一点什么——基线写摘要，我们写坐标）。

但往下看之前，得先接受一个前提——后面每个数字的读法都由它决定：

> **上下文不是攒出来的，是每一轮从 0 构建出来的。**

模型自己不带记忆。每一轮递过去的上下文，都是照着当时的环境**重新构建一份**，而不是把前一轮接着往下续。所以在这套架构里，**"一份内容不在上下文里"是默认状态**——不需要谁做决定、不需要动作、不花钱；反过来，**"它在上下文里"才是那个需要理由的状态**：得有人决定把它构建进来，付一次它头一回被读的钱，然后在此后的每一步继续为它付钱。

这个前提一摆出来，两条路线的差别就不再是"谁删得更聪明"，而是**"谁需要删"**。基线把内容构建进去之后，想让它出来只有压缩一条路：一次有损的、必须做判断的、要付三笔钱的动作。本架构里没有这个动作——内容默认就不在里面，要用才构建，用完回到默认。**序言里记的那三次翻车（工具剪切收益极低、结论压缩拿不到模型的剪切信号、压缩产物的坐标不准导致组装出错），全都是"做减法"时翻的车；减法在这套架构里根本不存在。**

**一份内容在这一生里，会在四个不同的价码上各停一停**，而它们的单价差着两个数量级：

| 台阶 | 这份内容在做什么 | 单价 | 折合"背着"多少步 | 属于哪种钱 |
| --- | --- | --- | --- | --- |
| 一 | **被写下来**（写摘要与写坐标同档） | ¥4 / M | 200 步 | 重置 |
| 二 | **第一次被构建进上下文**（构建过又离开、之后再取回来一次，也是这一档） | ¥1 / M | 50 步 | 背着（未命中） |
| 三 | **此后每一步都被读一次** | ¥0.02 / M | 1 步 | 背着（命中） |
| 四 | **根本没被构建进去**（原文留在 git 里，随时可照坐标取回） | **0** | 0 步 | 不付 |

第四列是这张表最有用的一列——它把"贵不贵"换算成了同一个尺子。**写一次的钱，等于把它背 200 步；构建一次的钱，等于把它背 50 步。** 于是对**已经构建进来的**每一份内容，该留还是该走只看一件事：**它下一次被用到，大概在多少步之后。** 五十步以内还会用到，就让它留着（继续待在台阶三）；下一次用到在五十步以外，就让它走（回到台阶四），到要用的时候再构建（台阶二）——只有那种每一步都得被参考的东西，才配一直停在台阶三。

只要一份内容还留在上下文里，模型每一步就必然要读它一次——台阶三收的就是这笔钱。所以"不构建"不是省钱的小技巧，它是一条判据：

> **对每一份内容挨个问一句：它值不值得此后每一步都被读一次？**

值，就以坐标的形式一直背着（比如那张"这个功能会落在哪些结构上"的意图映射）；不值，就让它用完之后回到"不在里面"的默认态，要用的时候再构建。

**我们的纪律只有两句，它们管的是两件不同的事：**

- **第一句管"写"和"构建"：每一份内容，台阶一只付一次、台阶二只付一次。** 一份原文尽量不要这一轮写一遍、构建一遍，下一轮又写一遍、构建一遍。这也是"把它写进前缀"比"反复按需读取"划算的原因——前缀每一轮都装配在同一个位置、逐字节不变，于是它只在第一次（或它真正变动的那一次）付台阶二，之后每一轮都直接命中。
- **第二句管"留"：构建完就让它走，除非它值得此后每一步都被读一次。**

**"一直背着"和"只付一次的未命中钱"因此并不矛盾**：一直背着的只能是那几样每一步都要看的东西（前缀），其余的只构建一次。**而基线的病根，正在于这两句它一句也做不到**——每压一次就把新残留整段重读一遍（台阶二付了又付），同时把一大堆积压内容一直背到触发点（台阶三也一直付）。实测里换轮时的缓存失效（占全部未命中 token 的 34%–43%），就是前一句的账单。

第 2 种钱的**单价**贵得多——是第 1 种的 200 倍。但真实任务里第 1 种的数量差不多也是第 2 种的 200 倍，所以两者在账单上份量相当（38.5% 对 39.8%）。**这一点很关键：它意味着"在哪一侧省钱"不是先验的，必须算。**

**先算"背着多少"。** 上下文在两次重置之间，是从"残留"一路涨到"触发点"的。如果每一步新增的量大致固定，那它就是一条直线；而一条直线在这一段里的平均值，正好等于两端的中间值：

$$\overline{L}=\frac{L_{0}+\theta W}{2}$$

| 符号 | 白话含义 | 基线 | 本架构 |
| --- | --- | --- | --- |
| $\theta$ | 触发重置时，上下文占窗口的比例 | $\theta_{b}$：**实测中位 31%**（见 A1.3） | $\theta_{d}\approx 20\%\sim 30\%$ |
| $L_{0}$ | 一轮刚开始、刚构建完时的残留长度 | $C_{c}$（摘要 + 保留的尾部） | $P+s$（前缀 + 切片） |
| $\overline{L}$ | **每一步平均带着多少 token** | $(C_{c}+\theta_{b}W)/2$ | $(P+s+\theta_{d}W)/2$ |

**因为两次重置之间是直线增长，这个式子不含可调参数：它是算出来的，不是拟合出来的。**

**实测校验：这个式子对得上量级。** 干净会话实测每步平均占用 208,853，式子算 $(15.2\mathrm{k}+310\mathrm{k})/2=162.6\mathrm{k}$，低估约 28%——因为增长并不均匀，而且用户换轮时并不重置时钟。这个偏差在两边以同样方式存在，不改变比值；它只说明本节的**绝对金额整体偏低**（对双方都偏低）。

于是两边每一步"背着"的差额是

$$\Delta=\frac{(\theta_{b}-\theta_{d})W+(C_{c}-P-s)}{2}$$

整个任务里"背着"这一侧的差额，换算成钱就是 $T\cdot\Delta\cdot p_{h}$。

**再算"重置一次多少钱"。**

基线重置一次，要写一份摘要，还要把压缩后新拼出来的上下文第一次读一遍：

$$E_{b}=\sigma\cdot p_{o}+C_{c}\cdot p_{m}$$

其中第一项是**写摘要**（输出价），第二项是**新内容第一次读**（未命中价）。

本架构重置一次，要写一份坐标申报，还要让分支把这一版新前缀读一遍——**未命中只付一次，不按分支数付**：

$$E_{d}=M\cdot p_{o}+\Delta P\cdot p_{m}+(P-\Delta P)\cdot p_{h}$$

第一项是**写申报**（输出价）。第二项要拆成两半，因为**前缀是 Harness 装配出来的、与模型决策无关的纯上下文产物**：

- 前缀里**变了的那一段** $\Delta P$，缓存作废，按未命中价付；
- 前缀里**没变的那一段** $P-\Delta P$，字节没动，**照旧命中缓存**，按命中价付。

只要装配时段序稳定（本设计要求的正是"重写不重排"），没变的部分就一直躺在缓存里。而且这一版前缀只在**第一个分支的调用**上付这一笔，之后所有分支、所有轮内步骤全部命中——所以它按"前缀版本数"计，**不按分支数计**。

**这里有一个取法上的选择。** 贴合机制的一种是：只有**变了的那一段** $\Delta P$ 失去缓存，没变的那段继续命中——本设计要求"重写不重排"，正是为了这一点。另一种是保守地把它整段按未命中算（即 $\Delta P=P$）。**本节基准档用后者**，A1.7 给出两者差额及其影响。

#### A1.3 基线不能"想压就压"

**这里有一个最容易犯的错：把基线的触发点 $\theta_{b}$ 当成一个可以随便拧的旋钮。**

如果基线"想压"就能压到 30%，那么上面的 $\Delta$ 会缩到很小、甚至变负，于是很容易得出"基线只要自己勤快一点，本架构的成本优势就没了"。**这个算法是错的，因为它等于让基线免费压缩。**

基线压低触发点的手段**只有压缩**，而压缩要花钱。两次压缩之间只隔

$$k_{b}=\frac{\theta_{b}W-C_{c}}{g}$$

步。触发点压得越低，这个间隔越短，压缩次数涨得越快。于是基线手上有两笔方向相反的钱：

| 基线把触发点压低 | 哪一笔变便宜 | 哪一笔变贵 |
| --- | --- | --- |
| $\theta_{b}$ 下降 | **背着**的钱按 $\theta_{b}$ 线性减少 | **重置**的钱按 $1/(\theta_{b}W-C_{c})$ 上升 |

两笔一平衡，就得到基线自己会选的那个最优触发点：

$$\theta_{b}^{*}W=C_{c}+\sqrt{\frac{2gE_{b}}{p_{h}}}$$

代回去，基线能压到的最低总成本是：

$$C_{b}^{*}=T\cdot p_{h}\cdot R_{b},\qquad R_{b}=C_{c}+\sqrt{\frac{2gE_{b}}{p_{h}}}$$

**这里的 $R_{b}$ 读作"有效残留"**——它把"每次重置要花的钱"折算成了"相当于常态多背多少 token"。

**现在把实测值代进去。** 用干净会话量出来的 $g=1{,}460$、$C_{c}=15.2\mathrm{k}$、$E_{b}\approx$ ¥0.029，基线自己的成本最优触发点是

$$\theta_{b}^{*}=\frac{C_{c}+\sqrt{2gE_{b}/p_{h}}}{W}\approx 8\%$$

**也就是每 45 步压一次、一个两千步的任务压 46 次。而实测是这样的**——干净会话里每一次压缩我全取出来了，全部由人手动触发，丢弃量由 Harness 自己报出：

| 触发时占用 | 丢弃 token | 压完保留 | 丢/留 |
| --- | --- | --- | --- |
| 23.5% | 165,397 | 14,585 | 11.3× |
| 29.3% | 206,478 | 15,489 | 13.3× |
| 52.4% | 433,498 | 14,427 | 30.0× |
| 35.2% | 242,800 | 15,011 | 16.2× |
| 32.5% | 244,932 | 13,834 | 17.7× |
| 44.7% | 333,979 | 17,933 | 18.6× |
| **5.9%** | 27,203 | 15,334 | **1.8×** |
| 20.2% | 115,939 | 15,486 | 7.5× |
| 30.9% | 178,092 | 15,908 | 11.2× |

触发占用中位 **30.9%**；九次总共丢掉 194.8 万 token，每次只留 1.5 万。

**注意唯一那一次 5.9% 的压缩：丢/留比掉到了 1.8×。** 占用已经很低时人就不再压了——不是不想省钱，而是**再压就真的没有东西可压了**。

> **8% 这个"成本最优"成立的前提，是把 $\rho=0$ 和"丢掉的东西一文不值"两条假设推到极致。它数学上成立，工程上等于算出了一条"把球砸向地板再弹进篮框"的轨迹——它把任务继续执行所必需的那些上下文当成了零。**
>
> **九次里，那个能看见任务的人九次都不同意这个判断：他停手的位置，中位数是 30.9%。**

于是那句对照按实测写出来是这样：

> **20%–30% 是能力上的要求（实测：人停手的位置中位 30.9%）；而只有有损压缩这一件工具时，钱算出来的均衡在 8%。两者的差，就是压缩税的实测大小——它大到没有人敢去领。**

**本架构的式子形状与基线完全一样。** 我们也要重置，重置成本是 $E_{d}$（见 A1.2）。它由三件事决定，**每一件都不是估计，而是这套设计本身决定的**：

1. **写的是地址，不是内容。** $M$ 是坐标申报量，$\sigma$ 是摘要量。摘要必须承载语义（"这段区间里以后还要用的东西**是什么**"），地址只承载位置（"东西**在哪**"）。**不在上下文里的那些内容，原文还在 git 里，所以不需要把它们写进上下文。**
2. **一次重置服务 $N$ 个分支。** 一轮里 $N$ 个分支并行推进，而合并与重建前缀在这一轮结束时只做一次。按总调用数摊算，我们的重置频率约为基线的 $1/N$。
3. **未命中只付变动的那一段。** 前缀没变的部分照旧命中缓存（这一条的分量由 A1.7 算清楚）。

**但必须说清楚：在实测参数下，我们的"单次重置"并不比基线便宜，反而更贵。** 因为我们的残留（$P+s=60\mathrm{k}$）是基线残留（$C_{c}=15.2\mathrm{k}$）的四倍，而每换一版前缀就要把它读一遍——**单次重置 ¥0.060，比基线的 ¥0.029 贵一倍。** 我们赢回来的是**频率**：基线每 202 步重置一次，我们每 384 步（$N\cdot L=4\times96$）一次。两边相乘之后，重置这一侧基本打平（¥0.32 对 ¥0.30）。

**所以这套账的胜负不在重置，而在"背着"。** 而我们"背着"更少的唯一原因，是我们能合法地站在更低的水位上——这句话在 A1.4、A1.6、A1.7 里会反复出现，因为它就是全部结论。

最优触发点满足同一个形状的式子：

$$\theta_{d}^{*}W=(P+s)+\sqrt{\frac{2gE_{d}}{N\,p_{h}}}$$

**于是两套架构被放进了同一个式子里：谁的有效残留更小，谁就更省。**

$$R_{d}=(P+s)+\sqrt{\frac{2gE_{d}}{N\,p_{h}}}\quad\text{对比}\quad R_{b}=C_{c}+\sqrt{\frac{2gE_{b}}{p_{h}}}$$

#### A1.4 做差：实测锚定与保守估计

本节把每个参数都标出来源和方向：能从记录里量出来的就量，量不出来的取保守值，并且说明这个方向为什么算是保守。

**能实测的（从干净会话里量出来的）：**

| 量 | 取值 | 从哪来 | 说明 |
| --- | --- | --- | --- |
| $W$ | 100 万 token | 请求头 | 前提 |
| $T$ | **2,068 步** | 干净会话 | 任务全程 |
| $g$ | **1,460 / 步** | 实测均值（中位 703） | 增长并不均匀，均值更能代表累积量 |
| $\theta_{b}$ | **31%** | 九次人工压缩的中位 | 人停手的位置 |
| $C_{c}$ | **15.2k** | 实测 | 摘要 + 保留的尾部 |
| $\sigma$ | **5k** | 摘要正文 3–8k | 写出去的量 |
| $E_{b}$ | **¥0.029** | 由 $\sigma$、$C_{c}$ 与价格算出 | 单次压缩的钱 |
| $k_{b}$ | **202 步**（实测 230） | 由 $\theta_{b}$、$C_{c}$、$g$ 算出 | 两次压缩之间的间隔 |
| 缓存命中率 | **98.6% – 99.7%** | 实测 | 整套账的前提，成立 |

**量不出来、只能取的（逐条标出这个方向偏哪边）：**

| 量 | 取法 | 取这个方向的理由 | 偏哪边 |
| --- | --- | --- | --- |
| 价格 | 取**低谷价** | 缓存越便宜，"背着"那一侧的优势越小 | 偏不利（保守） |
| $P+s$ | 取**大**（60k），并且假设切片整段预先构建 | 本架构残留得越多，它越重；按需取用本来能让切片的一部分根本不进上下文（见 A1.6 第 8 条） | 偏不利（保守） |
| $M$ | **取大**（10k） | 申报量越大，我们的重置越贵。现实申报是坐标，量级是一步的输出（实测每步输出 833–1,133 token） | 偏不利（保守），且偏得极重 |
| $\Delta P$ | 取**极端**（$=P$：整段前缀按未命中） | 我们的重置因此最贵 | 偏不利，但越过了真实机制（见 A1.7） |
| $\rho$ | **取 0** | 重发现完全不计——白送基线一笔 | 偏不利（保守） |
| $\varepsilon$ | **取 0** | 本架构多出来的协调步完全不计 | 偏不利（保守） |
| 合并冲突 $N^{2}c$ | **不入账**（只写进 A1.1 的成本表） | 每轮真实存在 | 偏不利（保守） |
| 质量与能力收益 | **一律设为 0** | 见 A1.6 | 偏不利（保守） |
| 并行省下来的时间 | **不计入结论** | 不拿时间上的好处去补贴成本上的主张 | 偏不利（保守） |
| $N$ | 取 4 | 把"设计被正确使用"当成了既定事实 | **偏有利（不保守）**：实测 $N=1$ |
| $\theta_{d}$ | 取 **20%** | 假定我们能在很低的水位上正常工作 | **偏有利（不保守）**：这是能力假设（见 A1.7） |

**这张表里有两条偏的不是不利的那一侧**——$N=4$ 与 $\theta_{d}=20\%$；再加上越过了真实机制的 $\Delta P=P$，一共三条。A1.7 逐条说明它们把结论推到了哪里。

参数取值：

| 量 | 取值 | 量 | 取值 |
| --- | --- | --- | --- |
| $W$ | 100 万 token | $\sigma$ | 5k（实测） |
| $g$ | 1,460 / 步（实测） | $M$ | 10k（保守取大） |
| $T$ | 2,068 步（实测） | $P$ | 20k（保守） |
| $C_{c}$ | 15.2k（实测） | $s$ | 40k（保守） |
| $\theta_{b}$ | 31%（实测） | $N$ | 4（另给 1） |
| $\theta_{d}$ | 20% | $p_{h}/p_{m}/p_{o}$ | 0.02 / 1 / 4（元每百万 token） |

**先把一件不好看的事说在前面：$\theta_{d}=20\%$ 本身就是一条偏向我们自己的假设。** 不受能力约束时，我们自己的成本最优会压到 **8%–11%**（取决于申报量与前缀计价）——**和基线的 8.0% 落在同一个区间**。也就是说，**如果两边都老老实实按钱来调，它们的最优点几乎落在同一个地方，差价只来自残留不同。** 本架构真正的成本主张是"**能在同一个能力水位上合法地把占用压得更低**"，这个主张的证明在 A1.6，不在本节算术里。为了让读者能自己判断，下面同时给出 $\theta_{d}=31\%$（与基线同水位）的结果。

先算中间量：

| 中间量 | 怎么算 | 结果 |
| --- | --- | --- |
| 基线每次重置 $E_{b}$ | 5k × ¥4/M ＋ 9.2k × ¥1/M | **¥0.029** |
| 基线的压缩间隔 $k_{b}$ | $(310\mathrm{k}-15.2\mathrm{k})/1{,}460$ | **202 步**（实测 230） |
| 本架构每次重置 $E_{d}$ | 10k × ¥4/M ＋ 20k × ¥1/M（前缀整段按未命中） | **¥0.060** |
| 本架构每次重置 $E_{d}'$ | 10k × ¥4/M ＋ 20k × ¥0.02/M（前缀走命中） | **¥0.040** |
| 本架构的轮长 $L$ | $(200\mathrm{k}-60\mathrm{k})/1{,}460$ | **96 步** |
| 本架构的轮数 $R$ | $T/(N\cdot L)=2{,}068/(4\times96)$ | **5.4 轮** |

然后是两套架构各自的账（一次 2,068 步的任务）：

| | 背着 | 重置 | 合计 |
| --- | --- | --- | --- |
| 基线，停在实测的 31% | ¥6.72 | ¥0.30 | **¥7.02** |
| 基线，撞 DSH 默认阈值 80%（完全不管控） | ¥16.86 | ¥0.11 | **¥16.97** |
| 基线，压到它自己的钱最优 8% | ¥1.98 | ¥1.35 | **¥3.33** |
| 本架构，20%、4 分支、申报按保守的 10k | ¥5.38 | ¥0.32 | **¥5.70** |
| 本架构，同上但前缀按命中计价 | ¥5.38 | ¥0.22 | **¥5.59** |

**结论：对实测的真实基线（人停手在 31%），省约 19%；对完全不管控、一路撞到 80% 默认阈值的用法，省约 66%。**

**但对照"基线自己的钱最优"（8%）必须诚实：那时它 ¥3.33，我们 ¥5.70，我们贵 71%。** 而那条路要求它每 45 步压一次、一个任务压 46 次——实测里人压了 9 次。**这不是一个"它随时可以选"的选项，这是 A1.3 里那条"砸地板进篮框"的轨迹。**

**把几个最要紧的假设单独拎出来看：**

| 情形 | 我们的成本 | 对真实基线 ¥7.02 | 对不管控基线 ¥16.97 |
| --- | --- | --- | --- |
| 基准：20%、申报 10k、4 分支 | ¥5.70 | 省 19% | 省 66% |
| 同上 + 前缀按命中计 | ¥5.59 | 省 20% | 省 67% |
| **与基线同水位（都停在 31%）** | **¥7.83** | **贵 12%** | 省 54% |
| 不并行（$N=1$），其余同基准 | ¥6.25 | 省 11% | 省 63% |
| 不并行且前缀整段未命中（双保守） | ¥6.67 | 省 5% | 省 61% |
| 申报回到现实值 2k | ¥5.53 | 省 21% | 省 67% |
| 申报 2k + 前缀按命中 | ¥5.42 | 省 23% | 省 68% |
| 悲观：模型整篇手写 20k | ¥5.92 | 省 16% | 省 65% |
| 双重悲观：整篇手写 20k **且**不并行 | ¥7.53 | **贵 7%** | 省 56% |

**两条读法：**

1. **只有在"模型整篇手写前缀"并且"完全不并行"同时成立时，我们才会输给真实基线**——而这两件事正是这套设计本身否定的。
2. **与基线站在同一个水位上时，我们反而贵 12%。** 这不是算错，是这套账的结构决定的：**我们的残留（$P+s=60\mathrm{k}$）是基线残留（$C_{c}=15.2\mathrm{k}$）的四倍。本架构的钱，全部赚在"能把水位合法地压低"这一件事上；水位相同，我们没得赚。**

**胜负由什么决定。** 把两边并排，只有三个量能动。

**一、我们自己的残留 $P+s$ 有一个明确上限。** 令两边的有效残留相等，解出我们的残留最多可以多大：

$$P+s<C_{c}+\sqrt{\frac{2gE_{b}}{p_{h}}}-\sqrt{\frac{2gE_{d}}{N\,p_{h}}}$$

| 申报量 $M$ | 前缀计价 | 残留上限 $P+s$ |
| --- | --- | --- |
| 2k | 命中 | **63k** |
| 10k | 命中 | 42k |
| 10k | 整段未命中 | 34k |

**读法：只要"前缀 + 切片"不超过 63k 我们就赢；本节保守取的 60k 刚好卡在线内，只剩 3k 余量。** 这条线不是装饰——它意味着**"前缀能不能比摘要更省地表达全局"是本架构的生死线**，而不是一个可以无限膨胀的容器。

**二、申报量 $M$ 的盈亏平衡点约 2.8k。** 当前缀按缓存命中计价，只要每轮申报不超过 2.8k，我们就赢；而申报的现实量级是**一步的输出**（实测每步输出 833–1,133 token）。**但如果前缀整段都按未命中计，这道式子无解**——那时 $E_{d}$ 里光"把前缀读一遍"就是 ¥0.02，已经超过全部预算 ¥0.0115，**无论申报量多小我们都赢不了**。这一笔在 A1.7 里单独交代。

**三、重发现 $\rho$ 的盈亏平衡点是 160–194 token/步。** 按实测参数，我们领先真实基线约 ¥0.00064–0.00078 每步，而 $\rho$ 走输出价，于是

$$\rho^{*}=\frac{0.00064\sim0.00078}{p_{o}}\approx 160\sim194$$

（单位：token / 步。这正是那笔在账单上从来不会单独列出来的钱——因为它伪装成了"模型正在思考"。）

实测里压缩之后的追赶峰值到过 **1,553 与 4,983 token/步**（九次压缩中有六次测不出追赶，两次很明显）。所以 $\rho$ 不是一个"符号对不对"的问题，**而是一个量级相当的真实风险**：只要基线平均每步多花 200 个 token 重新推导，那 19% 就被吃光了。

**$N$ 与成本的关系最容易被高估。** 按前缀命中计，**$N$ 只在申报量 $M$ 大时才重要**：$M=2\mathrm{k}$ 时，$N$ 从 1 涨到 4 只带来 2 个百分点的差别；$M=10\mathrm{k}$ 时带来 9 个百分点。**开分支的理由因此落在能力、隔离与可回滚上，而不是成本。**

于是判据变成三个**具体的、可以被否掉的问题**：

> **一、每轮更新前缀时，模型真正"写出来"的是坐标（不超过 2.8k token），还是整篇正文（20k token）？**
>
> **二、装配前缀时，段序是否稳定到"没变的那半段能一直命中缓存"？**（这一条决定生死，见 A1.7）
>
> **三、"前缀 + 切片"能不能压在 63k 以内？**

第一条正是本文序言里已经走过的路——**从"申报原文"改成"申报坐标"**。第二条的分量最容易被低估。第三条是规模的暴露面。

#### A1.5 规模变大之后：三推一反

A1.3 的两个闭式里有两个容易被忽略的性质，先说清楚，因为它决定了趋势该怎么谈：

- **任务长度 $T$ 只是一个共同乘数。** 两边都随 $T$ 线性放大，所以 **$T$ 不改变胜负方向，只放大结论**。**"任务会变长"本身不是论据**——这一点必须先说清楚，否则会给人"我们是靠任务长赢的"错觉。
- **窗口 $W$ 不进入最优总成本。** 因为 $W$ 变大时最优触发点会跟着往下走（$\theta^{*}$ 是解出来的，不是定死的），两边的最优总成本里都没有 $W$。**"百万窗口"对双方的最优解既不帮忙也不添乱**——它惩罚的只是**死守某个固定百分比、不调参**的用法。

**那么"规模变大"到底改变了什么？** 把实测数据摊开看，是**三推一反**。

**推力一：失控的基线会一路右移到 80%。** $\theta_{b}$ 不是一个常数，它是"**谁在管上下文**"的产物。实测里能清楚看到三种活法：

| 活法 | 触发点 | 每步平均背着 | 一次 2,068 步 |
| --- | --- | --- | --- |
| 程序员手动压（能看见任务） | 31% | 162.6k | ¥7.02 |
| 完全不管控 | 撞 DSH 默认 80% | 407.6k | ¥16.97 |
| 纯按钱调（不可达） | 8% | 47.8k | ¥3.33 |

实测里那条**从不压缩、455 步一路顶到 70.2%** 的会话，就是"不管控"长什么样；同一批数据里，管与不管的每步成本差 **1.33 倍**。**用户越多、任务越杂、越没人盯上下文，基线就越往右边那一档滑。**

**推力二：摘要的容量不随规模增长，而任务必需的上下文会。** 实测里残留恒定在 1.3–1.8 万，与任务多大无关——因为它是**模型一次写出来的、长度固定的摘要**。任务一大，被丢掉的细节就多，$\rho$ 就往上走，而 $\rho$ 按**输出价**计费（是背着的 200 倍）。**这一条是结构性的，不依赖任何参数，也是本架构最硬的论据。**

**推力三：用户级的任务天然可并行。** 实测 $N=1$——程序员在 2,068 步里一次子代理都没开。用户级任务（多来源、多方案、多文件）天然能切片并行，$N>1$ 会变成常态。不过 A1.4 已经算过：**$N$ 只在申报量 $M$ 大时才重要**；$M$ 一小，$N$ 就退居次席。

**反方向一：我们的残留 $P+s$ 也会随规模增长。** 这是唯一的暴露面。基线不管任务多大都只留 1.5 万；我们的前缀随代码库涨、切片随任务面涨。A1.4 给的线是 63k——**所以"前缀能不能比摘要更省地表达全局"，是本架构能不能随规模一起赢的分水岭。** 而这条线管的是**每一轮都构建进上下文、此后被反复读**的那些东西：切片可以按需取用（A1.6 第 8 条），所以真正要长期盯住的其实是前缀 $P$。

**于是趋势的落点很具体：不是"任务变长"，而是三件事——基线会不会失控、$\rho$ 会不会涨、我们的残留会不会跟着涨。** 前两件对我们有利，第三件是风险。

还差最后一个实测：**每轮更新前缀时，模型真正新写出来的 token 有多少？第 1 轮（要建底座映射）与第 2…$R$ 轮（只改增量）差多少？** 如果第 1 轮明显更贵、后面很便宜，说明底座确实被前缀分摊掉了；如果每轮都差不多，说明"底座映射"没有被复用，$M$ 压不下来。

#### A1.6 没有计入账里的收益

A1.4 的账里有几笔明显对我们有利的钱，**不是在账里被设成了零，就是被按最笨的活法算了一遍**：重发现 $\rho=0$、协调开销 $\varepsilon=0$、准确前缀省下来的工具调用不算、并行省下来的时间不算、切片按整段预先构建、合并冲突不入账。把这几笔加回来，就是下面这些。它们逐条列出，**不在账里计，但应当被知道**。

**（1）平均上下文更短，注意力更集中。** 本架构每一步的平均上下文长度比基线低 $\Delta$（按实测参数：触发点从 31% 降到 20%，平均每步少背约 33k；对完全不管控、一路撞到 80% 的基线，少背约 278k）。上下文越长、模型判断质量越低是可复现的现象。**本架构不是让模型更强，而是让同一个模型少受长上下文拖累。**

**（2）中段迷失被结构性规避。** 长上下文里，处在中间位置的信息最容易被忽略。切片把工作时的上下文限制在 20%–30%，中间段本身变短，"中间"这个位置不再显著。

**（3）基线必须做一次"丢什么"的判断，我们这边根本没有"丢"这个动作。** 压缩丢的是**没有被计划过**的信息——你无法预知摘要丢掉的那一行会不会在十步之后被用到，**因此不可补偿**。而切片只是**没有被构建进去**：没构建不等于丢了，原文还在 git 里，主 Agent 随时能照着坐标把它构建回来（按需读取），或者干脆把它申报进前缀。**压缩这一步必须判断丢什么，而且丢错了无法挽回；分区不需要这个判断——"构建什么"本身就是可以照着坐标机械校验的。** 这是两条路线最本质的差别，且不依赖任何参数。

**（4）没有有损压缩，因此没有重发现。** 基线的 $\rho$ 来自被摘要丢掉、之后又被需要的细节。本架构里，一个分支做完就退出——它的上下文跟着分支一起消失，但**消失的只是"它曾经被构建在上下文里"这件事，不是内容本身**（原文仍然留在 git 里，照坐标可取回）。既然没有任何东西被删掉，也就没有"重新推导一遍"这笔钱。

**（5）探索受约束，路径更收敛。** 分支启动时拿到的那一小段内容（环境视图 + 结构意图映射 + 委派文件）限定了分支的起点与范围，减少盲目通读和随机试错。这同时降低了 $T$ **和 $T$ 的波动幅度**——而波动变小在成本上是双份好处：既减少平均返工，也减少做工期估算时必须留的余量。**这一条在 A1.4 的账里被设成了 $\varepsilon=0$（协调开销为零），但它的真实符号是负的**：前缀越准，分支越不需要自己找路，总步数 $T$ 反而比基线更小。

**（6）已有外部证据表明"边界"本身就是主要增量。** 在公开的多智能体对照实验中，同一个弱执行者被强规划者给定边界后，得分从 46 / 71 / 87 分别提升到 93 / 94 / 97；审计者的归因是"执行者写了每一行，但规划者决定了边界，而边界正是把它从 B 抬到 A 的主要原因"。这是与"上下文更长"无关的能力增量，来源正是边界与契约。

**（7）与本文序言记录的失败史一致。** 系列被放弃的压缩路线（工具剪切收益极低、结论压缩因模型难以给出剪切信号而弃用、压缩产物坐标不准导致组装出错），最终收敛到"申报坐标"——**也就是分区加按需读取**。这条演化路径本身就是"分区优于压缩"的一个样本。

**（8）切片不必整段预先构建。** A1.4 的账里，切片 $s=40\mathrm{k}$ 是按"每个分支每一轮都把整段 40k 构建进上下文"算的。但设计本身允许另一条路：只把公共前缀放进去，剩下的文件交给一个工具**按需取用**——没被取用的部分根本不进上下文，于是一路停在台阶四；被取用的部分才走台阶二与台阶三。**这是"从 0 构建"最直接的推论：默认是不构建，构建才是那个需要理由的动作。** 这笔钱能省多少，取决于"分支实际需要取用多少"，我们既没有实测，也不打算把它计进账里，但它说明 A1.4 那个 $P+s=60\mathrm{k}$ 的残留，保守的成分不只是"取大"，还有"假设了最笨的构建方式"。

#### A1.7 三条例外

前面每一处取法都取在对我们不利的一侧，只有三条例外：两条不是保守取法，而是**假设**；一条是取法本身越过了真实机制。它们必须单独说清，否则上面的结论会被读得比它实际支持的更强。

**一、$N=4$ 不是保守值，它把"设计被正确使用"当成了既定事实。** 实测是 $N=1$——那条两千步的干净会话里，人一次子代理都没开。在基准档下，这一条把结论从"省 19%"拉到"省 5%"；在前缀按命中计的档位下，从"省 20%"拉到"省 11%"。

**二、$\theta_{d}=20\%$ 是能力假设，不是成本结论。** 本节所有"省 19%"都建立在"我们能在 20% 的水位上正常工作"之上。不受能力约束时，我们自己的成本最优会压到 8%–11%，和基线的 8.0% 落在同一个区间；**同一个水位下我们反而贵 12%**。本架构在成本上的主张因此不是"我们更便宜"，而是"**我们能在同一个能力水位上合法地把占用压得更低**"——这句话的证明在 A1.6，不在算术里。

**三、前缀整段按未命中，是一条越过了真实机制的取法。** $E_{d}$ 里的前缀项本该拆成两半：变了的那一段 $\Delta P$ 失去缓存，没变的那段继续命中。取 $\Delta P=P$ 等于假装每换一版都要整段重读，差额是 $P(p_{m}-p_{h})=20\mathrm{k}\times(1-0.02)/\mathrm{M}$，也就是 **¥0.0196**。

它单独看只占我们总成本的 1.9%（¥0.11 / ¥5.70），看起来无关紧要——**但它进的是开方号里的 $E_{d}$，于是被放大成"我们能不能赢下基线自己的最优点"这件事**：

| 取法 | $E_{d}$ | 我们的有效残留 $R_{d}$ | 对基线 $R_{b}=80.5\mathrm{k}$ |
| --- | --- | --- | --- |
| 前缀整段未命中、申报 10k | ¥0.060 | 106.8k | **输** |
| 前缀按命中、申报 10k | ¥0.040 | 98.4k | **输** |
| 前缀整段未命中、申报 2k | ¥0.028 | 92.0k | **输** |
| 前缀按命中、申报 2k | ¥0.008 | **77.5k** | **赢** |

**单改前缀的计价还不够，必须同时把申报量从保守的 10k 放回现实的 2k，$R_{d}$ 才能压到 $R_{b}$ 之下。** 而这两件事恰好都是这套设计本来就主张的：**写坐标而不是正文（小 $M$）＋ 段序稳定（前缀可缓存）。**

**这里要留意一点：因为每一轮都是从 0 构建，"段序稳定"这句话的分量比它看上去大**——它决定的正是"上一轮构建在同一位置的那几段字节，这一轮还在不在同一个位置"。在，就继续命中；不在，就整段按未命中重读一遍。**而这份差额的方向是清楚的：把前缀按真实机制计价，只会让我们更便宜。** 本节"省 19%"用的仍是保守档，没有靠它。

第三条不是取法偏向哪一边的问题，它只是**越过了真实机制**。除此之外，本节的立场是这样：

> **在能把差值算清楚的所有项目上，我们都取了对自己不利的一侧；剩下三条不是取法问题，而是这套设计能不能兑现的假设——它们的证明不在账上，在 A1.6。**

#### A1.8 本节结论

**不看公式，只看这件事：**

1. 主流做法是"把内容都攒在上下文里，装不下了就写摘要、把原文删掉"。删掉的东西以后还要用，就只能重新想一遍——**而"想"是按最贵的价格计费的。**
2. 这套做法是"每一轮都从空手开始，照着提货单去仓库取这一轮要用的东西"。提货单上写的是**东西在哪**、不是**东西是什么**，所以写起来很短；取回来的还是原件，用完就放手——**它回到的是"不在上下文里"的默认态，而不是被删掉了。**
3. 于是困难的问题换了：**从"该丢什么"变成"该构建什么"。** 前者必须由模型做一次判断，判断错了无法挽回（序言里那三次翻车都翻在这里）；后者写的是坐标，照着坐标就能核对构建对了没有。**这套账里省下的钱，大头不是"少写"，而是"根本没构建"。**
4. 在这个价位上，"写"极其昂贵：**模型写 1 个 token 的钱，等于背着它走 200 步。** 实测里模型每步只写 1,043 个 token（是背着量的 0.51%），却付出了和"背着"一样多的钱——**账单里有四成，就花在这 0.5% 的量上。**

**看数（一次 2,068 步的真实任务，参数全部来自实测）：**

| | 基线（实测：人停在 31%） | 基线（不管控，撞 80% 阈值） | 本架构（20%、4 分支、申报按保守的 10k） |
| --- | --- | --- | --- |
| 背着 | ¥6.72 | ¥16.86 | ¥5.38 |
| 重置 | ¥0.30 | ¥0.11 | ¥0.32 |
| **合计** | **¥7.02** | **¥16.97** | **¥5.70** |

**对实测的真实基线省约 19%；对完全不管控的基线省约 66%。**

**同时必须说清楚两件不好看的事，否则这张表就是骗人的：**

- **把基线按钱调到它自己的最优（8% 占用、每 45 步压一次），它比我们便宜 71%。** 但那条路要求它在一次任务里压 46 次，而实测里人压了 9 次。**它数学上成立，工程上不可达——因为压缩丢掉的，正是任务继续执行所必需的东西。**
- **站在同一个水位上（都停在 31%），我们反而贵 12%。** 因为我们的残留（60k）是基线残留（15.2k）的四倍。**本架构的钱不是赚在单价上，是赚在"能合法地把水位压低"上。**

所以这一节的最终立场是：

> **成本这一侧，本架构与一个被精心调过的基线大致打平；真正把两条路线分开的，是"能在同一个能力水位上把占用压低"这件事——它在账上表现为 19%，在能力上表现为 A1.6 那八条。**

---

要把本节从"半定量"推到一张可以直接对照的账单，还差三个数：**每轮更新前缀时真正写出的申报量 $M$**、**"前缀 + 切片"的真实残留 $P+s$**、**契约成本的分摊情况**（第 1 轮 vs 后续轮）。实验设计（对照组 = 直接要求完成 RootUp 0.8.8；实验组 = 手动组装前缀与委派）在路线图里。

已经量出来的：三条干净会话、9,023 步、54 次原生压缩，$\theta_{b}$ 中位 **31%**、$C_{c}\approx$ **15.2k**、$\sigma\approx$ **5k**、$g\approx$ **1,460 / 步**、$E_{b}\approx$ **¥0.029**、缓存命中 **98.6%–99.7%**、$N=$ **1**，以及"写 / 背兑换率约 **200 : 1**"。



### 附录2：Harness耦合现象的思考 与 Context作为一等公民的必要性



Deepseek V4 Pro-0813在实际发布后饱受诟病，其被质疑为刷分模型，在除了极简模式 + 类linux之外环境下，均难以产生较好的结果。在上文中，我也曾经提到了关于Harness作为模型原生手脚的比喻，耦合以发挥模型能力上限。



而当我们放下个人视角，从模型训练的角度，Tools是否有泛化的必要与泛化的可能？



在我看来，格式化输出在训练上的对齐是十分必要的，但是模型在Tools的泛化上完全没有必要进行特殊训练。对Tools格式的理解完全可以被通用工具，代码调用覆盖，将参数与信息量浪费在对齐可能出现的不同自定义Tools上面是因噎废食。我们Harness构造的最终目的是能力上的通用，而非不同框架下的普适，就像我们不怎么需要去训练模型写什么Visual Basic代码一样。



而在数据构造的角度来说，我们给模型输入的构造数据，其涵盖的Tool类型也必然是给定有限，优先确定，通用的优秀组合。如Edit，Bash等。其主要目的就是聚焦Agent完成任务过程中，充当眼与手。如果要增加模型对于多样工具调用的适应性，为此降低其对通用普适，且已经足够达成通用能力的工具的调用能力，显然是得不偿失的。



那么，我们所需要面临的一点问题就是Harness框架作为产品的独立性与市场性又该如何保证？每一家的模型在自家Agent上都会默认胜出，那如何通过优秀的Harness设计抢占市场，制定标准，把握用户数据入口？



答案就在于Context作为与模型直接交互的中间层。现在的通用Harness在API层实现对模型的解耦，因此其不能对齐模型的训练数据与偏好环境。真正行之有效的适配方式，是在上下文组装环境为每个模型进行分野。不同的工具调用对于模型的形状各不相同，对应着模型的原生“肢体”。这样的一套通用Agent架构，才能实现对于同一模型，同一发挥下的“可比性”。




### 附录3：引用、外部证据与对照

#### A3.0 标记约定

正文与附录1 里的方括号编号（形如 `[7]`）指向本节 A3.1 的条目；编号只为这类标记服务，不改变任何原有文字。

每个条目都带一个核验层级，全文只用三档：

| 标记 | 含义 |
| --- | --- |
| 【原文】 | 原始页面、论文摘要或官方公告我直接读到，引文逐字可查 |
| 【转述】 | 原始页面不可访问，经镜像、索引或第三方转述核实；数字方向可信，精度待复核 |
| 【未证实】 | 只有标题或转述，正文未核到——**本文不引，仅列在 A3.6** |

#### A3.1 条目

**[1]** Chroma（Hong, Troynikov, Huber），*Context Rot: How Increasing Input Tokens Impacts LLM Performance*，2025-07-14。https://www.trychroma.com/research/context-rot 【原文】

**[2]** NVIDIA（Hsieh 等），*RULER: What's the Real Context Size of Your Long-Context Language Models?*，COLM 2024，arXiv:2404.06654 【转述】

**[3]** Kuratov 等，*BABILong*，NeurIPS 2024，arXiv:2406.10149 【转述】

**[4]** 界面新闻（铑科技），《黄仁勋押注，DeepSeek内测，详解Harness到底是什么？》，2026-08-03。https://www.jiemian.com/article/14868365.html 【原文】

**[5]** Zhuang 等，*AgentRewind: Recoverable Execution for Long-Horizon LLM Agents*，arXiv:2608.14380，2026-08 【转述】

**[6]** OpenHands / All Hands AI，*Context Condenser* 文档。https://docs.openhands.dev/sdk/guides/context-condenser 【转述】

**[7]** Anthropic，*How Claude Code uses prompt caching*（官方文档）。https://code.claude.com/docs/en/prompt-caching 【转述】

**[8]** Preble 等，关于"prompt 之间共享 token 比例"的测量，ICLR 2025，arXiv:2407.00023 【转述】

**[9]** Anthropic，Claude Code 官方文档 *Create custom subagents*、*Worktrees*、*Agent teams* 【转述】

**[10]** Anthropic（Rajasekaran, Dixon, Ryan, Hadfield 等），*Effective context engineering for AI agents*，2025-09-29。https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents 【原文】

**[11]** LMSYS / SGLang HiCache，前缀缓存命中率实测（Claude Code traces over SWE-bench），2026。https://www.lmsys.org/blog/2026-06-27-netpreme-xmem 【转述】

**[12]** vLLM × AgentX，生产轨迹统计（2026-09-08）。https://vllm.ai/blog/2026-09-08-vllm-agentx 【转述】

**[13]** Red Hat AI Americas，vLLM v0.19.0 KV 缓存验证（字节稳定性与命中率）。https://github.com/redhat-ai-americas/memory-hub 【转述】

**[14]** Liu 等，*Lost in the Middle: How Language Models Use Long Contexts*，TACL 12:157–173，2024 【转述】

**[15]** Cognition（Walden Yan），*Don't Build Multi-Agents*，2025-06-12。https://cognition.com/blog/dont-build-multi-agents 【原文】

**[16]** Kim 等，*Towards a Science of Scaling Agent Systems*，arXiv:2512.08296，2025-12 【转述】

**[17]** Splitwise（Patel 等），ISCA 2024，arXiv:2311.18677 【转述】

**[18]** Mooncake（Moonshot AI / 清华），FAST 2025 最佳论文，arXiv:2407.00079 【转述】

**[19]** DeepSeek，*Models & Pricing*（官方定价页）。https://api-docs.deepseek.com/quick_start/pricing 【原文】

**[20]** DeepSeek，*DeepSeek-V4.1-Flash: Smarter, Faster, More Efficient*（发布公告），2026-09-10。https://api-docs.deepseek.com/news/news260910 【转述】

**[21]** Bai, Huang, Wang, Sun, Mihalcea, Brynjolfsson, Pentland, Pei，*How Do AI Agents Spend Your Money? Analyzing and Predicting Token Consumption in Agentic Coding Tasks*，arXiv:2604.22750，2026 【转述】

**[22]** Manus，*Context Engineering for AI Agents: Lessons from Building Manus*，2025-07-18。https://manus.im/blog/Context-Engineering-for-AI-Agents-Lessons-from-Building-Manus 【转述】

**[23]** Microsoft Learn / Azure OpenAI Foundry，*Prompt caching*（更新至 2026-08-11）。https://learn.microsoft.com/en-us/azure/foundry/openai/how-to/prompt-caching 【转述】

**[24]** Lindenbauer 等（JetBrains Research / TUM），*The Complexity Trap: Simple Observation Masking Is as Efficient as LLM Summarization for Agent Context Management*，NeurIPS 2025 DL4Code workshop，arXiv:2508.21433 【转述】

**[25]** Cursor（Cassano, Rush），*Training Composer for longer horizons*（自摘要），2026-03-17。https://cursor.com/blog/self-summarization 【转述】

**[26]** Cognition（Walden Yan），*Multi-Agents: What's Actually Working*，2026-04-22。https://cognition.com/blog/multi-agents-working 【原文】

**[27]** Augment Code，*AI Coding Cost Analysis: Where Token Spend Really Goes in an Agent Loop*，2026-07-24 【转述】

**[28]** *Governance Decay*：压缩静默抹除治理约束的测量，arXiv:2606.22528 【转述】

**[29]** ProjectDiscovery，*How We Cut LLM Costs by 59% With Prompt Caching*，2026-04-10。https://projectdiscovery.io/blog/how-we-cut-llm-cost-with-prompt-caching 【转述】

**[30]** Zhang 等（Stanford / SambaNova / UC Berkeley），*Agentic Context Engineering: Evolving Contexts for Self-Improving Language Models*（ACE），ICLR 2026，arXiv:2510.04618 【原文】（摘要）

**[31]** Ehrlich, Blackman，*LCM: Lossless Context Management*，arXiv:2605.04050，2026-02-14 【原文】（摘要）

**[32]** Conner，*pi-fold: Agent-Governed Lossless Context Folding*，Zenodo，2026-08-09，DOI 10.5281/zenodo.21856874 【原文】

**[33]** Packer 等，*MemGPT: Towards LLMs as Operating Systems*，arXiv:2310.08560，2023 【转述】

**[34]** Hsu, Lu，*Scoped Verification for Reliable Long-Horizon Agentic Context Evolution*（GRACE），arXiv:2607.09175，2026-07 【转述】

**[35]** *Beyond Compaction: Structured Context Eviction for Long-Horizon Agents*，arXiv:2606.11213，2026 【转述】

**[36]** Kongmen，*Pull: Lazy Materialization of Working Memory for Stateful LLM Conversations*，Zenodo，2026，DOI 10.5281/zenodo.21984705 【转述】

**[37]** Li, Li, Zhang, Mei, Bendersky（Google），*Retrieval Augmented Generation or Long-Context LLMs?*，EMNLP 2024 Industry Track 【转述】

**[38]** Laban, Hayashi, Zhou, Neville（Microsoft），*LLMs Get Lost In Multi-Turn Conversation*，ICLR 2026，arXiv:2505.06120 【转述】

**[39]** Cursor（Wilson Lin），*Scaling long-running autonomous coding*，2026-01-14 【转述】

**[40]** Anthropic（Nicholas Carlini），*Building a C compiler with a team of parallel Claudes*，2026-02-05 【转述】

**[41]** KVFlow / KVCOMM，多智能体工作流的前缀复用测量，NeurIPS 2025 【转述】

**[42]** Anthropic（Hadfield 等），*How we built our multi-agent research system*，2025-06-13。https://www.anthropic.com/engineering/multi-agent-research-system 【原文】

**[43]** akitaonrails，*llm-coding-benchmark*，手动编排对照报告。https://github.com/akitaonrails/llm-coding-benchmark 【原文】

**[44]** Google Gemini API 定价（显式上下文缓存的按小时存储计费），经第三方整理核实 【转述】

#### A3.2 主张与外部证据

| 本文主张 | 外部证据 | 关系 |
| --- | --- | --- |
| 上下文越长，判断越不可靠 | Chroma[1]：18 个模型，只变长度不变难度；同一批问题给 focused（约 300 token，只给相关部分）与 full（约 113k token）两版，focused 一致更好；且结构性连贯的 haystack 反而更差 | 支撑，是"少背就是能力"最直接的一手实验 |
| 衰减跟的是**绝对 token 量**，不是窗口百分比 | RULER[2]：有效上下文为标称窗口的 16–50%，只有约一半模型在 32K 上保住质量；BABILong[3]：推理任务上"只有效利用 10–20%" | 支撑，并**修正口径**：所谓"20–30%"是夹在两条实测带之间的经验值 |
| 背着是账单最大的一笔 | Bai et al.[21]（8 个前沿模型 × 500 个 SWE-bench-Verified 任务）："Despite output tokens being priced ~80× higher per token, **the sheer volume of accumulated context makes cheap-per-token cache reads the largest cost contributor** in aggregate"；DeepSeek 发布公告[20]："**Cache-hit charges often account for a large share of agent costs**"；Manus[22]：平均输入输出比约 100:1 | 支撑，三源独立 |
| 写比读贵得多 | Splitwise[17]：BLOOM-176B 上 1,500 个输入 token 的 prefill 与 **6 个**输出 token 的 decode 耗时相同；Anthropic 与 OpenAI 对缓存**写入**收 1.25×–2×[23] | 支撑，且**论证的落点应是硬件比**，不是价格比 |
| 高命中率是常态 | LMSYS[11] 平均约 **98%**；vLLM × AgentX[12] 每轮中位输入 **142K**、命中 >96%；Red Hat[13] 字节稳定前缀 **98.29%**；Preble[8] **85–97% 的 prompt token 与他请求共享**；ProjectDiscovery[29] 在 26 步的生产 agent 上把命中率从 7% 提到 **84%**、成本降 **59%** | 支撑，五条独立来源包住本文实测的 98.6–99.7% |
| 子代理只把果实交回主干 | Anthropic[10]："returns only a condensed, distilled summary of its work (often **1,000-2,000 tokens**)"；Claude Code[9] 示例："The subagent read **6,100** tokens of files. You got a **420**-token result"；Cursor[25] 自摘要约 **1,000** token 对基线 >5,000，且"reduces the error from compaction by **50%**" | 支撑，且与本文实测"每步写 1,043 token"同量级 |
| 边界的价值大于执行者本身 | akitaonrails[43]：同一批执行者在强规划者给定边界后，46→**93**、71→**94**、87→**97**；审计归因"执行者写了每一行，但**规划者决定了边界**" | 支撑 |
| 上下文不该累积 | AgentRewind[5]：从头重来 **78.0%** > 继续推进 62.2%；OpenHands[6] 的架构是"**append-only event log**"，递给模型的是由 `View` 派生的视图 | 支撑：状态与提示词是两件事，提示词是派生视图 |
| 回滚需要三件套 | AgentRewind[5] 消融：**只回滚上下文 43.9%——比什么都不做（62.2%）还差**；只回滚环境 51.2%；三者齐备 87.8% | 支撑本文"分支 + 环境 + 信号回流"缺一不可 |
| 机械操作能打平模型判断 | Complexity Trap[24]：观察遮罩与 LLM 摘要能力打平、省约一半成本；Beyond Compaction[35]：无 LLM 的确定性驱逐，89 任务 / 8,000 万 token 无可见退化；Pull[36]：本地确定性清理零 LLM 调用、省 75.1% token，ΔF1 不显著 | 支撑原则；**序言记录过更早的一手经验**（工具剪切中间层，实测收益极低后搁置） |
| 压缩有账外代价 | Augment[27]：每步 token −75%，但**解完所需轮数 4→14**，总量只 −14%；Governance Decay[28]：压缩后治理约束被静默抹掉，7 个模型家族违规率 **0%→30%**，个别到 59% | 支撑，是"压缩税"之外的第二种压缩代价 |
| 长会话里"累积"本身在退化 | Laban et al.[38]：多轮对话相比单轮平均掉 **39%**，且"wrong turn 之后不会自己走回来" | 支撑重建与回滚 |
| 边界的价值独立于上下文长度 | Cognition[26]：代码审查代理在**与写作者完全不共享上下文**时效果最好，归因是 attention 的数学 | 支撑上下文隔离 |

#### A3.3 已经发生过的坑，与本文对应的设计区分点

**这一栏不是反驳清单，而是"已经有系统在这里摔倒过"，以及本文在哪一步做了不同的选择。**

| 已经发生过的坑 | 谁遇到的 | 本文对应的区分点 |
| --- | --- | --- |
| 子代理有自己的系统提示与工具集，**打不到父会话的缓存**；只有精确继承的 fork 才行 | Claude Code 官方文档[7][9] | 种子必须是**共享前缀的严格延伸**：公共部分逐字节同一，分支差异只能落在前缀之后（正文 129） |
| 缓存按机器与目录分家，**同一仓库的不同 worktree 互相打不到** | Claude Code 官方文档[7] | 环境隔离与前缀解耦：**环境标识绝不进前缀** |
| 在一个命中 98.29% 的前缀后追加 **41 个字符**，命中率塌到 **1.78%**，官方口径是"Effectively a complete miss" | Red Hat[13] | 完全前缀组装 + 段序稳定（正文 129；附录1 A1.7 的 ΔP） |
| **工具列表的顺序也是缓存身份**，MCP 列表一变动命中率 71%→33% | 社区实测（permafrost 项目，小样本） | 前缀内不放任何会漂移的字节 |
| 并发写者互相覆盖；20 个并行 agent 塌缩成 2–3 个的吞吐，最后把 integrator 角色删掉 | Anthropic C 编译器[40]、Cursor[39] | 各写各的虚拟文件 + 事前契约 + **单写者合并**（正文 105/143） |
| 独立代理把错误放大 **17.2 倍**，集中式只有 4.4 倍；顺序推理任务上多代理退化 39–70% | Kim et al.[16] | 本文不是"独立代理投票"：主 Agent 定契约与断言、单写者合并——**正落在该文的正结果一侧（集中式协调）** |
| 多智能体比对话贵 **15 倍**[42]，agent teams 约 **7 倍**[9] | Anthropic 自己的数字 | 那 15× 来自**每个子代理各背一份上下文**；共享前缀 + 按需取用打的正是这一点 |
| 让模型重写自己上一轮写的正文，会"context collapse"、越写越薄 | ACE[30]；GRACE[34] 的对照里，结构化、限定范围的更新 pass^3 = **0.673**，而平铺式整体重写只有 **0.191** | 本文不重写正文：Harness 按坐标**重装原文**——内容上是增量、装配上是重取 |
| 压缩会**主动作废**整层缓存（官方口径："invalidates the conversation layer"） | Claude Code 官方文档[7] | 重建 ≠ 重排：段序稳定时前缀照旧命中 |
| 预算充足时，长上下文一致优于检索增强 | Li et al.[37] | 与 Chroma[1] 的 focused/full 结论方向相反，两文条件不同、互相抵消；本文的主张不是"小上下文更强"，而是"**在必须站低水位的前提下不必因此丢内容**" |

#### A3.4 至今没有人做过的对照

**这比上一栏更要紧：它划出引用管不到、只能靠实验回答的部分。**

**一、把"机械 vs 模型"控制住之后，再比"累积 vs 重建"。** 现有文献里，机械清除[24]与重建架构从未在同一条件下同台。对照应当是四臂，**且 B 与 C′ 必须使用同一种现成的机械手段**，否则又是拿"有"比"没有"：

| 臂 | 内容 | 它单独回答什么 |
| --- | --- | --- |
| A | 累积 + LLM 压缩 | 现行默认 |
| B | 累积 + **厂商级现成的机械清除** | 机械能否打平模型判断 |
| C | 每轮从环境重建 + 共享前缀 | |
| **C′** | 每轮从环境重建 + 共享前缀 **+ 同一种机械清除** | 本文的完整形态 |

**A 对 B 切出来的是本文与别人共享的那部分原则；B 对 C′ 切出来的才是本文自己的增量**——这两臂机械手段完全相同，唯一剩下的变量是"上下文是累积的还是每轮重建的"。

**二、"压缩税"没有任何直接测量。** 三个方向找下来，没有任何来源把"模型写摘要"这一笔单独计成账；最接近的净额数据是 Augment 的"每步 −75%、总量 −14%"[27]。所以本文把它算成一张账单，**是本文自己的贡献**，引用只能提供方向性支撑，不能提供数值背书。

**三、"前缀更准 ⇒ 更少工具调用"这条因果没有任何论文测过。** 附录1 A1.6 第 5 条把它写成设计意图是对的；但它至今是**待测**项。

**四、"N² 写冲突"是本文的用词。** 冲突现象被反复观察到（A3.3），但没有文献这样命名或量化它。

#### A3.5 口径与适用范围（对附录1 的补充说明）

| 项 | 附录1 的说法 | 适用范围 |
| --- | --- | --- |
| 三档价格 ¥0.02 / ¥1 / ¥4[19] | 按 DeepSeek 计价 | 这是 **DeepSeek-V4.1-Flash 空闲时段**的官方价；**高峰为 2 倍**；取低谷价是保守取法 |
| 未命中是命中的 50 倍、输出是命中的 200 倍 | 两个比值决定整笔账的形状 | 该比值**只对 Flash 成立**：Pro 是 30×/90×；Anthropic 与 OpenAI 的缓存读为输入价的 0.1×（10×）[23]；Bai et al. 的价格表约为 80×[21]。**不能讲成 prompt caching 的普遍性质** |
| 背着 38.5% / 首读 21.7% / 写 39.8% | 三条实测会话的账单构成 | 这是在 **DeepSeek 价目 + 长会话稳定前缀**这一工作量上测得的。换到 Anthropic 文档里的 agent 会话，按其用量重算约为**背着 51.0% / 缓存写入 33.9% / 输出 14.4%**[23]——**换价目只会让"背着"占比更高**。所以它是工作量形状，不是 agent 的普遍形状 |
| "背着的便宜被体积抵消" | 背着单价极低但数量巨大 | 更准确：**价格上便宜，资源上不便宜**。按公开的 KV 尺寸，1M token 的缓存在 Llama-3-8B 一类模型上约 122 GB；而 DeepSeek 的命中价约 $0.0028/M[19]。命中价是**算力价（省掉的 prefill）**，不是存储价。Google 对显式缓存**按小时收存储费**[44]，这一项已经在被计价 |
| 一次重置的未命中"只付一次，不按分支数付" | 本架构重置成本的构成 | 前提比原句更严：缓存要求**完整前缀单元匹配**且为 best-effort；分叉点必须落在同一个已存单元边界上。这不是风险提示，而是**设计纪律**——正文 129 的"段序稳定"与"公共部分逐字节相同"正是为它写的 |
| 三条会话遥测（9,023 步 / 203,660 token 每步 / 98.6–99.7% 命中） | 参数全部来自实测 | 这是**本文自己的一手遥测**，外部无法复核。其中 203,660 token/步 属于"长会话 + 工具密集 + 含子代理"的区间（vLLM × AgentX 的生产中位是每轮输入 142K[12]），**不是编码 agent 的普遍水平**；98.6–99.7% 的命中率则有四条独立来源包住（A3.2） |
| "人在 30% 上下停手" | 九次人工压缩的中位 30.9% | 这是**本文自己的观察**，不是行业规范；公开默认值要激进得多（Claude Code 的自动压缩在窗口的 83.5% 上下触发）[7] |
| 无损可回取 | 分区优于压缩 | 这一层**已有先行工作**：LCM[31] 的"保留指向每一份原文的无损指针"、pi-fold[32] 的哈希寻址与逐字节取回、MemGPT[33] 的虚拟上下文分页。本文的区别不在"无损"本身，而在**信源**：它们的指针指回**对话历史**，本文的坐标指回**环境** |

#### A3.6 未采信与不可用清单

写下这一栏，是为了让读者知道本文的证据边界在哪里。

- **不引用**：CompactionRL（arXiv:2607.05378）与 Recursive Language Models（arXiv:2512.24601）——只有标题或转述，正文未能核到。
- **不引二手数字**：ACE 的"18,282 → 122 token"collapse 数字来自第三方笔记而非论文正文，本文只引其摘要原话[30]。
- **美元数字不可重算的不引美元**：Bun 的 Zig→Rust 迁移报告约 $165,000，无法用任何公开价目重算到该数，只在 A3.5 的口径讨论中取其 token 形状。
- **降级使用**：Anthropic 与 OpenAI 的倍率来自 Microsoft Learn（对 OpenAI 机制属一手）与镜像仓库，不是厂商实时页面；MAST 的分类百分比、RULER/BABILong/NoLiMa 的具体数字、Kim et al. 与 AgentRewind 的消融百分比，均为【转述】。
- **同行评审比例**：本节已引用并经同行评审的有 Lost in the Middle（TACL 2024）[14]、RULER（COLM 2024）[2]、Complexity Trap（NeurIPS 2025 workshop）[24]、KVFlow/KVCOMM（NeurIPS 2025）[41]、Li et al.（EMNLP 2024）[37]、Laban et al.（ICLR 2026）[38]、Prompt Cache（MLSys 2024）、SGLang（NeurIPS 2024）、Preble（ICLR 2025）[8]、Mooncake（FAST 2025）[18]、Generative Agents（UIST 2023）、Reflexion（NeurIPS 2023）、Splitwise（ISCA 2024）[17]、ACE（ICLR 2026）[30]。**其余为预印本、厂商文档或行业博客**，本节不把它们当作已确立的结论使用。

#### A3.7 一句话

> **本文的价格模型与"背着是账单最大的一笔"有官方口径、独立研究与厂商原话三重支撑；"无损可回取"这一层已有先行工作，本文的区别在信源是环境而不是对话历史；而本文真正的成本主张——压缩税——至今没有被任何人直接测量过，所以它是本文自己的账，不是引文的结论。把"机械 vs 模型"与"累积 vs 重建"分开的那张四臂对照，也还没有人做过。**














