# Agent重架构：迈向上下文原生智能（Context-Native Agent）



*作者：余烬*



### 序言：余烬在剪枝



上个月，Deepseek核弹连发，不管是掀起flash革命的Deepseek V4 Flash-0731，还是神鬼二相性的Deepseek V4 Pro-0813，至少都在大家伙心里留下了深刻的印象。更不要说面向AGI设计，万物皆插件的野心勃勃的Deepseek Harness了。对我这个🐋小子来说，惊喜来的实在是太突然。我立刻动手体验了这个有趣的Harness框架，并测试了它的创造模式（虽然不出所料地把自己搞崩溃了）。然而噩耗来的总是那般突然，就在我兴奋地为我的个人小文件整理开源项目添砖加瓦的时候，梁文峰带着他的涨价来了。眼见着自己的费用蹭蹭上涨，上下文缓存那些钱也不由得让我心疼起来。可怜的余烬那时候还完全没有意识到，其实雷霆大思考才是上下文那么长的主因。于是我萌生了这样的想法：”要不我自己动手做个插件，把或成熟，或创新的上下文压缩机制缝进去，顺便美美收Github星星，投递一点大模型公司简历“。然而直到动手之前，我都还没意识到自己即将面临怎么样的”零花钱地狱“。



上下文管理是个被研究的十分透彻的领域，在通过免费快速的Deepseek网页快速模式Deeeepseeeek了一段时间后，我就笃定了自己已经对于Harness的上下文管理机制有了充分的了解。好！动手！结果自然是大败而归。



在dsh-price-less插件设计的初版，我进行了几个重大构想：**RAG/代码树为底层的语义化文件读取**，**基于意图的任务边界识别**，**任务边界与压力阈值自动压缩**，**提示词优化器**。



具体来说，任务，也就是Task，并非特指某种类型的任务，而是针对代码的某个结构/功能，具有文件读取特征的修改。根据这一定义，我们在边界区间压缩上下文能够最大程度避免上下文失效，降低重读率，同时拒绝为原本用不到那么多次，但是一直存在于缓存中的内容，大量重复计费哪怕很廉价的缓存计价。



而任务边界唯一的判定标准，就是用户输入。每轮任务内，全量的用户输入都会进入到”任务判别器“的上下文，再被组装上”项目整体目标”作为稳定前缀，确保任务边界判别的成本极低，压缩自动化且准确。而提示词优化器则更是巧妙复用，通过task前上下文，当前用户输入，与安装在Harness中的Skills列表按需取用，做到将用户的提示词扩写锚定，并且对于一些Skill能覆盖的操作给出最优解决方案，避免试错。RAG/代码树则是语义化，细粒度地读取文件，减少盲目全读和反复重读。



设想很美好，但是限时很骨感。因为实习要求在即，我不得不把RAG这种复杂内容搁置，移动到路线图中，全力开发边界识别。在这期间我经历了多次失败。包括机械判定完全不准确，embedding模型区分度极低，参数调优倾向于刀刀切，作弊获取奖励，评价标准难以综合多个指标。更不要说我作为夜猫子多次忘记梁文峰，足足烧掉了几十块的零花钱，最终才形成了上面那套能和提示词优化器复用缓存的成熟方案。



过程中还诞生了很多新的想法，包括工具剪切中间层等。通过对工具结果的保守通配，只保留有用信息，如编译器报错。但是在实测之后收益极低，最终被搁置。还有结论压缩，基于和工具剪切的类似理念，将中间结论直接代替“思考-工具调用-再思考-结论”，嵌入上下文。但是实际上，提示词注入后，大模型很难按照要求给出剪切信号，机械剪切风险和收益不成正比，也被弃用了。还有压缩产物的结构，也让我殚精竭虑。通过任务和类型化子任务高度浓缩之前任务信息固然不错，但是模型申报原文作为热尾天天坐标不准组装出错也非常令我恼火。事实上，经过一些实际使用，有限的原文长度对于模型的复读阻止作用十分有限，现在已经在本地改成申报坐标了，原文什么的，自己多读几轮，至少还更精确吧。



庸庸碌碌忙活这么久，最大的成功还是边界压缩，给我气笑了。索性还是有点安慰，至少Kimi可能为真的面试题，竟然句句踩在我失败的embedding和机械路径上，总算是长舒了一口气。不对啊？人家零花钱比我多多了，要是真试出来更好的，那还是我亏麻了，气气气！



但是这样磕磕绊绊的探索，也让我产生了一个足够疯狂的想法。已知上下文剪枝非常困难，已知上下文积累速度极高，主要来自于模型调用读取的文件慢慢积累，慢慢因为长度和中段被遗忘，然后再次读取...



我们处在一个上下文不够用的环境下太久了，以至于我们一直在思考如何做减法...那有没有可能，上下文是应该自己长出来的呢？



### 正文：欢迎来到上下文原生智能（Context-Native Agent）的世界



所以，当我想到这一点的时候，一切就豁然开朗了。在过去，上下文极度紧缩，完全不够用的时代，我们花费了不知多少功夫，试图将海量内容塞在上下文里，通过各种索引RAG的方式确保模型能够瞻前顾后，将一切纳入考量，但是随着Deepseek家模型拉开百万上下文的普惠年代，50%上下文就开始手动压缩成为了常态（目前平衡信息量与模型能力衰减的甜点级上下文长度在20-30%，不严谨，但是大概为社区主流观点），那么我们是否也该反思自己：“上下文压缩与剪切，是否还是正确的上下文处理方式呢？”



究其本源，我想我们要回归一个最基本的问题上：什么是Agent？在目前的社区主流与共识的说法中：“Agent = Harness + Model”。实际上Deepseek甚至将这个理念写入到了他们的招聘要求中。在他们的实践中，甚至也出现了模型与Harness框架耦合才能解锁最高能力的现象。我对这一点其实并不持反对态度。虽然说在客观上这限制了用户API调用的选择范围，但是事实上这就好比人使用自己的手达成通用能力一样，这样的耦合也许有助于达成更高的能力上限。不过，当然更大的原因是Deepseek Harness是一款自由，开源的框架，并非是为了商业目的的强迫。



回到问题本身，Harness本身并不是什么简单的东西，尤其是学习Claude成熟经验后诞生的各种千奇百怪Harness框架。有的内置各种RAG，Skills，MCP，Tools，以Agent Loop为核心。将Harness视为Agent的组成部分诚然合理，但是这个框架太大，太重，让我们不知道如何去管理。当我们预设了每一分上下文信息的必要性，那必然就要面临顺序，取舍与重读，还有困难的剪枝。历史和实践证明，我们总是习惯性地通过补丁来解决，引入更多的机制，更多的压缩，更多的坐标。可是我在这里想要问下一句，有没有可能，上下文是从一颗纯净的前缀“种子”中生长出来的？



当我们去审视大语言模型的本质，在它的视角里，并不存在什么所谓“Harness”，甚至就连思考过程都是依靠训练对齐的，它只是一遍遍地处理着语言输入的黑盒函数。决定它输出状态的只有上下文与参数，所有的工具调用，思考过程都是训练对齐的结果。那我想，我们应该拒绝以用户交互的对象为Agent主体的认知错位，事实上：**“Agent = Environment + Context + Model”** 。



这句话反映的是两个层级：模型的状态只取决于参数与上下文；Agent的状态取决于环境 + 上下文 + 模型。



那么这种理解方式能给我们带来什么启示？

- **推论1：** 在一个环境受控的Harness框架下，保存一个Agent所需的运行状态无需任何与当前Context无关内容，成本远低于“Agent = Harness + Model”的假设。

- **推论2：** 在同一个 Harness 框架下、且环境一致时，给定上下文就能立刻重建一个 Agent 状态。

- **推论3（普适）**：上下文不该是 Loop Then Compress，而是按轮 **再生成 → 切片 → 合并 → 重载**（Regenerated, Sliced, Combined, Reloaded）。 这一条不依赖环境：对纯对话系统（无文件系统、无进程、无副作用），它同样成立——上下文照样要重建、切分、合并、重载，只是没有需要维护的环境实体。

- **推论3′（Agent 特化）**：当系统存在环境实体（代码、依赖、进程、端口）时，上述四步落到具体对象上即为——**环境维持共享、代码 fork、上下文 build**。
  
  

最终，以上述的理念为核心，我们可以推导出下一代Agent范式，与其较为具体的工程实现。我称其为上下文原生智能（Context-Native Agent）。在这一架构下Harness的职责从管理Model转向管理Context与管理一个Agent切片运行所需的环境。Context与Harness复杂框架解耦，作为与模型唯一的交互通道被解耦出来，成为Harness的一等公民。



以这一理念出发，一种上下文原生智能的具体实现可以以一种AI native “Git tree” 为核心，其分为主Agent与子Agent，具有模型一致性。



对于任意一个项目，主Agent与子Agent拥有一个共享缓存前缀，其为当前系统版本下，必要的代码与意图映射汇总，与必要的系统状态。当一个任务被委派时，主Agent负责与用户对齐需求，并对任务进行拆分，任务的拆分的具体原则以降低耦合度与提高子任务目标的内聚性为主，且对任务的规模进行控制，确保其上下文占用对齐模型能力甜点区间。



然后，其将为每个子Agent创建一个分支，具体而言：maintain environment, fork code, then build context as a seed。每个子Agent的工作会作用于分支的虚拟文件上，以提交记录的形式落盘。



任务解耦完毕后，主agent将会运用其对于代码整体的理解，为每个子Agent委派上下文中预置的文件内容，然后由Harness进行最大前缀组装。且主Agent会制定断言与测试作为合并前必要验收标准。



任务过程中，子Agent的动作可能或主动或被动提交Signal，供主Agent进行环境观察与调度。也就是说，这一具体设计类似于一种响应式Agent调度框架。



全部子Agent回归后，由主Agent/委派Agent进行代码合并，取长补短，并进行最终测试与汇报，并同时会对系统状态，代码映射等进行更新/重写，这个新的前缀将会成为下一次执行时候的公共前缀，其作为全局理解对于之后的主Agent任务委派与子Agent的全局观起到指导作用，是衔尾蛇循环重启自己生命历程的步骤。



综上为我为上下文原生智能（Context-Native Agent）设计的一种Git Base的基础架构。其具有理解/执行模型解耦，过程透明可理解，并行效率高，各个Agent可复用现有单流程Agent上下文管理成熟经验，前缀缓存利用率高，且规避了上下文长度瓶颈，更是从信息熵/上下文长度两方面确保了模型能力发挥稳定。



具体来说其相对主流，定性可能具有的优势：

- 在上下文层面：每个子Agent只会被给定极其干净的 [ Environmental View ] + [ Project Structure and Intention Mapping ] + [ Raw Files Assigned ]，作为其生长出果实的“种子”。这样干净的上下文有助于模型立刻进行工作，减少读取的工具调用次数和探索信息的不可控，但因为只是种子，也保留了发挥空间与一定的不可控风险。在具体的工程实现上，如果Raw Files Assigned段太长，则考虑仅先放入公共前缀部分，后续内容通过一个特定工具按需取用。

- 在缓存利用层面：每个子Agent共享大量共同头，甚至Raw Files之前的内容可以与主Agent共享缓存前缀，等价于将所有只需要探索一次的内容的复利控制在了每个子Agent分区中，上下文累计只以子Agent结出的果实的形式回归主干，被吸收利用。而根据Git与软件工程的成熟经验，一次合并就意味着项目意图/功能结构的变更，此时主Agent上下文也是时候进行更新与重启了，最终做到每个目标实现时，上下文长度几乎不触及压缩阈值，每次变更只进行一次干净彻底的探索，不带着压缩后信息密度高却丢失细节的信息包袱。

- 在透明度层面：主次分明，探索思考，需求对齐与实际落地解耦，交互均有signal可查，代码，架构与功能变更清晰可确认。

- 在模型适配性层面：可对齐每个模型所需求的最优上下文形状。

- 在模型能力层面：主要收益来自于信息熵的降低与上下文长度可控，中段迷失和能力下降被有效控制。



其有待实验验证的点在于任务规模与解耦设计对于成本与能力的具体影响。但是，随着Goal模式等交互模式的兴起，任务的复杂度在上升，且逐渐变得更加高层，与实现无关。因此，传统的上下文累计思路必然会受到对齐需求与探索时期复利的反噬，哪怕是压缩之后也一样。



而对于我们这套Git架构，越是随着任务的规模增大与目的的实现无关，就越可以被细化为边界清晰、数量可控、架构解耦的落地分支，并由主Agent一次生成一份稳定的、共享的、能回答"这个功能会落在哪些结构上"的项目意图映射与清晰边界的架构契约。最终规避并行写者N²的冲突，因为那是在任务高度耦合的具体实现目标下的不理想情况。当然，具体方面的优化打磨，不易解耦的任务，我们的架构能以付出轻微额外成本为代价，即可轻松回退到主Agent/少分支结构上。



我相信build上下文会是上限更高，更符合未来需求与趋势的更优选择。Git管理只是我结合软件工程经验给定的一个具体实现，只要思想存在，就一定会涌现出更多优秀设计。而当前Git架构下去，困难点在于虚拟文件的编译与测试、环境稳定性维护。



而在更加遥远的未来，当大模型的输出速度成为瓶颈的时候，缓存架构的优秀是否也意味着，首Token和平均Token都能大大提高？具有实际生产价值的模型规模上限得到扩充？不过这些都是当前架构成为主流顺带来的好处罢了，总之，它意在扭转还没有回头的迟滞当下，是面向未来以工程复杂度换取落地价值的优秀架构。现在的大模型架构革新，以收敛难度与炸炉风险换取能力上限，而Harness架构的革新成本与风险，可是要小得多，难道不值得探索吗？



简而言之，Context成为一等公民的真正价值，在于文本管理，项目管理，操作系统与软件工程的成熟经验，均可以套用在管理Agent状态的切片上，实现高度透明，机械化，有保障的Agent协作，助力大模型在前AGI时代发挥能力上限。



### 附录1：上下文原生智能 vs 主流Agent框架 实验设计（待执行）



基础harness框架采用Deepseek Harness Git bash包装的极简模式，验证项目为我自己的开源项目RootUp，目标为更新0.8.8版本。



对照组：直接要求完成该版本，Agent一般会自行制定计划，故不采用显式计划。

实验组：主Agent进行审查和状态记录，然后拆分任务，选择文件作为子Agent上下文，由我手动录入公共前缀+最大上下文序列+具体子任务进行委派。子Agent在多个复制的项目文件夹下分别工作。主模型只允许读取Git记录并进行合并。



最终结果使用大模型进行代码对比验收，并为我描述代码宏观行为，综合对比评判结果分数，最终结合总成本进行综合比较与验收。



该实验设计较大程度对现存框架更优，因为我手动组装委派只是模拟，缺少了很多架构优势，如果仍然更优说明该架构具有极大潜力。



### 附录2：Harness耦合现象的思考 与 Context作为一等公民的必要性



Deepseek V4 Pro-0813在实际发布后饱受诟病，其被质疑为刷分模型，在除了极简模式 + 类linux之外环境下，均难以产生较好的结果。在上文中，我也曾经提到了关于Harness作为模型原生手脚的比喻，耦合以发挥模型能力上限。



而当我们放下个人视角，从模型训练的角度，Tools是否有泛化的必要与泛化的可能？



在我看来，格式化输出在训练上的对齐是十分必要的，但是模型在Tools的泛化上完全没有必要进行特殊训练。对Tools格式的理解完全可以被通用工具，代码调用覆盖，将参数与信息量浪费在对齐可能出现的不同自定义Tools上面是因噎废食。我们Harness构造的最终目的是能力上的通用，而非不同框架下的普适，就像我们不怎么需要去训练模型写什么Visual Basic代码一样。



而在数据构造的角度来说，我们给模型输入的构造数据，其涵盖的Tool类型也必然是给定有限，优先确定，通用的优秀组合。如Edit，Bash等。其主要目的就是聚焦Agent完成任务过程中，充当眼与手。如果要增加模型对于多样工具调用的适应性，为此降低其对通用普适，且已经足够达成通用能力的工具的调用能力，显然是得不偿失的。



那么，我们所需要面临的一点问题就是Harness框架作为产品的独立性与市场性又该如何保证？每一家的模型在自家Agent上都会默认胜出，那如何通过优秀的Harness设计抢占市场，制定标准，把握用户数据入口？



答案就在于Context作为与模型直接交互的中间层。现在的通用Harness在API层实现对模型的解耦，因此其不能对齐模型的训练数据与偏好环境。真正行之有效的适配方式，是在上下文组装环境为每个模型进行分野。不同的工具调用对于模型的形状各不相同，对应着模型的原生“肢体”。这样的一套通用Agent架构，才能实现对于同一模型，同一发挥下的“可比性”。
















