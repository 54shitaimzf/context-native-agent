# 口径与理解（可复用）

> 这份文件是给"下一位读者、下一个 agent"的：把这篇长文的主张、机制、成本口径，以及**最容易读错的地方**一次讲清。读完它再读正文，可以少走一遍弯路。
>
> 它不是正文，也不替代正文。**口径冲突时以正文与附录为准**；发现冲突请提 issue。
> 落盘时间：v1.7。维护原则：只写结论与边界，不重复正文的论证过程。

## 0. 一句话

Context 从 Harness 里解耦出来、成为一等公民；"内容离开上下文＝失去内容"这个约束被取消；于是压缩不再是唯一出路——而文中的 Git 树，只是这个新解空间里的**一个点**。

## 1. 三层称呼（先分清，否则后面全对不上）

| 层 | 指什么 |
| --- | --- |
| **范式层** | 上下文原生：Context 与 Harness 解耦、成为一等公民 |
| **实现层** | Git 树：本文给出的那一个具体实现（主 Agent + 子 Agent + 分支 + 合并） |
| **账目层** | 附录2 里的数字：**实现层**这一处的账 |

全文"本架构/这套做法"必须能回答指哪一层。

## 2. 命题

- `Agent = Environment + Context + Model`（不是 `Harness + Model`）。模型的状态只取决于参数与上下文；Agent 的状态取决于环境、上下文与模型。
- **被取消的约束**：内容离开上下文 ≠ 失去内容。旧框架里上下文是缓冲区（离开＝丢失），新框架里上下文是**投影**（离开＝此刻不需要）。
- **但"投影"只覆盖一半**：**环境里已有的**东西（代码、状态、依赖）才是投影，离开随时能重新取出；**环境里没有的**东西（"这个功能落在哪些结构上"这份理解）离开就得重新读懂一遍，所以它不离开——读懂之后写下来，作为前缀被每一步廉价复用，只在合并时更新一次，靠写一次摊薄到每一轮。**前缀里住着这两种东西**，这是全文最容易读漏的一处。
- 上下文不是攒出来的，是**每一轮从 0 构建**出来的；"不在上下文里"是默认态，"在里面"才需要理由。
- 再生成 → 切片 → 合并 → 重载；落到环境实体上：**环境维持共享、代码 fork、上下文 build**。

## 3. 机制（最容易被读错的一段）

**前缀 P（稳定公共段）是什么**：跨分支、跨轮次都用且内容稳定的那一段——人设与工具清单、项目结构说明、意图映射（这个功能落在哪些结构上）、必要的系统状态。**入选判据只有一条：它值不值得此后每一步都被读一次。**（里面住的两种东西——取出来的与写下来的——差别见 §2。）

**"协议"指什么**：每个模型自己的**上下文与工具拟合形状**——解耦 Context 之后得到的那层"模型上下文耦合兼容层"（附录1 的落点）。它不是任务契约（契约是任务边界上的约定），也不是环境内容。正文里"环境 + 协议"的"协议"就是这个意思。

**谁产出、谁装配**（这一条最容易搞反）：
- 前缀内容**由环境与模型产出**：模型重写**需要模型更新的那一段**（意图映射等），其余各段由 Harness 从环境取出；
- **装配由 Harness 做**，而且是**分段装配**、排布稳定——这是字节级稳定的关键。
- 所以"模型重写"与"Harness 装配"不是二选一，是**分工**：谁该更新谁更新，装配与排布由 Harness 统一负责。

**任务边界与两个尺度的重启（这一处我原来读错）**：**重启点就是任务边界**，也就是用户那句"我要加个功能""我要完成某个版本"。一个目标可以太大，于是被拆成**多轮「分支并行 + 归并」**结构——这是主 Agent 的活。于是重启有两个尺度：

- **目标级**：用户给出一个新目标＝一次任务边界，主 Agent 的上下文从头重建；
- **轮级**：目标的实现途径就是**归并**；一轮末归并完成，就是"对代码库、功能与意图状态的一次大更新"，前缀随之换代，主 Agent 上下文再一次重启。

**"不必等全部完成再归并"说的是不必等整个目标完成**——一轮一收，轮边界由信号推动（见下）。**一轮之内不换前缀版本**，附录2 的缓存账（未命中按"前缀版本数"计、不按分支数计）就架在这上面。

**更新时机**：一次合并＝一次大变更。合并时主 Agent 查 git 记录、合并、同时更新前缀内容；新前缀稳定到下一次大变更，并作为下一轮的公共前缀。

**并发是响应式的，不是批处理**（作者原话）：**用户与子 Agent 都是输入信号**，主 Agent 的契约是一份**声明**，系统像前端框架的响应式＋声明式 UI 那样重新求值——用户中途改口也是一次信号，轮边界就是被这些信号推动的。

**"最大前缀组装"的"最大"是两件事**：一是**共享字节最大**、尽量前置；二是**保住文件本身的结构性**——装出来的形状要对齐模型训练时反复见到的 tools／文件格式。（与附录1 的"为每个模型分野"是同一件事的两面。）

**段序**：易变段（意图映射、系统状态）固定在前缀**尾部**，稳定段在前——"变动的只落在尾部、没变的部分逐字节相同"才成立。**正文不展开这一条**（展开要讲工程复杂度，也不是文章重点），但它是账成立的前提。

**环境怎么共享**：底层内容只有一份，每个分支拿到**自己的一份虚拟工作区**（相当于把工作区复制多份分下去，以内存换效率），**只有真正落地修改时才占空间**。难点也正在这里：**虚拟文件的编译与测试**。

**没有"回读"这个禁忌**：读一份此前没进过上下文的内容，就是"第一次被构建"（付一次未命中）；曾经装过、现在不在里面的内容要用时再读一次，走同一档。默认态是"不在里面"。

**两层各自的粒度（我在这里读错过一次）**：主 Agent 面对用户、意图不固定，**边界之内是真正按需存取**；但**它的重启点全都是给定的**（目标级＝任务边界，轮级＝归并，见上），到点重建一次。子 Agent 的任务在分派时就已确定、可预测 → 上下文可以**预先构建成种子**。所以"构建可被规划"**在两侧都成立**，差别只在粒度与触发点。把它说成"只适用于子 Agent 那一侧"是错的。

**低水位从哪来**：全量理解在前、拆分在后。理解被编译进前缀（短、稳、每一步都在），干活只装自己那一片，缺什么按需读一次。**低水位不是设计出来的，是拆分出来的；而拆分的前提是先有一次全量理解。**

**底层压缩还在，只是不去碰它**：Harness 的原生压缩是底层能力，本架构**不关闭它、只是不触发**（"上下文长度几乎不触及压缩阈值"是设计目标，不是保证）。真撞上的极端处置是**收敛为成熟的线性结构兜底**（暂定方案），而只要拆分粒度合理，就不该出现这种情况。要强调：**哪怕占用高了也不压缩，只重启**——重建一份干净的上下文。

**模型与角色**：

- **模型一致性不是要求，是更优的情形**：主 Agent 与子 Agent 用**同一个模型**时，同一份缓存前缀能在两边命中，成本结构在**同模型**下最好；**不同模型也不影响结构成立**——分叉、契约、按需读回、合并都不依赖它，各自模型内部那份缓存照旧逐轮命中，丢掉的只是"主与子共用同一份前缀"这一层折扣。对**单模型串行**、对**"贵模型规划、便宜模型执行"**，这套结构都更优（后者是定性比较，没有进账）。
- **"理解／执行解耦"解耦的是职责，不是模型实例**，所以它与"模型一致性"不冲突。
- 不变量：**对齐理解必须在主 Agent，具体落地必须委派——哪怕不分支也要委派。**
- **线性结构是分支循环重启结构的特例**（退化情形，不是降级）：不易解耦时分支变少、缓存共享的摊薄变差，这就是"轻微额外成本"的落点；退到单分支，基本等同于计划-落地模式。

## 4. 成本口径

- **四个台阶**：写 ¥4/M（＝背着走 200 步）、第一次构建 ¥1/M（＝背 50 步）、此后每步被读 ¥0.02/M（＝背 1 步）、**根本没构建 0**。
- **一次 2,068 步任务**：实测基线 ¥7.02 / 完全不管控 ¥16.97 / 本架构 ¥5.70（省 19% / 66%）。
- **三条判据**：每轮写出的**更新量** $M$ ≤ 2.8k；段序稳定到没变的那半段一直命中；**前缀＋切片** < 63k——**第三条的 63k 以前两条成立为前提**（更新量回到现实的 2k、前缀按命中计价）；本节保守取的那一档只有 34k，那一档赢不下基线的纸面最优点。基线的重发现 $\rho^{*}$ = 160–194 token/步。
- **四条例外**：$N=4$、$\theta_d=20\%$、$\Delta P=P$、$k_b$ 取公式值 202（实测 230）。
- **账属于实现层**：换一处实现、换一组参数，数字会变，而四个台阶、三条判据、有效残留的**形状不变**。

## 5. 为什么比现状（典型 harness loop）优

1. **钱花在哪**：现状的钱花在"删得准不准"（必须做判断，判断错了不可挽回）；我们的钱花在"装得对不对"（可核对，错只是少省一部分）。
2. **成本形状**：现状每步背着的量从残留涨到触发点，重置次数随任务长度线性，重发现按输出价计费且**不封顶**；我们残留是常数，重置频率是基线的 $1/N$。
3. **信息**：压缩是**删除**（不可补偿），重建是**不构建**（无损）。我们换的是上下文的**来源**：从"上一步的输出历史"换成"环境的当前状态"。
4. **缓存**：现状里压缩会主动作废整层缓存，追加 41 个字符就 98.29%→1.78%；我们这边"没变的部分照旧命中"是**纪律**，由 Harness 分段装配保证，不交给模型自觉。
5. **并行**：现状多智能体的 15× 来自**每个子代理各背一份上下文**；我们的分支共享同一段字节的前缀（严格延伸），未命中按"前缀版本数"付，不按分支数付；合并是单写者，避开 $N^2$ 冲突；真出现冲突时由**主分支**依它制定契约时的那份上下文审查、择优采纳——解耦了探索与落地、又事先有契约，巨大冲突是小概率事件。
6. **能力**：**更短、更干净、更聚焦的上下文本身就是能力上的更强**——不是"少受长上下文拖累"，而是同一个模型发挥得更好。
7. **水位**：**现状站不到低水位**——它只有一层上下文，理解与干活挤在一起；串行又不拆分，它就不可能不带着足量的信息工作。压到它自己的钱最优（8%）就等于让它别理解、只管干，那是纸面点。所以"两边都按在 31% 时贵 12%"只是**算法核对**，不是让步。
8. **失效模式**：现状越用越坏且**静默**（不管控就滑向 80%；摘要抹掉治理约束、违规率 0%→30%，账上不留痕）；我们的暴露面能写成一条线（$P$ 会不会涨过 63k）加一个已知缺口。
9. **解空间**：现状的解法都被逼到"在信息量与长度之间取舍"这一条路上，那是补丁的极限；解耦之后可设计的维度才出现：**载体、装配粒度、更新触发、共享形状、重置频率**。所以主张不是"这套实现更好用"，而是"这个空间此前不在候选里"。

## 6. 边界与缺口（引用时别越界）

- "两边都按在 31% 时贵 12%"是算法核对，不是任何一方会选的位置。
- 实测 $N=1$；$N=4$ 是"设计被正确使用"的假设，不是保守值。
- $\theta_d=20\%$ 是**能力假设**（"人停手的位置中位 30.9%"是实测，20% 是我们主张能达到的水位），要靠 A2.6 证明。
- 必须**同时**承认"只写变动的那一段"与"段序稳定"，账才成立；把前缀整段按未命中、又把更新量取 10k，$R_d$ 压不到 $R_b$ 之下。
- **意图映射的完整性没有机械校验**：能校验"装回来的字节与原文一致"，不能校验"该写进映射的有没有漏"；映射还是**版本相关**的（一次合并就会让结构整体位移）。这是设计缺口，不是保守取法。
- 缓存身份**不只是字节**：环境标识绝不进前缀；工具列表顺序、客户端 HTTP 头指纹与请求参数都算缓存身份；跨会话预热不可能。
- "错的代价不是损失"有定义域：只对"**多装了为真却用不上的内容**"成立；装进来与事实不符的东西，代价仍然是做错决策，不在这一档（正文 105 行已限定）。
- **跨轮的分支存活未经验证**：作者口径是"也许可以存在，但需要切实验证"。它的代价很具体——轮末前缀换代，跨轮活着的那个分支手里的种子就成了旧版，要去下一次未命中重读。这正是"需要验证"的所在。
- **底层压缩阈值仍在**（不关闭、只不触发）；极端情况回落线性结构。别把它读成"本架构关掉了压缩"。
- **段序（易变段后置）是账成立的前提**，而正文不展开它——引用"逐字节相同"时要知道它踩在这条上。
- **两个尺度的重启已经进正文**：正文 L105 那半句写明"重启点由任务边界给定（一个目标之内，每完成一轮归并又是一次）"——目标级与轮级都在里面，引用时按正文读即可。
- "机械 vs 模型"×"累积 vs 重建"的四臂对照至今没人做过——成本主张是本文自己的账，不是引文的结论。

## 7. 读错过的地方（已由作者纠正，别再犯）

1. 把"前缀"当成**一个**对象。至少要分开四样：稳定公共段 $P$ / 每一版装配出来的公共前缀 / 分支种子里 Raw Files 之前的那一段 / "最大前缀组装"这个**动作**。
2. 对"坐标"有执念：把"写地址 → Harness 按坐标取回原文装配成前缀"当成机制。实际是**主 Agent 按需存取**，前缀里不装一批靠坐标取回的原文；合并那一步更新的是**需要模型更新的那一段**。
3. 把"模型重写 vs Harness 装配"当成二选一。实际是**分段**：谁该更新谁更新，装配由 Harness 分别做。
4. 把"回读"当成纪律禁区。实际它只是"第一次被构建"那一档，是正常付费动作。
5. 以为架构是文章的核心。核心是 **Context 解耦对架构解空间的贡献**，Git 树只是一个点。
6. 以为"上下文更强"是过度主张。干净、聚焦、更短**就是**更强。
7. 以为"与调优基线同水位贵 12%"是必须承认的让步。那个水位对现状**不可达**。
8. 以为摘要必须存在（曾往正文加过一整节摘要）——作者判定它没有价值，已整节移除。
9. 把推论3（普适）读成"抽掉环境之后四步没有依据、只能从对话历史再生成，于是退化成改名"。实际它讲的是**四步的形态**：纯 chat 里上下文就是全部环境，状态仍然要**以一个个可指认的单元保存下来、像文件一样被管理**（写下来、取出来、换版、弃置），只是没有可维护的环境实体。它的价值是把"文件化管理"从"必须有文件系统"里解耦出来，与"Git 只是一个具体实现"同向。
10. 把序言里的"申报坐标"当成架构机制。那是 **dsh-price-less 插件的现行设计**（原文热尾的替代），与本文的前缀机制无关。
11. 把"不反对模型与 Harness 耦合"与"拒绝以交互对象为 Agent 主体的认知错位"当成自相矛盾。两者不在同一层：**训练层**——与特定环境/上下文形状耦合，换取训练难度下降与能力上限上升，是好事；**认知与架构层**——Harness 作为一个整体不等于 Agent 的主体，模型拟合的是特定的上下文与环境形状。要拒绝的是后一层。
12. 把"构建可被规划"限定成"只适用子 Agent 那一侧"。实际两侧都成立，差别只在粒度与触发点（见 §3）。
13. 把"任务边界"与"合并"当成两个互斥的重启触发。实际**重启点就是任务边界（＝用户目标）**，而**归并是实现途径**：一个目标可以含多轮，每轮末归并一次、前缀换代一次。两个尺度，不是两个来源。
14. 以为"归并要等全部子 Agent 完成"。实际是**不必等整个目标完成**——一轮一收；一轮之内不换前缀版本。
15. 以为不易解耦时主 Agent 会自己动手。实际**落地一律委派，哪怕不分支也委派**；回退只是分支数变少（一直到单分支）。
16. 把"理解／执行模型解耦"读成两个模型。实际解耦的是**职责**；模型一致性只是更优的情形（同模型时缓存前缀在两边命中），不是要求。
17. 把"环境维持共享"读成共享一个可写运行时。实际是**底层一份、每分支一份虚拟工作区、写时才占空间**（以内存换效率）。
18. 把 63k 那条线当成无条件判据。它以前两条成立为前提（更新量 2k、前缀按命中）；保守档下只有 34k，那时赢不下基线的纸面最优点。

## 8. 文件地图

| 文件 | 管什么 |
| --- | --- |
| `Agent架构革新…-Context-Native Agent.md` | 唯一事实来源：序言 + 正文 + 附录1/2/3 |
| 同名 `.html` / `.pdf` | 同一内容的网页版与 A4 成品（40 页、210 链接、6 张图；图与图注由渲染层生成，正文里没有"见图 x"） |
| `README.md` | 面向读者：顶上摘要的位置，外行也能读懂这篇在说什么 |
| `REVISION.md` | 改了哪里、原来是什么、改后是什么；账的数字从哪来；**怎么重建**（第六节） |
| `UNDERSTANDING.md` | 本文件：口径、易错点、边界 |
| `CITATION.cff` | 引用信息（版本与正文口径同步） |
